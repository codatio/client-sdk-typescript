/* tslint:disable */
/* eslint-disable */
/**
 * Bill pay (synchronous solution)
 * The API reference for the synchronous Bill Pay solution.   The synchronous Bill Pay solution is an API and a set of supporting tools designed to integrate a bill pay flow into your app as quickly as possible. It\'s ideal for facilitating essential bill payment processes within your SMB\'s accounting software.  [Explore solution](https://docs.codat.io/payables/overview) | [See OpenAPI spec](https://github.com/codatio/oas)  --- ## Supported Integrations  | Integration                   | Supported | |-------------------------------|-----------| | FreeAgent                     | Yes       | | QuickBooks Online             | Yes       | | Oracle NetSuite               | Yes       | | Sage Intacct                  | Yes       | | Xero                          | Yes       | | Zoho Books                    | Yes       |  --- <!-- Start Codat Tags Table --> ## Endpoints  | Endpoints | Description | | :- |:- | | Companies | Create and manage your SMB users\' companies. | | Connections | Create new and manage existing data connections for a company. | | Company information | View company profile from the source platform. | | Bills | Get, create, and update Bills. | | Bill payments | Get, create, and update Bill payments. | | Suppliers | Get, create, and update Suppliers. | | Bank accounts | Create a bank account for a given company\'s connection. | <!-- End Codat Tags Table -->
 *
 * The version of the OpenAPI document: 3.0.0
 * Contact: support@codat.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../../../runtime';
import type { BankAccountType } from './bankaccounttype';
import {
    BankAccountTypeFromJSON,
    BankAccountTypeFromJSONTyped,
    BankAccountTypeToJSON,
    BankAccountTypeToJSONTyped,
} from './bankaccounttype';
import type { BankAccountStatus } from './bankaccountstatus';
import {
    BankAccountStatusFromJSON,
    BankAccountStatusFromJSONTyped,
    BankAccountStatusToJSON,
    BankAccountStatusToJSONTyped,
} from './bankaccountstatus';

/**
 * 
 * @export
 * @interface BankAccount
 */
export interface BankAccount {
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof BankAccount
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the bank account, unique for the company in the accounting software.
     * @type {string}
     * @memberof BankAccount
     */
    id?: string;
    /**
     * Code used to identify each nominal account for a business.
     * @type {string}
     * @memberof BankAccount
     */
    nominalCode?: string | null;
    /**
     * Name of the bank account in the accounting software.
     * @type {string}
     * @memberof BankAccount
     */
    name?: string | null;
    /**
     * 
     * @type {BankAccountType}
     * @memberof BankAccount
     */
    accountType?: BankAccountType;
    /**
     * Account number for the bank account.
     * 
     * Xero integrations
     * Only a UK account number shows for bank accounts with GBP currency and a combined total of sort code and account number that equals 14 digits, For non-GBP accounts, the full bank account number is populated.
     * @type {string}
     * @memberof BankAccount
     */
    accountNumber?: string | null;
    /**
     * Sort code for the bank account. This is relevant to UK bank accounts.
     * 
     * Xero integrations
     * The sort code is only displayed when the currency = GBP and the sort code and account number sum to 14 digits. For non-GBP accounts, this field is not populated.
     * @type {string}
     * @memberof BankAccount
     */
    sortCode?: string | null;
    /**
     * The currency data type in Codat is the [ISO 4217](https://en.wikipedia.org/wiki/ISO_4217) currency code, e.g. _GBP_.
     * 
     * ## Unknown currencies
     * 
     * In line with the ISO 4217 specification, the code _XXX_ is used when the data source does not return a currency for a transaction. 
     * 
     * There are only a very small number of edge cases where this currency code is returned by the Codat system.
     * @type {string}
     * @memberof BankAccount
     */
    currency?: string;
    /**
     * 
     * @type {BankAccountStatus}
     * @memberof BankAccount
     */
    status?: BankAccountStatus;
}



/**
 * Check if a given object implements the BankAccount interface.
 */
export function instanceOfBankAccount(value: object): value is BankAccount {
    return true;
}

export function BankAccountFromJSON(json: any): BankAccount {
    return BankAccountFromJSONTyped(json, false);
}

export function BankAccountFromJSONTyped(json: any, ignoreDiscriminator: boolean): BankAccount {
    if (json == null) {
        return json;
    }
    return {
        
        ...(json['sourceModifiedDate'] !== undefined ? { 'sourceModifiedDate': json['sourceModifiedDate'] == null ? undefined : json['sourceModifiedDate'] } : {}),
        ...(json['id'] !== undefined ? { 'id': json['id'] == null ? undefined : json['id'] } : {}),
        ...(json['nominalCode'] !== undefined ? { 'nominalCode': json['nominalCode'] } : {}),
        ...(json['name'] !== undefined ? { 'name': json['name'] } : {}),
        ...(json['accountType'] !== undefined ? { 'accountType': json['accountType'] == null ? undefined : BankAccountTypeFromJSON(json['accountType']) } : {}),
        ...(json['accountNumber'] !== undefined ? { 'accountNumber': json['accountNumber'] } : {}),
        ...(json['sortCode'] !== undefined ? { 'sortCode': json['sortCode'] } : {}),
        ...(json['currency'] !== undefined ? { 'currency': json['currency'] == null ? undefined : json['currency'] } : {}),
        ...(json['status'] !== undefined ? { 'status': json['status'] == null ? undefined : BankAccountStatusFromJSON(json['status']) } : {}),
    };
}

export function BankAccountToJSON(json: any): BankAccount {
    return BankAccountToJSONTyped(json, false);
}

export function BankAccountToJSONTyped(value?: BankAccount | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'sourceModifiedDate': value['sourceModifiedDate'],
        'id': value['id'],
        'nominalCode': value['nominalCode'],
        'name': value['name'],
        'accountType': BankAccountTypeToJSON(value['accountType']),
        'accountNumber': value['accountNumber'],
        'sortCode': value['sortCode'],
        'currency': value['currency'],
        'status': BankAccountStatusToJSON(value['status']),
    };
}


export interface BankAccountBankAccounts {
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof BankAccount
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the bank account, unique for the company in the accounting software.
     * @type {string}
     * @memberof BankAccount
     */
    id?: string;
    /**
     * Code used to identify each nominal account for a business.
     * @type {string}
     * @memberof BankAccount
     */
    nominalCode?: string | null;
    /**
     * Name of the bank account in the accounting software.
     * @type {string}
     * @memberof BankAccount
     */
    name?: string | null;
    /**
     * 
     * @type {BankAccountType}
     * @memberof BankAccount
     */
    accountType?: BankAccountType;
    /**
     * Account number for the bank account.
     * 
     * Xero integrations
     * Only a UK account number shows for bank accounts with GBP currency and a combined total of sort code and account number that equals 14 digits, For non-GBP accounts, the full bank account number is populated.
     * @type {string}
     * @memberof BankAccount
     */
    accountNumber?: string | null;
    /**
     * Sort code for the bank account. This is relevant to UK bank accounts.
     * 
     * Xero integrations
     * The sort code is only displayed when the currency = GBP and the sort code and account number sum to 14 digits. For non-GBP accounts, this field is not populated.
     * @type {string}
     * @memberof BankAccount
     */
    sortCode?: string | null;
    /**
     * The currency data type in Codat is the [ISO 4217](https://en.wikipedia.org/wiki/ISO_4217) currency code, e.g. _GBP_.
     * 
     * ## Unknown currencies
     * 
     * In line with the ISO 4217 specification, the code _XXX_ is used when the data source does not return a currency for a transaction. 
     * 
     * There are only a very small number of edge cases where this currency code is returned by the Codat system.
     * @type {string}
     * @memberof BankAccount
     */
    currency?: string;
    /**
     * 
     * @type {BankAccountStatus}
     * @memberof BankAccount
     */
    status?: BankAccountStatus;
}

export interface BankAccountCreateResponseBankAccount {
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof BankAccount
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the bank account, unique for the company in the accounting software.
     * @type {string}
     * @memberof BankAccount
     */
    id?: string;
    /**
     * Code used to identify each nominal account for a business.
     * @type {string}
     * @memberof BankAccount
     */
    nominalCode?: string | null;
    /**
     * Name of the bank account in the accounting software.
     * @type {string}
     * @memberof BankAccount
     */
    name?: string | null;
    /**
     * 
     * @type {BankAccountType}
     * @memberof BankAccount
     */
    accountType?: BankAccountType;
    /**
     * Account number for the bank account.
     * 
     * Xero integrations
     * Only a UK account number shows for bank accounts with GBP currency and a combined total of sort code and account number that equals 14 digits, For non-GBP accounts, the full bank account number is populated.
     * @type {string}
     * @memberof BankAccount
     */
    accountNumber?: string | null;
    /**
     * Sort code for the bank account. This is relevant to UK bank accounts.
     * 
     * Xero integrations
     * The sort code is only displayed when the currency = GBP and the sort code and account number sum to 14 digits. For non-GBP accounts, this field is not populated.
     * @type {string}
     * @memberof BankAccount
     */
    sortCode?: string | null;
    /**
     * The currency data type in Codat is the [ISO 4217](https://en.wikipedia.org/wiki/ISO_4217) currency code, e.g. _GBP_.
     * 
     * ## Unknown currencies
     * 
     * In line with the ISO 4217 specification, the code _XXX_ is used when the data source does not return a currency for a transaction. 
     * 
     * There are only a very small number of edge cases where this currency code is returned by the Codat system.
     * @type {string}
     * @memberof BankAccount
     */
    currency?: string;
    /**
     * 
     * @type {BankAccountStatus}
     * @memberof BankAccount
     */
    status?: BankAccountStatus;
}

export interface BankAccountCreateResponseBankAccounts {
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof BankAccount
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the bank account, unique for the company in the accounting software.
     * @type {string}
     * @memberof BankAccount
     */
    id?: string;
    /**
     * Code used to identify each nominal account for a business.
     * @type {string}
     * @memberof BankAccount
     */
    nominalCode?: string | null;
    /**
     * Name of the bank account in the accounting software.
     * @type {string}
     * @memberof BankAccount
     */
    name?: string | null;
    /**
     * 
     * @type {BankAccountType}
     * @memberof BankAccount
     */
    accountType?: BankAccountType;
    /**
     * Account number for the bank account.
     * 
     * Xero integrations
     * Only a UK account number shows for bank accounts with GBP currency and a combined total of sort code and account number that equals 14 digits, For non-GBP accounts, the full bank account number is populated.
     * @type {string}
     * @memberof BankAccount
     */
    accountNumber?: string | null;
    /**
     * Sort code for the bank account. This is relevant to UK bank accounts.
     * 
     * Xero integrations
     * The sort code is only displayed when the currency = GBP and the sort code and account number sum to 14 digits. For non-GBP accounts, this field is not populated.
     * @type {string}
     * @memberof BankAccount
     */
    sortCode?: string | null;
    /**
     * The currency data type in Codat is the [ISO 4217](https://en.wikipedia.org/wiki/ISO_4217) currency code, e.g. _GBP_.
     * 
     * ## Unknown currencies
     * 
     * In line with the ISO 4217 specification, the code _XXX_ is used when the data source does not return a currency for a transaction. 
     * 
     * There are only a very small number of edge cases where this currency code is returned by the Codat system.
     * @type {string}
     * @memberof BankAccount
     */
    currency?: string;
    /**
     * 
     * @type {BankAccountStatus}
     * @memberof BankAccount
     */
    status?: BankAccountStatus;
}

export interface BankAccounts {
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof BankAccount
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the bank account, unique for the company in the accounting software.
     * @type {string}
     * @memberof BankAccount
     */
    id?: string;
    /**
     * Code used to identify each nominal account for a business.
     * @type {string}
     * @memberof BankAccount
     */
    nominalCode?: string | null;
    /**
     * Name of the bank account in the accounting software.
     * @type {string}
     * @memberof BankAccount
     */
    name?: string | null;
    /**
     * 
     * @type {BankAccountType}
     * @memberof BankAccount
     */
    accountType?: BankAccountType;
    /**
     * Account number for the bank account.
     * 
     * Xero integrations
     * Only a UK account number shows for bank accounts with GBP currency and a combined total of sort code and account number that equals 14 digits, For non-GBP accounts, the full bank account number is populated.
     * @type {string}
     * @memberof BankAccount
     */
    accountNumber?: string | null;
    /**
     * Sort code for the bank account. This is relevant to UK bank accounts.
     * 
     * Xero integrations
     * The sort code is only displayed when the currency = GBP and the sort code and account number sum to 14 digits. For non-GBP accounts, this field is not populated.
     * @type {string}
     * @memberof BankAccount
     */
    sortCode?: string | null;
    /**
     * The currency data type in Codat is the [ISO 4217](https://en.wikipedia.org/wiki/ISO_4217) currency code, e.g. _GBP_.
     * 
     * ## Unknown currencies
     * 
     * In line with the ISO 4217 specification, the code _XXX_ is used when the data source does not return a currency for a transaction. 
     * 
     * There are only a very small number of edge cases where this currency code is returned by the Codat system.
     * @type {string}
     * @memberof BankAccount
     */
    currency?: string;
    /**
     * 
     * @type {BankAccountStatus}
     * @memberof BankAccount
     */
    status?: BankAccountStatus;
}

export interface CreateBankAccountResponseBankAccount {
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof BankAccount
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the bank account, unique for the company in the accounting software.
     * @type {string}
     * @memberof BankAccount
     */
    id?: string;
    /**
     * Code used to identify each nominal account for a business.
     * @type {string}
     * @memberof BankAccount
     */
    nominalCode?: string | null;
    /**
     * Name of the bank account in the accounting software.
     * @type {string}
     * @memberof BankAccount
     */
    name?: string | null;
    /**
     * 
     * @type {BankAccountType}
     * @memberof BankAccount
     */
    accountType?: BankAccountType;
    /**
     * Account number for the bank account.
     * 
     * Xero integrations
     * Only a UK account number shows for bank accounts with GBP currency and a combined total of sort code and account number that equals 14 digits, For non-GBP accounts, the full bank account number is populated.
     * @type {string}
     * @memberof BankAccount
     */
    accountNumber?: string | null;
    /**
     * Sort code for the bank account. This is relevant to UK bank accounts.
     * 
     * Xero integrations
     * The sort code is only displayed when the currency = GBP and the sort code and account number sum to 14 digits. For non-GBP accounts, this field is not populated.
     * @type {string}
     * @memberof BankAccount
     */
    sortCode?: string | null;
    /**
     * The currency data type in Codat is the [ISO 4217](https://en.wikipedia.org/wiki/ISO_4217) currency code, e.g. _GBP_.
     * 
     * ## Unknown currencies
     * 
     * In line with the ISO 4217 specification, the code _XXX_ is used when the data source does not return a currency for a transaction. 
     * 
     * There are only a very small number of edge cases where this currency code is returned by the Codat system.
     * @type {string}
     * @memberof BankAccount
     */
    currency?: string;
    /**
     * 
     * @type {BankAccountStatus}
     * @memberof BankAccount
     */
    status?: BankAccountStatus;
}

export interface CreateBankAccountResponseBankAccounts {
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof BankAccount
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the bank account, unique for the company in the accounting software.
     * @type {string}
     * @memberof BankAccount
     */
    id?: string;
    /**
     * Code used to identify each nominal account for a business.
     * @type {string}
     * @memberof BankAccount
     */
    nominalCode?: string | null;
    /**
     * Name of the bank account in the accounting software.
     * @type {string}
     * @memberof BankAccount
     */
    name?: string | null;
    /**
     * 
     * @type {BankAccountType}
     * @memberof BankAccount
     */
    accountType?: BankAccountType;
    /**
     * Account number for the bank account.
     * 
     * Xero integrations
     * Only a UK account number shows for bank accounts with GBP currency and a combined total of sort code and account number that equals 14 digits, For non-GBP accounts, the full bank account number is populated.
     * @type {string}
     * @memberof BankAccount
     */
    accountNumber?: string | null;
    /**
     * Sort code for the bank account. This is relevant to UK bank accounts.
     * 
     * Xero integrations
     * The sort code is only displayed when the currency = GBP and the sort code and account number sum to 14 digits. For non-GBP accounts, this field is not populated.
     * @type {string}
     * @memberof BankAccount
     */
    sortCode?: string | null;
    /**
     * The currency data type in Codat is the [ISO 4217](https://en.wikipedia.org/wiki/ISO_4217) currency code, e.g. _GBP_.
     * 
     * ## Unknown currencies
     * 
     * In line with the ISO 4217 specification, the code _XXX_ is used when the data source does not return a currency for a transaction. 
     * 
     * There are only a very small number of edge cases where this currency code is returned by the Codat system.
     * @type {string}
     * @memberof BankAccount
     */
    currency?: string;
    /**
     * 
     * @type {BankAccountStatus}
     * @memberof BankAccount
     */
    status?: BankAccountStatus;
}

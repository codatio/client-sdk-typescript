/* tslint:disable */
/* eslint-disable */
/**
 * Platform API
 * An API for the common components of all of Codat\'s products.  These end points cover creating and managing your companies, data connections, and integrations.  [Read about the building blocks of Codat...](https://docs.codat.io/core-concepts/companies) | [See our OpenAPI spec](https://github.com/codatio/oas)   --- <!-- Start Codat Tags Table --> ## Endpoints  | Endpoints | Description | | :- |:- | | Companies | Create and manage your SMB users\' companies. | | Connections | Create new and manage existing data connections for a company. | | Connection management | Configure connection management UI and retrieve access tokens for authentication. | | Webhooks | Create and manage webhooks that listen to Codat\'s events. | | Integrations | Get a list of integrations supported by Codat and their logos. | | Refresh data | Initiate data refreshes, view pull status and history. | | Settings | Manage company profile configuration, sync settings, and API keys. | | Push data | Initiate and monitor Create, Update, and Delete operations. | | Supplemental data | Configure and pull additional data you can include in Codat\'s standard data types. | | Custom data type | Configure and pull additional data types that are not included in Codat\'s standardized data model. | <!-- End Codat Tags Table -->
 *
 * The version of the OpenAPI document: 3.0.0
 * Contact: support@codat.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { CompanyAccessToken } from '../sdk/models/index';
import * as runtime from '../runtime';
import type * as shared from '../sdk/models/index';
import type {
  AccessToken,
  Company,
  CompanyRequestBody,
  CompanySyncSettings,
  CompanyUpdateRequest,
  ErrorMessage,
  RefreshProductDataRequestBody,
  SetCompanySyncSettingsRequestBody,
} from '../sdk/models/index';
import {
    AccessTokenFromJSON,
    AccessTokenToJSON,
    CompaniesFromJSON,
    CompaniesToJSON,
    CompanyFromJSON,
    CompanyToJSON,
    CompanyRequestBodyFromJSON,
    CompanyRequestBodyToJSON,
    CompanySyncSettingsFromJSON,
    CompanySyncSettingsToJSON,
    CompanyUpdateRequestFromJSON,
    CompanyUpdateRequestToJSON,
    ErrorMessageFromJSON,
    ErrorMessageToJSON,
    RefreshProductDataRequestBodyFromJSON,
    RefreshProductDataRequestBodyToJSON,
    SetCompanySyncSettingsRequestBodyFromJSON,
    SetCompanySyncSettingsRequestBodyToJSON,
} from '../sdk/models/index';

export interface AddProductRequest {
    companyId: string;
    productIdentifier: string;
}

export interface CreateCompanyRequest {
    companyRequestBody?: CompanyRequestBody;
}

export interface DeleteCompanyRequest {
    companyId: string;
}

export interface GetCompanyRequest {
    companyId: string;
}

export interface GetCompanyAccessTokenRequest {
    companyId: string;
}

export interface GetCompanySyncSettingsRequest {
    companyId: string;
}

export interface ListCompaniesRequest {
    page?: number;
    pageSize?: number;
    query?: string;
    orderBy?: string;
    tags?: string;
}

export interface RefreshProductDataRequest {
    companyId: string;
    productIdentifier: string;
    requestBody?: RefreshProductDataRequestBody;
}

export interface RemoveProductRequest {
    companyId: string;
    productIdentifier: string;
}

export interface ReplaceCompanyRequest {
    companyId: string;
    companyRequestBody?: CompanyRequestBody;
}

export interface SetCompanySyncSettingsRequest {
    companyId: string;
    requestBody?: SetCompanySyncSettingsRequestBody;
}

export interface UpdateCompanyRequest {
    companyId: string;
    companyUpdateRequest?: CompanyUpdateRequest;
}

/**
 * 
 */
export class Companies extends runtime.BaseAPI {

    /**
     * Use the *Add product* endpoint to enable a product for the company specified by `companyId`.  > Note: This feature is currently in alpha and available only to participants in the development program.
     * Add product
     */
    private async addProductRaw(requestParameters: AddProductRequest, options?: runtime.RequestOptions): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['companyId'] == null) {
            throw new runtime.RequiredError(
                'companyId',
                'Required parameter "companyId" was null or undefined when calling addProduct().'
            );
        }

        if (requestParameters['productIdentifier'] == null) {
            throw new runtime.RequiredError(
                'productIdentifier',
                'Required parameter "productIdentifier" was null or undefined when calling addProduct().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};
        headerParameters['Accept'] = 'application/json';

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["Authorization"] = await this.configuration.apiKey("Authorization"); // auth_header authentication
        }

        const response = await this.request({
            path: `/companies/{companyId}/products/{productIdentifier}`.replace(`{${"companyId"}}`, encodeURIComponent(String(requestParameters['companyId']))).replace(`{${"productIdentifier"}}`, encodeURIComponent(String(requestParameters['productIdentifier']))),
            method: 'PUT',
            operationId: 'add-product',
            errorStatuses: [401, 402, 403, 404, 429, 500, 503, ],
            headers: headerParameters,
            query: queryParameters,
        }, options);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Use the *Add product* endpoint to enable a product for the company specified by `companyId`.  > Note: This feature is currently in alpha and available only to participants in the development program.
     * Add product
     */
    async addProduct(requestParameters: AddProductRequest, options?: runtime.RequestOptions): Promise<void> {
        await this.addProductRaw(requestParameters, options);
    }

    /**
     * ﻿Use the *Create company* endpoint to create a new [company](https://docs.codat.io/platform-api#/schemas/Company) that represents your customer in Codat.   A [company](https://docs.codat.io/platform-api#/schemas/Company) represents a business sharing access to their data. Each company can have multiple [connections](https://docs.codat.io/platform-api#/schemas/Connection) to different data sources, such as one connection to Xero for accounting data, two connections to Plaid for two bank accounts, and a connection to Zettle for POS data.  If forbidden characters (see `name` pattern) are present in the request, a company will be created with the forbidden characters removed. For example, `Company (Codat[1])` with be created as `Company Codat1`.
     * Create company
     */
    private async createCompanyRaw(requestParameters: CreateCompanyRequest, options?: runtime.RequestOptions): Promise<runtime.ApiResponse<Company>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};
        headerParameters['Accept'] = 'application/json';

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["Authorization"] = await this.configuration.apiKey("Authorization"); // auth_header authentication
        }

        const response = await this.request({
            path: `/companies`,
            method: 'POST',
            operationId: 'create-company',
            errorStatuses: [400, 401, 402, 403, 429, 500, 503, ],
            headers: headerParameters,
            query: queryParameters,
            body: CompanyRequestBodyToJSON(requestParameters['companyRequestBody']),
        }, options);

        return new runtime.JSONApiResponse(response, (jsonValue) => CompanyFromJSON(jsonValue));
    }

    /**
     * ﻿Use the *Create company* endpoint to create a new [company](https://docs.codat.io/platform-api#/schemas/Company) that represents your customer in Codat.   A [company](https://docs.codat.io/platform-api#/schemas/Company) represents a business sharing access to their data. Each company can have multiple [connections](https://docs.codat.io/platform-api#/schemas/Connection) to different data sources, such as one connection to Xero for accounting data, two connections to Plaid for two bank accounts, and a connection to Zettle for POS data.  If forbidden characters (see `name` pattern) are present in the request, a company will be created with the forbidden characters removed. For example, `Company (Codat[1])` with be created as `Company Codat1`.
     * Create company
     */
    async create(companyRequestBody?: CompanyRequestBody, options?: runtime.RequestOptions): Promise<Company> {
        const response = await this.createCompanyRaw({ companyRequestBody: companyRequestBody }, options);
        return await response.value();
    }

    /**
     * ﻿The *Delete company* endpoint permanently deletes a [company](https://docs.codat.io/platform-api#/schemas/Company), its [connections](https://docs.codat.io/platform-api#/schemas/Connection) and any cached data. This operation is irreversible.  A [company](https://docs.codat.io/platform-api#/schemas/Company) represents a business sharing access to their data. Each company can have multiple [connections](https://docs.codat.io/platform-api#/schemas/Connection) to different data sources, such as one connection to Xero for accounting data, two connections to Plaid for two bank accounts, and a connection to Zettle for POS data. 
     * Delete a company
     */
    private async deleteCompanyRaw(requestParameters: DeleteCompanyRequest, options?: runtime.RequestOptions): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['companyId'] == null) {
            throw new runtime.RequiredError(
                'companyId',
                'Required parameter "companyId" was null or undefined when calling deleteCompany().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};
        headerParameters['Accept'] = 'application/json';

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["Authorization"] = await this.configuration.apiKey("Authorization"); // auth_header authentication
        }

        const response = await this.request({
            path: `/companies/{companyId}`.replace(`{${"companyId"}}`, encodeURIComponent(String(requestParameters['companyId']))),
            method: 'DELETE',
            operationId: 'delete-company',
            errorStatuses: [401, 402, 403, 404, 429, 500, 503, ],
            headers: headerParameters,
            query: queryParameters,
        }, options);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * ﻿The *Delete company* endpoint permanently deletes a [company](https://docs.codat.io/platform-api#/schemas/Company), its [connections](https://docs.codat.io/platform-api#/schemas/Connection) and any cached data. This operation is irreversible.  A [company](https://docs.codat.io/platform-api#/schemas/Company) represents a business sharing access to their data. Each company can have multiple [connections](https://docs.codat.io/platform-api#/schemas/Connection) to different data sources, such as one connection to Xero for accounting data, two connections to Plaid for two bank accounts, and a connection to Zettle for POS data. 
     * Delete a company
     */
    async delete(requestParameters: DeleteCompanyRequest, options?: runtime.RequestOptions): Promise<void> {
        await this.deleteCompanyRaw(requestParameters, options);
    }

    /**
     * ﻿The *Get company* endpoint returns a single company for a given `companyId`.  A [company](https://docs.codat.io/platform-api#/schemas/Company) represents a business sharing access to their data. Each company can have multiple [connections](https://docs.codat.io/platform-api#/schemas/Connection) to different data sources, such as one connection to Xero for accounting data, two connections to Plaid for two bank accounts, and a connection to Zettle for POS data. 
     * Get company
     */
    private async getCompanyRaw(requestParameters: GetCompanyRequest, options?: runtime.RequestOptions): Promise<runtime.ApiResponse<Company>> {
        if (requestParameters['companyId'] == null) {
            throw new runtime.RequiredError(
                'companyId',
                'Required parameter "companyId" was null or undefined when calling getCompany().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};
        headerParameters['Accept'] = 'application/json';

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["Authorization"] = await this.configuration.apiKey("Authorization"); // auth_header authentication
        }

        const response = await this.request({
            path: `/companies/{companyId}`.replace(`{${"companyId"}}`, encodeURIComponent(String(requestParameters['companyId']))),
            method: 'GET',
            operationId: 'get-company',
            errorStatuses: [401, 402, 403, 404, 429, 500, 503, ],
            headers: headerParameters,
            query: queryParameters,
        }, options);

        return new runtime.JSONApiResponse(response, (jsonValue) => CompanyFromJSON(jsonValue));
    }

    /**
     * ﻿The *Get company* endpoint returns a single company for a given `companyId`.  A [company](https://docs.codat.io/platform-api#/schemas/Company) represents a business sharing access to their data. Each company can have multiple [connections](https://docs.codat.io/platform-api#/schemas/Connection) to different data sources, such as one connection to Xero for accounting data, two connections to Plaid for two bank accounts, and a connection to Zettle for POS data. 
     * Get company
     */
    async get(requestParameters: GetCompanyRequest, options?: runtime.RequestOptions): Promise<Company> {
        const response = await this.getCompanyRaw(requestParameters, options);
        return await response.value();
    }

    /**
     * Use the _Get company access token_ endpoint to return an access token for the specified company ID. The token is valid for one day.   The token is required by Codat\'s embeddable UIs (such as [Connections SDK](https://docs.codat.io/auth-flow/optimize/connection-management) and [Link SDK](https://docs.codat.io/auth-flow/authorize-embedded-link)) to verify the identity of the user and improve the reliability of data provided by them.
     * Get company access token
     */
    private async getCompanyAccessTokenRaw(requestParameters: GetCompanyAccessTokenRequest, options?: runtime.RequestOptions): Promise<runtime.ApiResponse<AccessToken>> {
        if (requestParameters['companyId'] == null) {
            throw new runtime.RequiredError(
                'companyId',
                'Required parameter "companyId" was null or undefined when calling getCompanyAccessToken().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};
        headerParameters['Accept'] = 'application/json';

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["Authorization"] = await this.configuration.apiKey("Authorization"); // auth_header authentication
        }

        const response = await this.request({
            path: `/companies/{companyId}/accessToken`.replace(`{${"companyId"}}`, encodeURIComponent(String(requestParameters['companyId']))),
            method: 'GET',
            operationId: 'get-company-access-token',
            errorStatuses: [401, 402, 403, 404, 429, 500, 503, ],
            headers: headerParameters,
            query: queryParameters,
        }, options);

        return new runtime.JSONApiResponse(response, (jsonValue) => AccessTokenFromJSON(jsonValue));
    }

    /**
     * Use the _Get company access token_ endpoint to return an access token for the specified company ID. The token is valid for one day.   The token is required by Codat\'s embeddable UIs (such as [Connections SDK](https://docs.codat.io/auth-flow/optimize/connection-management) and [Link SDK](https://docs.codat.io/auth-flow/authorize-embedded-link)) to verify the identity of the user and improve the reliability of data provided by them.
     * Get company access token
     */
    async getAccessToken(requestParameters: GetCompanyAccessTokenRequest, options?: runtime.RequestOptions): Promise<shared.CompanyAccessToken> {
        const response = await this.getCompanyAccessTokenRaw(requestParameters, options);
        return await response.value();
    }

    /**
     * Retrieve the [sync settings](https://docs.codat.io/knowledge-base/advanced-sync-settings) for a specific company. This includes how often data types should be queued to be updated, and how much history should be fetched.
     * Get company sync settings
     */
    private async getCompanySyncSettingsRaw(requestParameters: GetCompanySyncSettingsRequest, options?: runtime.RequestOptions): Promise<runtime.ApiResponse<CompanySyncSettings>> {
        if (requestParameters['companyId'] == null) {
            throw new runtime.RequiredError(
                'companyId',
                'Required parameter "companyId" was null or undefined when calling getCompanySyncSettings().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};
        headerParameters['Accept'] = 'application/json';

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["Authorization"] = await this.configuration.apiKey("Authorization"); // auth_header authentication
        }

        const response = await this.request({
            path: `/companies/{companyId}/syncSettings`.replace(`{${"companyId"}}`, encodeURIComponent(String(requestParameters['companyId']))),
            method: 'GET',
            operationId: 'get-company-syncSettings',
            errorStatuses: [401, 402, 403, 404, 429, 500, 503, ],
            headers: headerParameters,
            query: queryParameters,
        }, options);

        return new runtime.JSONApiResponse(response, (jsonValue) => CompanySyncSettingsFromJSON(jsonValue));
    }

    /**
     * Retrieve the [sync settings](https://docs.codat.io/knowledge-base/advanced-sync-settings) for a specific company. This includes how often data types should be queued to be updated, and how much history should be fetched.
     * Get company sync settings
     */
    async getCompanySyncSettings(requestParameters: GetCompanySyncSettingsRequest, options?: runtime.RequestOptions): Promise<CompanySyncSettings> {
        const response = await this.getCompanySyncSettingsRaw(requestParameters, options);
        return await response.value();
    }

    /**
     * ﻿The *List companies* endpoint returns a list of [companies](https://docs.codat.io/platform-api#/schemas/Company) associated to your instances.  A [company](https://docs.codat.io/platform-api#/schemas/Company) represents a business sharing access to their data. Each company can have multiple [connections](https://docs.codat.io/platform-api#/schemas/Connection) to different data sources, such as one connection to Xero for accounting data, two connections to Plaid for two bank accounts, and a connection to Zettle for POS data.  ## Filter by tags  The *List companies* endpoint supports the filtering of companies using [tags](https://docs.codat.io/using-the-api/managing-companies#add-metadata-to-a-company). It supports the following operators with [Codat’s query language](https://docs.codat.io/using-the-api/querying):  - equals (`=`) - not equals (`!=`) - contains (`~`)  For example, you can use the querying to filter companies tagged with a specific foreign key, region, or owning team:  - Foreign key: `uid = {yourCustomerId}` - Region: `region != uk` - Owning team and region: `region = uk && owningTeam = invoice-finance`
     * List companies
     */
    private async listCompaniesRaw(requestParameters: ListCompaniesRequest, options?: runtime.RequestOptions): Promise<runtime.ApiResponse<shared.Companies>> {
        const queryParameters: any = {};

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }
        else {
            queryParameters['page'] = 1;
        }

        if (requestParameters['pageSize'] != null) {
            queryParameters['pageSize'] = requestParameters['pageSize'];
        }
        else {
            queryParameters['pageSize'] = 100;
        }

        if (requestParameters['query'] != null) {
            queryParameters['query'] = requestParameters['query'];
        }

        if (requestParameters['orderBy'] != null) {
            queryParameters['orderBy'] = requestParameters['orderBy'];
        }

        if (requestParameters['tags'] != null) {
            queryParameters['tags'] = requestParameters['tags'];
        }

        const headerParameters: runtime.HTTPHeaders = {};
        headerParameters['Accept'] = 'application/json';

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["Authorization"] = await this.configuration.apiKey("Authorization"); // auth_header authentication
        }

        const response = await this.request({
            path: `/companies`,
            method: 'GET',
            operationId: 'list-companies',
            errorStatuses: [400, 401, 402, 403, 404, 429, 500, 503, ],
            headers: headerParameters,
            query: queryParameters,
        }, options);

        return new runtime.JSONApiResponse(response, (jsonValue) => CompaniesFromJSON(jsonValue));
    }

    /**
     * ﻿The *List companies* endpoint returns a list of [companies](https://docs.codat.io/platform-api#/schemas/Company) associated to your instances.  A [company](https://docs.codat.io/platform-api#/schemas/Company) represents a business sharing access to their data. Each company can have multiple [connections](https://docs.codat.io/platform-api#/schemas/Connection) to different data sources, such as one connection to Xero for accounting data, two connections to Plaid for two bank accounts, and a connection to Zettle for POS data.  ## Filter by tags  The *List companies* endpoint supports the filtering of companies using [tags](https://docs.codat.io/using-the-api/managing-companies#add-metadata-to-a-company). It supports the following operators with [Codat’s query language](https://docs.codat.io/using-the-api/querying):  - equals (`=`) - not equals (`!=`) - contains (`~`)  For example, you can use the querying to filter companies tagged with a specific foreign key, region, or owning team:  - Foreign key: `uid = {yourCustomerId}` - Region: `region != uk` - Owning team and region: `region = uk && owningTeam = invoice-finance`
     * List companies
     */
    async list(requestParameters: ListCompaniesRequest = {}, options?: runtime.RequestOptions): Promise<shared.Companies> {
        const response = await this.listCompaniesRaw(requestParameters, options);
        return await response.value();
    }

    /**
     * Use the **Refresh product data** endpoint to manually refresh data for a custom product for a specific company.  ### Tips and traps  - This endpoint only supports refreshing data for **custom products** and can\'t be used for Codat\'s standard solutions. Refer to [individual solutions\' documentation](https://docs.codat.io/) instead. - If a data sync is already in progress for a custom product, the refresh request will return a `Bad request (400)` response. - If a company has multiple custom products enabled, you can refresh data for each product individually.  - Optionally include a request body with `dataTypes` to refresh only selected data types for the specified product. If omitted, the product\'s scheduled refresh is triggered as usual.  - When specifying `dataTypes`, each value must be a valid data type supported by the product. Invalid values will result in a `Bad request (400)` response listing valid options.
     * Refresh product data
     */
    private async refreshProductDataRaw(requestParameters: RefreshProductDataRequest, options?: runtime.RequestOptions): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['companyId'] == null) {
            throw new runtime.RequiredError(
                'companyId',
                'Required parameter "companyId" was null or undefined when calling refreshProductData().'
            );
        }

        if (requestParameters['productIdentifier'] == null) {
            throw new runtime.RequiredError(
                'productIdentifier',
                'Required parameter "productIdentifier" was null or undefined when calling refreshProductData().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};
        headerParameters['Accept'] = 'application/json';

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["Authorization"] = await this.configuration.apiKey("Authorization"); // auth_header authentication
        }

        const response = await this.request({
            path: `/companies/{companyId}/products/{productIdentifier}/refresh`.replace(`{${"companyId"}}`, encodeURIComponent(String(requestParameters['companyId']))).replace(`{${"productIdentifier"}}`, encodeURIComponent(String(requestParameters['productIdentifier']))),
            method: 'POST',
            operationId: 'refresh-product-data',
            errorStatuses: [400, 401, 402, 403, 404, 429, 500, 503, ],
            headers: headerParameters,
            query: queryParameters,
            body: RefreshProductDataRequestBodyToJSON(requestParameters['requestBody']),
        }, options);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Use the **Refresh product data** endpoint to manually refresh data for a custom product for a specific company.  ### Tips and traps  - This endpoint only supports refreshing data for **custom products** and can\'t be used for Codat\'s standard solutions. Refer to [individual solutions\' documentation](https://docs.codat.io/) instead. - If a data sync is already in progress for a custom product, the refresh request will return a `Bad request (400)` response. - If a company has multiple custom products enabled, you can refresh data for each product individually.  - Optionally include a request body with `dataTypes` to refresh only selected data types for the specified product. If omitted, the product\'s scheduled refresh is triggered as usual.  - When specifying `dataTypes`, each value must be a valid data type supported by the product. Invalid values will result in a `Bad request (400)` response listing valid options.
     * Refresh product data
     */
    async refreshProductData(requestParameters: RefreshProductDataRequest, options?: runtime.RequestOptions): Promise<void> {
        await this.refreshProductDataRaw(requestParameters, options);
    }

    /**
     * Use the *Remove product* endpoint to disable a product for the company specified by `companyId`.  > Note: This feature is currently in alpha and available only to participants in the development program.
     * Remove product
     */
    private async removeProductRaw(requestParameters: RemoveProductRequest, options?: runtime.RequestOptions): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['companyId'] == null) {
            throw new runtime.RequiredError(
                'companyId',
                'Required parameter "companyId" was null or undefined when calling removeProduct().'
            );
        }

        if (requestParameters['productIdentifier'] == null) {
            throw new runtime.RequiredError(
                'productIdentifier',
                'Required parameter "productIdentifier" was null or undefined when calling removeProduct().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};
        headerParameters['Accept'] = 'application/json';

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["Authorization"] = await this.configuration.apiKey("Authorization"); // auth_header authentication
        }

        const response = await this.request({
            path: `/companies/{companyId}/products/{productIdentifier}`.replace(`{${"companyId"}}`, encodeURIComponent(String(requestParameters['companyId']))).replace(`{${"productIdentifier"}}`, encodeURIComponent(String(requestParameters['productIdentifier']))),
            method: 'DELETE',
            operationId: 'remove-product',
            errorStatuses: [401, 402, 403, 404, 429, 500, 503, ],
            headers: headerParameters,
            query: queryParameters,
        }, options);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Use the *Remove product* endpoint to disable a product for the company specified by `companyId`.  > Note: This feature is currently in alpha and available only to participants in the development program.
     * Remove product
     */
    async removeProduct(requestParameters: RemoveProductRequest, options?: runtime.RequestOptions): Promise<void> {
        await this.removeProductRaw(requestParameters, options);
    }

    /**
     * ﻿Use the *Replace company* endpoint to replace the existing name, description, and tags of the company. Calling the endpoint will replace existing values even if new values haven\'t been defined in the payload.  A [company](https://docs.codat.io/platform-api#/schemas/Company) represents a business sharing access to their data. Each company can have multiple [connections](https://docs.codat.io/platform-api#/schemas/Connection) to different data sources, such as one connection to Xero for accounting data, two connections to Plaid for two bank accounts, and a connection to Zettle for POS data.
     * Replace company
     */
    private async replaceCompanyRaw(requestParameters: ReplaceCompanyRequest, options?: runtime.RequestOptions): Promise<runtime.ApiResponse<Company>> {
        if (requestParameters['companyId'] == null) {
            throw new runtime.RequiredError(
                'companyId',
                'Required parameter "companyId" was null or undefined when calling replaceCompany().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};
        headerParameters['Accept'] = 'application/json';

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["Authorization"] = await this.configuration.apiKey("Authorization"); // auth_header authentication
        }

        const response = await this.request({
            path: `/companies/{companyId}`.replace(`{${"companyId"}}`, encodeURIComponent(String(requestParameters['companyId']))),
            method: 'PUT',
            operationId: 'replace-company',
            errorStatuses: [401, 402, 403, 404, 429, 500, 503, ],
            headers: headerParameters,
            query: queryParameters,
            body: CompanyRequestBodyToJSON(requestParameters['companyRequestBody']),
        }, options);

        return new runtime.JSONApiResponse(response, (jsonValue) => CompanyFromJSON(jsonValue));
    }

    /**
     * ﻿Use the *Replace company* endpoint to replace the existing name, description, and tags of the company. Calling the endpoint will replace existing values even if new values haven\'t been defined in the payload.  A [company](https://docs.codat.io/platform-api#/schemas/Company) represents a business sharing access to their data. Each company can have multiple [connections](https://docs.codat.io/platform-api#/schemas/Connection) to different data sources, such as one connection to Xero for accounting data, two connections to Plaid for two bank accounts, and a connection to Zettle for POS data.
     * Replace company
     */
    async replace(requestParameters: ReplaceCompanyRequest, options?: runtime.RequestOptions): Promise<Company> {
        const response = await this.replaceCompanyRaw(requestParameters, options);
        return await response.value();
    }

    /**
     * Set the sync settings for a specific company.
     * Set company sync settings
     */
    private async setCompanySyncSettingsRaw(requestParameters: SetCompanySyncSettingsRequest, options?: runtime.RequestOptions): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['companyId'] == null) {
            throw new runtime.RequiredError(
                'companyId',
                'Required parameter "companyId" was null or undefined when calling setCompanySyncSettings().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};
        headerParameters['Accept'] = 'application/json';

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["Authorization"] = await this.configuration.apiKey("Authorization"); // auth_header authentication
        }

        const response = await this.request({
            path: `/companies/{companyId}/syncSettings`.replace(`{${"companyId"}}`, encodeURIComponent(String(requestParameters['companyId']))),
            method: 'POST',
            operationId: 'set-company-syncSettings',
            errorStatuses: [400, 401, 402, 403, 404, 429, 500, 503, ],
            headers: headerParameters,
            query: queryParameters,
            body: SetCompanySyncSettingsRequestBodyToJSON(requestParameters['requestBody']),
        }, options);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Set the sync settings for a specific company.
     * Set company sync settings
     */
    async setCompanySyncSettings(requestParameters: SetCompanySyncSettingsRequest, options?: runtime.RequestOptions): Promise<void> {
        await this.setCompanySyncSettingsRaw(requestParameters, options);
    }

    /**
     * ﻿Use the *Update company* endpoint to update the name, description, or tags of the company.  The *Update company* endpoint doesn\'t have any required fields. If any of the fields provided are `null` or not provided, they won\'t be included in the update.    A [company](https://docs.codat.io/platform-api#/schemas/Company) represents a business sharing access to their data.
     * Update company
     */
    private async updateCompanyRaw(requestParameters: UpdateCompanyRequest, options?: runtime.RequestOptions): Promise<runtime.ApiResponse<Company>> {
        if (requestParameters['companyId'] == null) {
            throw new runtime.RequiredError(
                'companyId',
                'Required parameter "companyId" was null or undefined when calling updateCompany().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};
        headerParameters['Accept'] = 'application/json';

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["Authorization"] = await this.configuration.apiKey("Authorization"); // auth_header authentication
        }

        const response = await this.request({
            path: `/companies/{companyId}`.replace(`{${"companyId"}}`, encodeURIComponent(String(requestParameters['companyId']))),
            method: 'PATCH',
            operationId: 'update-company',
            errorStatuses: [401, 402, 403, 404, 429, 500, 503, ],
            headers: headerParameters,
            query: queryParameters,
            body: CompanyUpdateRequestToJSON(requestParameters['companyUpdateRequest']),
        }, options);

        return new runtime.JSONApiResponse(response, (jsonValue) => CompanyFromJSON(jsonValue));
    }

    /**
     * ﻿Use the *Update company* endpoint to update the name, description, or tags of the company.  The *Update company* endpoint doesn\'t have any required fields. If any of the fields provided are `null` or not provided, they won\'t be included in the update.    A [company](https://docs.codat.io/platform-api#/schemas/Company) represents a business sharing access to their data.
     * Update company
     */
    async update(requestParameters: UpdateCompanyRequest, options?: runtime.RequestOptions): Promise<Company> {
        const response = await this.updateCompanyRaw(requestParameters, options);
        return await response.value();
    }

}

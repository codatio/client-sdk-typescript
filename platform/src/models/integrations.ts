/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { remap as remap$ } from "../lib/primitives.js";
import { Integration, Integration$ } from "./integration.js";
import { Links, Links$ } from "./links.js";
import * as z from "zod";

export type Integrations = {
    results?: Array<Integration> | undefined;
    /**
     * Current page number.
     */
    pageNumber: number;
    /**
     * Number of items to return in results array.
     */
    pageSize: number;
    /**
     * Total number of items.
     */
    totalResults: number;
    links: Links;
};

/** @internal */
export namespace Integrations$ {
    export const inboundSchema: z.ZodType<Integrations, z.ZodTypeDef, unknown> = z
        .object({
            results: z.array(Integration$.inboundSchema).optional(),
            pageNumber: z.number().int(),
            pageSize: z.number().int(),
            totalResults: z.number().int(),
            _links: Links$.inboundSchema,
        })
        .transform((v) => {
            return remap$(v, {
                _links: "links",
            });
        });

    export type Outbound = {
        results?: Array<Integration$.Outbound> | undefined;
        pageNumber: number;
        pageSize: number;
        totalResults: number;
        _links: Links$.Outbound;
    };

    export const outboundSchema: z.ZodType<Outbound, z.ZodTypeDef, Integrations> = z
        .object({
            results: z.array(Integration$.outboundSchema).optional(),
            pageNumber: z.number().int(),
            pageSize: z.number().int(),
            totalResults: z.number().int(),
            links: Links$.outboundSchema,
        })
        .transform((v) => {
            return remap$(v, {
                links: "_links",
            });
        });
}

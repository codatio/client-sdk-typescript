/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { GroupRef, GroupRef$ } from "./groupref";
import * as z from "zod";

export type CompanyRequestBody = {
    /**
     * Additional information about the company. This can be used to store foreign IDs, references, etc.
     */
    description?: string | undefined;
    /**
     * Reference to the groups that the company is assigned to.
     */
    groups?: Array<GroupRef> | undefined;
    /**
     * Name of company being connected.
     */
    name: string;
};

/** @internal */
export namespace CompanyRequestBody$ {
    export const inboundSchema: z.ZodType<CompanyRequestBody, z.ZodTypeDef, unknown> = z
        .object({
            description: z.string().optional(),
            groups: z.array(GroupRef$.inboundSchema).optional(),
            name: z.string(),
        })
        .transform((v) => {
            return {
                ...(v.description === undefined ? null : { description: v.description }),
                ...(v.groups === undefined ? null : { groups: v.groups }),
                name: v.name,
            };
        });

    export type Outbound = {
        description?: string | undefined;
        groups?: Array<GroupRef$.Outbound> | undefined;
        name: string;
    };

    export const outboundSchema: z.ZodType<Outbound, z.ZodTypeDef, CompanyRequestBody> = z
        .object({
            description: z.string().optional(),
            groups: z.array(GroupRef$.outboundSchema).optional(),
            name: z.string(),
        })
        .transform((v) => {
            return {
                ...(v.description === undefined ? null : { description: v.description }),
                ...(v.groups === undefined ? null : { groups: v.groups }),
                name: v.name,
            };
        });
}

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { settingsUpdateSyncSettings } from "../../funcs/settingsUpdateSyncSettings.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateProfileSyncSettingsRequestBody$inboundSchema
    .optional(),
};

export const tool$settingsUpdateSyncSettings: ToolDefinition<typeof args> = {
  name: "settings-update-sync-settings",
  description: `Update all sync settings

Update sync settings for all data types.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await settingsUpdateSyncSettings(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};

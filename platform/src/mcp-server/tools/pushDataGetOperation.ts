/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { pushDataGetOperation } from "../../funcs/pushDataGetOperation.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetPushOperationRequest$inboundSchema,
};

export const tool$pushDataGetOperation: ToolDefinition<typeof args> = {
  name: "push-data-get-operation",
  description: `Get push operation

The **Get push operation** endpoint returns a specific [push operation](/using-the-api/push) identified by the \`pushOperationKey\` that was performed on the company.

Write operations are actions that send requests to Codat, enabling the creation, updating, deletion of records, or uploading attachments in the connected accounting software.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await pushDataGetOperation(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

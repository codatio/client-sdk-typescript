/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { settingsUpdateProfile } from "../../funcs/settingsUpdateProfile.js";
import * as shared from "../../sdk/models/shared/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: shared.Profile$inboundSchema.optional(),
};

export const tool$settingsUpdateProfile: ToolDefinition<typeof args> = {
  name: "settings-update-profile",
  description: `Update profile

Update your Codat profile`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await settingsUpdateProfile(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

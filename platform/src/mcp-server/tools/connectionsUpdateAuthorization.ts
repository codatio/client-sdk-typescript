/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { connectionsUpdateAuthorization } from "../../funcs/connectionsUpdateAuthorization.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateConnectionAuthorizationRequest$inboundSchema,
};

export const tool$connectionsUpdateAuthorization: ToolDefinition<typeof args> =
  {
    name: "connections-update-authorization",
    description: `Update authorization

Update data connection's authorization.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await connectionsUpdateAuthorization(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };

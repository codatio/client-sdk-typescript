/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { connectionsList } from "../../funcs/connectionsList.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListConnectionsRequest$inboundSchema,
};

export const tool$connectionsList: ToolDefinition<typeof args> = {
  name: "connections-list",
  description: `List connections

List the connections for a company.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await connectionsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

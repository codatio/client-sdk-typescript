/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customDataTypeRefresh } from "../../funcs/customDataTypeRefresh.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RefreshCustomDataTypeRequest$inboundSchema,
};

export const tool$customDataTypeRefresh: ToolDefinition<typeof args> = {
  name: "custom-data-type-refresh",
  description: `Refresh custom data type

The *Refresh custom data type* endpoint refreshes the specified custom data type for a given company. This is an asynchronous operation that will sync updated data from the linked integration into Codat for you to view.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await customDataTypeRefresh(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

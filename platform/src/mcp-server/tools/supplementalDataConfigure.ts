/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { supplementalDataConfigure } from "../../funcs/supplementalDataConfigure.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ConfigureSupplementalDataRequest$inboundSchema,
};

export const tool$supplementalDataConfigure: ToolDefinition<typeof args> = {
  name: "supplemental-data-configure",
  description: `Configure

The *Configure* endpoint allows you to maintain or change configuration required to return supplemental data for each integration and data type combination.

[Supplemental data](https://docs.codat.io/using-the-api/supplemental-data/overview) is additional data you can include in Codat's standard data types.

**Integration-specific behavior**
See the *examples* for integration-specific frequently requested properties.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await supplementalDataConfigure(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};

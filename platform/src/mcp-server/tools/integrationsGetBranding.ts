/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsGetBranding } from "../../funcs/integrationsGetBranding.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetIntegrationsBrandingRequest$inboundSchema,
};

export const tool$integrationsGetBranding: ToolDefinition<typeof args> = {
  name: "integrations-get-branding",
  description: `Get branding

Get branding for platform.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await integrationsGetBranding(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

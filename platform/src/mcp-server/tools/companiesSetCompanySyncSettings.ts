/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { companiesSetCompanySyncSettings } from "../../funcs/companiesSetCompanySyncSettings.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.SetCompanySyncSettingsRequest$inboundSchema,
};

export const tool$companiesSetCompanySyncSettings: ToolDefinition<typeof args> =
  {
    name: "companies-set-company-sync-settings",
    description: `Set company sync settings

Set the sync settings for a specific company.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await companiesSetCompanySyncSettings(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };

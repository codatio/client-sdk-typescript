/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { refreshDataGetPullOperation } from "../../funcs/refreshDataGetPullOperation.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetPullOperationRequest$inboundSchema,
};

export const tool$refreshDataGetPullOperation: ToolDefinition<typeof args> = {
  name: "refresh-data-get-pull-operation",
  description: `Get pull operation

Retrieve information about a single dataset or pull operation.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await refreshDataGetPullOperation(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

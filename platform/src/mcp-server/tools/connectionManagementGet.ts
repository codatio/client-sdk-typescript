/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { connectionManagementGet } from "../../funcs/connectionManagementGet.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetConnectionManagementAccessTokenRequest$inboundSchema,
};

export const tool$connectionManagementGet: ToolDefinition<typeof args> = {
  name: "connection-management-get",
  description: `Get access token (old)

The new [Get company access token](https://docs.codat.io/platform-api#/operations/get-company-access-token) endpoint replaces this endpoint and includes additional functionality.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await connectionManagementGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

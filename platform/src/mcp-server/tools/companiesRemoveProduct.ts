/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { companiesRemoveProduct } from "../../funcs/companiesRemoveProduct.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveProductRequest$inboundSchema,
};

export const tool$companiesRemoveProduct: ToolDefinition<typeof args> = {
  name: "companies-remove-product",
  description: `Remove product

Use the *Remove product* endpoint to disable a product for the company specified by \`companyId\`.

> Note: This feature is currently in alpha and available only to participants in the development program.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await companiesRemoveProduct(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { supplementalDataGetConfiguration } from "../../funcs/supplementalDataGetConfiguration.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetSupplementalDataConfigurationRequest$inboundSchema,
};

export const tool$supplementalDataGetConfiguration: ToolDefinition<
  typeof args
> = {
  name: "supplemental-data-get-configuration",
  description: `Get configuration

The *Get configuration* endpoint returns supplemental data configuration previously created for each integration and data type combination.

[Supplemental data](https://docs.codat.io/using-the-api/supplemental-data/overview) is additional data you can include in Codat's standard data types.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await supplementalDataGetConfiguration(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

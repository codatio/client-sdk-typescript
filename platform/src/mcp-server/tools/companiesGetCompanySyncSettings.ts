/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { companiesGetCompanySyncSettings } from "../../funcs/companiesGetCompanySyncSettings.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetCompanySyncSettingsRequest$inboundSchema,
};

export const tool$companiesGetCompanySyncSettings: ToolDefinition<typeof args> =
  {
    name: "companies-get-company-sync-settings",
    description: `Get company sync settings

Retrieve the [sync settings](https://docs.codat.io/knowledge-base/advanced-sync-settings) for a specific company. This includes how often data types should be queued to be updated, and how much history should be fetched.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await companiesGetCompanySyncSettings(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsGet } from "../../funcs/integrationsGet.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetIntegrationRequest$inboundSchema,
};

export const tool$integrationsGet: ToolDefinition<typeof args> = {
  name: "integrations-get",
  description: `Get integration

Get single integration, by platformKey`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await integrationsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

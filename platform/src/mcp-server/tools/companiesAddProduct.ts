/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { companiesAddProduct } from "../../funcs/companiesAddProduct.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.AddProductRequest$inboundSchema,
};

export const tool$companiesAddProduct: ToolDefinition<typeof args> = {
  name: "companies-add-product",
  description: `Add product

Use the *Add product* endpoint to enable a product for the company specified by \`companyId\`.

> Note: This feature is currently in alpha and available only to participants in the development program.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await companiesAddProduct(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};

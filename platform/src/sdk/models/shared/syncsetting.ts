/* tslint:disable */
/* eslint-disable */
/**
 * Platform API
 * An API for the common components of all of Codat\'s products.  These end points cover creating and managing your companies, data connections, and integrations.  [Read about the building blocks of Codat...](https://docs.codat.io/core-concepts/companies) | [See our OpenAPI spec](https://github.com/codatio/oas)   --- <!-- Start Codat Tags Table --> ## Endpoints  | Endpoints | Description | | :- |:- | | Companies | Create and manage your SMB users\' companies. | | Connections | Create new and manage existing data connections for a company. | | Connection management | Configure connection management UI and retrieve access tokens for authentication. | | Webhooks | Create and manage webhooks that listen to Codat\'s events. | | Integrations | Get a list of integrations supported by Codat and their logos. | | Refresh data | Initiate data refreshes, view pull status and history. | | Settings | Manage company profile configuration, sync settings, and API keys. | | Push data | Initiate and monitor Create, Update, and Delete operations. | | Supplemental data | Configure and pull additional data you can include in Codat\'s standard data types. | | Custom data type | Configure and pull additional data types that are not included in Codat\'s standardized data model. | <!-- End Codat Tags Table -->
 *
 * The version of the OpenAPI document: 3.0.0
 * Contact: support@codat.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import type { DataType } from '../DataType';
import type { Company } from './company';
import { mapValues } from '../../../runtime';
/**
 * Describes how often, and how much history, should be fetched for the given data type when a pull operation is queued.
 * @export
 * @interface SyncSetting
 */
export interface SyncSetting {
    /**
     * Available data types
     * @type {string}
     * @memberof SyncSetting
     */
    dataType: DataType;
    /**
     * Whether this data type should be queued after a company has authorized a connection.
     * @type {boolean}
     * @memberof SyncSetting
     */
    fetchOnFirstLink: boolean;
    /**
     * Number of hours after which this data type should be refreshed.
     * @type {number}
     * @memberof SyncSetting
     */
    syncSchedule: number;
    /**
     * The sync in which data types are queued for a sync.
     * @type {number}
     * @memberof SyncSetting
     */
    syncOrder: number;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof SyncSetting
     */
    syncFromUtc?: string;
    /**
     * Number of months of data to be fetched. Set this *or* `syncFromUTC`.
     * @type {number}
     * @memberof SyncSetting
     */
    syncFromWindow?: number;
    /**
     * Months of data to fetch, for report data types (`balanceSheet` & `profitAndLoss`) only.
     * @type {number}
     * @memberof SyncSetting
     */
    monthsToSync?: number;
    /**
     * `True` if the [sync setting](https://docs.codat.io/knowledge-base/advanced-sync-settings) is locked.
     * @type {boolean}
     * @memberof SyncSetting
     */
    isLocked?: boolean;
}


/**
 * @export
 */
export const SyncSettingDataTypeEnum = {
    AccountTransactions: 'accountTransactions',
    BalanceSheet: 'balanceSheet',
    BankAccounts: 'bankAccounts',
    BankTransactions: 'bankTransactions',
    BillCreditNotes: 'billCreditNotes',
    BillPayments: 'billPayments',
    Bills: 'bills',
    CashFlowStatement: 'cashFlowStatement',
    ChartOfAccounts: 'chartOfAccounts',
    Company: 'company',
    CreditNotes: 'creditNotes',
    Customers: 'customers',
    DirectCosts: 'directCosts',
    DirectIncomes: 'directIncomes',
    Invoices: 'invoices',
    ItemReceipts: 'itemReceipts',
    Items: 'items',
    JournalEntries: 'journalEntries',
    Journals: 'journals',
    PaymentMethods: 'paymentMethods',
    Payments: 'payments',
    ProfitAndLoss: 'profitAndLoss',
    PurchaseOrders: 'purchaseOrders',
    SalesOrders: 'salesOrders',
    Suppliers: 'suppliers',
    TaxRates: 'taxRates',
    TrackingCategories: 'trackingCategories',
    Transfers: 'transfers',
    BankingAccountBalances: 'banking-accountBalances',
    BankingAccounts: 'banking-accounts',
    BankingTransactionCategories: 'banking-transactionCategories',
    BankingTransactions: 'banking-transactions',
    CommerceCompanyInfo: 'commerce-companyInfo',
    CommerceCustomers: 'commerce-customers',
    CommerceDisputes: 'commerce-disputes',
    CommerceLocations: 'commerce-locations',
    CommerceOrders: 'commerce-orders',
    CommercePaymentMethods: 'commerce-paymentMethods',
    CommercePayments: 'commerce-payments',
    CommerceProductCategories: 'commerce-productCategories',
    CommerceProducts: 'commerce-products',
    CommerceTaxComponents: 'commerce-taxComponents',
    CommerceTransactions: 'commerce-transactions'
} as const;
export type SyncSettingDataTypeEnum = typeof SyncSettingDataTypeEnum[keyof typeof SyncSettingDataTypeEnum];


/**
 * Check if a given object implements the SyncSetting interface.
 */
export function instanceOfSyncSetting(value: object): value is SyncSetting {
    if (!('dataType' in value) || value['dataType'] === undefined) return false;
    if (!('fetchOnFirstLink' in value) || value['fetchOnFirstLink'] === undefined) return false;
    if (!('syncSchedule' in value) || value['syncSchedule'] === undefined) return false;
    if (!('syncOrder' in value) || value['syncOrder'] === undefined) return false;
    return true;
}

export function SyncSettingFromJSON(json: any): SyncSetting {
    return SyncSettingFromJSONTyped(json, false);
}

export function SyncSettingFromJSONTyped(json: any, ignoreDiscriminator: boolean): SyncSetting {
    if (json == null) {
        return json;
    }
    return {
        
        'dataType': json['dataType'],
        'fetchOnFirstLink': json['fetchOnFirstLink'],
        'syncSchedule': json['syncSchedule'],
        'syncOrder': json['syncOrder'],
        ...(json['syncFromUtc'] !== undefined ? { 'syncFromUtc': json['syncFromUtc'] == null ? undefined : json['syncFromUtc'] } : {}),
        ...(json['syncFromWindow'] !== undefined ? { 'syncFromWindow': json['syncFromWindow'] == null ? undefined : json['syncFromWindow'] } : {}),
        ...(json['monthsToSync'] !== undefined ? { 'monthsToSync': json['monthsToSync'] == null ? undefined : json['monthsToSync'] } : {}),
        ...(json['isLocked'] !== undefined ? { 'isLocked': json['isLocked'] == null ? undefined : json['isLocked'] } : {}),
    };
}

export function SyncSettingToJSON(json: any): SyncSetting {
    return SyncSettingToJSONTyped(json, false);
}

export function SyncSettingToJSONTyped(value?: SyncSetting | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'dataType': value['dataType'],
        'fetchOnFirstLink': value['fetchOnFirstLink'],
        'syncSchedule': value['syncSchedule'],
        'syncOrder': value['syncOrder'],
        'syncFromUtc': value['syncFromUtc'],
        'syncFromWindow': value['syncFromWindow'],
        'monthsToSync': value['monthsToSync'],
        'isLocked': value['isLocked'],
    };
}


export interface CompanySyncSettingsGetResponseSyncSetting {
    /**
     * Available data types
     * @type {string}
     * @memberof SyncSetting
     */
    dataType: SyncSettingDataTypeEnum;
    /**
     * Whether this data type should be queued after a company has authorized a connection.
     * @type {boolean}
     * @memberof SyncSetting
     */
    fetchOnFirstLink: boolean;
    /**
     * Number of hours after which this data type should be refreshed.
     * @type {number}
     * @memberof SyncSetting
     */
    syncSchedule: number;
    /**
     * The sync in which data types are queued for a sync.
     * @type {number}
     * @memberof SyncSetting
     */
    syncOrder: number;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof SyncSetting
     */
    syncFromUtc?: string;
    /**
     * Number of months of data to be fetched. Set this *or* `syncFromUTC`.
     * @type {number}
     * @memberof SyncSetting
     */
    syncFromWindow?: number;
    /**
     * Months of data to fetch, for report data types (`balanceSheet` & `profitAndLoss`) only.
     * @type {number}
     * @memberof SyncSetting
     */
    monthsToSync?: number;
    /**
     * `True` if the [sync setting](https://docs.codat.io/knowledge-base/advanced-sync-settings) is locked.
     * @type {boolean}
     * @memberof SyncSetting
     */
    isLocked?: boolean;
}

export interface CompanySyncSettingsSyncSetting {
    /**
     * Available data types
     * @type {string}
     * @memberof SyncSetting
     */
    dataType: SyncSettingDataTypeEnum;
    /**
     * Whether this data type should be queued after a company has authorized a connection.
     * @type {boolean}
     * @memberof SyncSetting
     */
    fetchOnFirstLink: boolean;
    /**
     * Number of hours after which this data type should be refreshed.
     * @type {number}
     * @memberof SyncSetting
     */
    syncSchedule: number;
    /**
     * The sync in which data types are queued for a sync.
     * @type {number}
     * @memberof SyncSetting
     */
    syncOrder: number;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof SyncSetting
     */
    syncFromUtc?: string;
    /**
     * Number of months of data to be fetched. Set this *or* `syncFromUTC`.
     * @type {number}
     * @memberof SyncSetting
     */
    syncFromWindow?: number;
    /**
     * Months of data to fetch, for report data types (`balanceSheet` & `profitAndLoss`) only.
     * @type {number}
     * @memberof SyncSetting
     */
    monthsToSync?: number;
    /**
     * `True` if the [sync setting](https://docs.codat.io/knowledge-base/advanced-sync-settings) is locked.
     * @type {boolean}
     * @memberof SyncSetting
     */
    isLocked?: boolean;
}

export interface GetCompanySyncSettingsResponseSyncSetting {
    /**
     * Available data types
     * @type {string}
     * @memberof SyncSetting
     */
    dataType: SyncSettingDataTypeEnum;
    /**
     * Whether this data type should be queued after a company has authorized a connection.
     * @type {boolean}
     * @memberof SyncSetting
     */
    fetchOnFirstLink: boolean;
    /**
     * Number of hours after which this data type should be refreshed.
     * @type {number}
     * @memberof SyncSetting
     */
    syncSchedule: number;
    /**
     * The sync in which data types are queued for a sync.
     * @type {number}
     * @memberof SyncSetting
     */
    syncOrder: number;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof SyncSetting
     */
    syncFromUtc?: string;
    /**
     * Number of months of data to be fetched. Set this *or* `syncFromUTC`.
     * @type {number}
     * @memberof SyncSetting
     */
    syncFromWindow?: number;
    /**
     * Months of data to fetch, for report data types (`balanceSheet` & `profitAndLoss`) only.
     * @type {number}
     * @memberof SyncSetting
     */
    monthsToSync?: number;
    /**
     * `True` if the [sync setting](https://docs.codat.io/knowledge-base/advanced-sync-settings) is locked.
     * @type {boolean}
     * @memberof SyncSetting
     */
    isLocked?: boolean;
}

export interface GetProfileSyncSettingsResponseSyncSetting {
    /**
     * Available data types
     * @type {string}
     * @memberof SyncSetting
     */
    dataType: SyncSettingDataTypeEnum;
    /**
     * Whether this data type should be queued after a company has authorized a connection.
     * @type {boolean}
     * @memberof SyncSetting
     */
    fetchOnFirstLink: boolean;
    /**
     * Number of hours after which this data type should be refreshed.
     * @type {number}
     * @memberof SyncSetting
     */
    syncSchedule: number;
    /**
     * The sync in which data types are queued for a sync.
     * @type {number}
     * @memberof SyncSetting
     */
    syncOrder: number;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof SyncSetting
     */
    syncFromUtc?: string;
    /**
     * Number of months of data to be fetched. Set this *or* `syncFromUTC`.
     * @type {number}
     * @memberof SyncSetting
     */
    syncFromWindow?: number;
    /**
     * Months of data to fetch, for report data types (`balanceSheet` & `profitAndLoss`) only.
     * @type {number}
     * @memberof SyncSetting
     */
    monthsToSync?: number;
    /**
     * `True` if the [sync setting](https://docs.codat.io/knowledge-base/advanced-sync-settings) is locked.
     * @type {boolean}
     * @memberof SyncSetting
     */
    isLocked?: boolean;
}

export interface ProfileSyncSettingsGetResponseSyncSetting {
    /**
     * Available data types
     * @type {string}
     * @memberof SyncSetting
     */
    dataType: SyncSettingDataTypeEnum;
    /**
     * Whether this data type should be queued after a company has authorized a connection.
     * @type {boolean}
     * @memberof SyncSetting
     */
    fetchOnFirstLink: boolean;
    /**
     * Number of hours after which this data type should be refreshed.
     * @type {number}
     * @memberof SyncSetting
     */
    syncSchedule: number;
    /**
     * The sync in which data types are queued for a sync.
     * @type {number}
     * @memberof SyncSetting
     */
    syncOrder: number;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof SyncSetting
     */
    syncFromUtc?: string;
    /**
     * Number of months of data to be fetched. Set this *or* `syncFromUTC`.
     * @type {number}
     * @memberof SyncSetting
     */
    syncFromWindow?: number;
    /**
     * Months of data to fetch, for report data types (`balanceSheet` & `profitAndLoss`) only.
     * @type {number}
     * @memberof SyncSetting
     */
    monthsToSync?: number;
    /**
     * `True` if the [sync setting](https://docs.codat.io/knowledge-base/advanced-sync-settings) is locked.
     * @type {boolean}
     * @memberof SyncSetting
     */
    isLocked?: boolean;
}

export interface SetCompanySyncSettingsRequestBodySyncSetting {
    /**
     * Available data types
     * @type {string}
     * @memberof SyncSetting
     */
    dataType: SyncSettingDataTypeEnum;
    /**
     * Whether this data type should be queued after a company has authorized a connection.
     * @type {boolean}
     * @memberof SyncSetting
     */
    fetchOnFirstLink: boolean;
    /**
     * Number of hours after which this data type should be refreshed.
     * @type {number}
     * @memberof SyncSetting
     */
    syncSchedule: number;
    /**
     * The sync in which data types are queued for a sync.
     * @type {number}
     * @memberof SyncSetting
     */
    syncOrder: number;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof SyncSetting
     */
    syncFromUtc?: string;
    /**
     * Number of months of data to be fetched. Set this *or* `syncFromUTC`.
     * @type {number}
     * @memberof SyncSetting
     */
    syncFromWindow?: number;
    /**
     * Months of data to fetch, for report data types (`balanceSheet` & `profitAndLoss`) only.
     * @type {number}
     * @memberof SyncSetting
     */
    monthsToSync?: number;
    /**
     * `True` if the [sync setting](https://docs.codat.io/knowledge-base/advanced-sync-settings) is locked.
     * @type {boolean}
     * @memberof SyncSetting
     */
    isLocked?: boolean;
}

export interface SyncSettingsSyncSetting {
    /**
     * Available data types
     * @type {string}
     * @memberof SyncSetting
     */
    dataType: SyncSettingDataTypeEnum;
    /**
     * Whether this data type should be queued after a company has authorized a connection.
     * @type {boolean}
     * @memberof SyncSetting
     */
    fetchOnFirstLink: boolean;
    /**
     * Number of hours after which this data type should be refreshed.
     * @type {number}
     * @memberof SyncSetting
     */
    syncSchedule: number;
    /**
     * The sync in which data types are queued for a sync.
     * @type {number}
     * @memberof SyncSetting
     */
    syncOrder: number;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof SyncSetting
     */
    syncFromUtc?: string;
    /**
     * Number of months of data to be fetched. Set this *or* `syncFromUTC`.
     * @type {number}
     * @memberof SyncSetting
     */
    syncFromWindow?: number;
    /**
     * Months of data to fetch, for report data types (`balanceSheet` & `profitAndLoss`) only.
     * @type {number}
     * @memberof SyncSetting
     */
    monthsToSync?: number;
    /**
     * `True` if the [sync setting](https://docs.codat.io/knowledge-base/advanced-sync-settings) is locked.
     * @type {boolean}
     * @memberof SyncSetting
     */
    isLocked?: boolean;
}

export interface UpdateProfileSyncSettingsRequestBodySyncSetting {
    /**
     * Available data types
     * @type {string}
     * @memberof SyncSetting
     */
    dataType: SyncSettingDataTypeEnum;
    /**
     * Whether this data type should be queued after a company has authorized a connection.
     * @type {boolean}
     * @memberof SyncSetting
     */
    fetchOnFirstLink: boolean;
    /**
     * Number of hours after which this data type should be refreshed.
     * @type {number}
     * @memberof SyncSetting
     */
    syncSchedule: number;
    /**
     * The sync in which data types are queued for a sync.
     * @type {number}
     * @memberof SyncSetting
     */
    syncOrder: number;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof SyncSetting
     */
    syncFromUtc?: string;
    /**
     * Number of months of data to be fetched. Set this *or* `syncFromUTC`.
     * @type {number}
     * @memberof SyncSetting
     */
    syncFromWindow?: number;
    /**
     * Months of data to fetch, for report data types (`balanceSheet` & `profitAndLoss`) only.
     * @type {number}
     * @memberof SyncSetting
     */
    monthsToSync?: number;
    /**
     * `True` if the [sync setting](https://docs.codat.io/knowledge-base/advanced-sync-settings) is locked.
     * @type {boolean}
     * @memberof SyncSetting
     */
    isLocked?: boolean;
}

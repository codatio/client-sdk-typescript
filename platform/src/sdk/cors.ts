/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { corsGet } from "../funcs/corsGet.js";
import { corsSet } from "../funcs/corsSet.js";
import { ClientSDK, RequestOptions } from "../lib/sdks.js";
import * as shared from "./models/shared/index.js";
import { unwrapAsync } from "./types/fp.js";

export class Cors extends ClientSDK {
  /**
   * Get CORS settings (old)
   *
   * @remarks
   * The *Get CORS settings* endpoint returns the allowed origins (i.e. your domains) you want to allow cross-origin resource sharing ([CORS](https://en.wikipedia.org/wiki/Cross-origin_resource_sharing)) with Codat.
   *
   * Enabling CORS with Codat is required by our embeddable UIs (such as [Connections SDK](https://docs.codat.io/auth-flow/optimize/connection-management) and [Link SDK](https://docs.codat.io/auth-flow/authorize-embedded-link)) to access Codat's API endpoints.
   *
   * @deprecated method: The new [Get CORS settings](https://docs.codat.io/platform-api#/operations/get-cors-settings) endpoint replaces this endpoint and includes additional functionality.
  Update your integrations to use the new path `/corsSettings` as the existing route will be removed in a future release.
  . Use get instead.
   */
  async get(
    options?: RequestOptions,
  ): Promise<shared.ConnectionManagementAllowedOrigins> {
    return unwrapAsync(corsGet(
      this,
      options,
    ));
  }

  /**
   * Set CORS settings (old)
   *
   * @remarks
   * The *Set CORS settings* endpoint allows you to register allowed origins (i.e. your domains) for use in cross-origin resource sharing ([CORS](https://en.wikipedia.org/wiki/Cross-origin_resource_sharing)).
   *
   * Enabling CORS with Codat is required by our embeddable UIs (such as [Connections SDK](https://docs.codat.io/auth-flow/optimize/connection-management) and [Link SDK](https://docs.codat.io/auth-flow/authorize-embedded-link)) to access Codat's API endpoints.
   *
   * @deprecated method: The new [Set CORS settings](https://docs.codat.io/platform-api#/operations/set-cors-settings) endpoint replaces this endpoint and includes additional functionality.
  Update your integrations to use the new path `/corsSettings` as the existing route will be removed in a future release.
  . Use set instead.
   */
  async set(
    request?: shared.ConnectionManagementAllowedOrigins | undefined,
    options?: RequestOptions,
  ): Promise<shared.ConnectionManagementAllowedOrigins> {
    return unwrapAsync(corsSet(
      this,
      request,
      options,
    ));
  }
}

/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { z } from "zod";

/**
 * Links journal entries to the relevant journal in accounting integrations that use multi-book accounting (multiple journals).
 */
export type JournalRef = {
    /**
     * GUID of the underlying journal.
     */
    id: string;
    /**
     * Name of journal
     */
    name?: string | null | undefined;
};

/** @internal */
export namespace JournalRef$ {
    export type Inbound = {
        id: string;
        name?: string | null | undefined;
    };

    export const inboundSchema: z.ZodType<JournalRef, z.ZodTypeDef, Inbound> = z
        .object({
            id: z.string(),
            name: z.nullable(z.string()).optional(),
        })
        .transform((v) => {
            return {
                id: v.id,
                ...(v.name === undefined ? null : { name: v.name }),
            };
        });

    export type Outbound = {
        id: string;
        name?: string | null | undefined;
    };

    export const outboundSchema: z.ZodType<Outbound, z.ZodTypeDef, JournalRef> = z
        .object({
            id: z.string(),
            name: z.nullable(z.string()).optional(),
        })
        .transform((v) => {
            return {
                id: v.id,
                ...(v.name === undefined ? null : { name: v.name }),
            };
        });
}

/* tslint:disable */
/* eslint-disable */
/**
 * Expenses
 * The API for Codat\'s Expenses solution.  Expenses is an API and a set of supporting tools. It has been built to enable corporate card and expense management platforms to provide high-quality integrations with multiple accounting software through a standardized API.  [Explore solution](https://docs.codat.io/sync-for-expenses/overview) | [See our OpenAPI spec](https://github.com/codatio/oas)  Not seeing the endpoints you\'re expecting? We\'ve [reorganized our solutions](https://docs.codat.io/updates/230901-new-products), and you may be using a [different version of Expenses](https://docs.codat.io/sync-for-expenses-v1-api#/).  --- <!-- Start Codat Tags Table --> ## Endpoints  | Endpoints | Description | | :- |:- | | Companies | Create and manage your SMB users\' companies. | | Connections | Create new and manage existing data connections for a company. | | Configuration | View and manage mapping configuration and defaults for expense transactions. | | Sync | Monitor the status of data syncs. | | Expenses | Create and update transactions that represent your customers\' spend. | | Transfers | Create and update transactions that represent the movement of your customers\' money. | | Reimbursements | Create and update transactions that represent your customers\' repayable spend. | | Attachments | Attach receipts to a transaction for a complete audit trail. | | Transaction status | Monitor the status of individual transactions in data syncs. | | Manage data | Control and monitor the retrieval of data from an integration. | | Push operations | View historic push operations. | | Accounts | Create accounts and view account schemas. | | Customers | Get, create, and update customers. | | Suppliers | Get, create, and update suppliers. | <!-- End Codat Tags Table -->
 *
 * The version of the OpenAPI document: prealpha
 * Contact: expenses@codat.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../../../runtime';
import type { SupplierStatus } from './supplierstatus';
import {
    SupplierStatusFromJSON,
    SupplierStatusFromJSONTyped,
    SupplierStatusToJSON,
    SupplierStatusToJSONTyped,
} from './supplierstatus';
import type { SupplementalData } from './supplementaldata';
import {
    SupplementalDataFromJSON,
    SupplementalDataFromJSONTyped,
    SupplementalDataToJSON,
    SupplementalDataToJSONTyped,
} from './supplementaldata';
import type { Metadata } from './metadata';
import {
    MetadataFromJSON,
    MetadataFromJSONTyped,
    MetadataToJSON,
    MetadataToJSONTyped,
} from './metadata';
import type { Items } from './items';
import {
    ItemsFromJSON,
    ItemsFromJSONTyped,
    ItemsToJSON,
    ItemsToJSONTyped,
} from './items';

/**
 * ## Overview
 * 
 * From the **Suppliers** endpoints, you can retrieve a list of [all the suppliers for a company](https://docs.codat.io/sync-for-expenses-api#/operations/list-suppliers). Suppliers' data links to accounts payable [bills](https://docs.codat.io/sync-for-expenses-api#/schemas/Bill).
 * @export
 * @interface Supplier
 */
export interface Supplier {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the supplier, unique to the company in the accounting software.
     * @type {string}
     * @memberof Supplier
     */
    id?: string;
    /**
     * Name of the supplier as recorded in the accounting system, typically the company name.
     * @type {string}
     * @memberof Supplier
     */
    supplierName?: string | null;
    /**
     * Name of the main contact for the supplier.
     * @type {string}
     * @memberof Supplier
     */
    contactName?: string | null;
    /**
     * Email address that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    emailAddress?: string | null;
    /**
     * Phone number that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    phone?: string | null;
    /**
     * An array of Addresses.
     * @type {Array<Items>}
     * @memberof Supplier
     */
    addresses?: Array<Items> | null;
    /**
     * Company number of the supplier. In the UK, this is typically the company registration number issued by Companies House.
     * @type {string}
     * @memberof Supplier
     */
    registrationNumber?: string | null;
    /**
     * Supplier's company tax number.
     * @type {string}
     * @memberof Supplier
     */
    taxNumber?: string | null;
    /**
     * 
     * @type {SupplierStatus}
     * @memberof Supplier
     */
    status: SupplierStatus;
    /**
     * Default currency the supplier's transactional data is recorded in.
     * @type {string}
     * @memberof Supplier
     */
    defaultCurrency?: string | null;
    /**
     * 
     * @type {Metadata}
     * @memberof Supplier
     */
    metadata?: Metadata;
    /**
     * 
     * @type {SupplementalData}
     * @memberof Supplier
     */
    supplementalData?: SupplementalData;
}



/**
 * Check if a given object implements the Supplier interface.
 */
export function instanceOfSupplier(value: object): value is Supplier {
    if (!('status' in value) || value['status'] === undefined) return false;
    return true;
}

export function SupplierFromJSON(json: any): Supplier {
    return SupplierFromJSONTyped(json, false);
}

export function SupplierFromJSONTyped(json: any, ignoreDiscriminator: boolean): Supplier {
    if (json == null) {
        return json;
    }
    return {
        
        'modifiedDate': json['modifiedDate'] == null ? undefined : json['modifiedDate'],
        'sourceModifiedDate': json['sourceModifiedDate'] == null ? undefined : json['sourceModifiedDate'],
        'id': json['id'] == null ? undefined : json['id'],
        'supplierName': json['supplierName'] == null ? undefined : json['supplierName'],
        'contactName': json['contactName'] == null ? undefined : json['contactName'],
        'emailAddress': json['emailAddress'] == null ? undefined : json['emailAddress'],
        'phone': json['phone'] == null ? undefined : json['phone'],
        'addresses': json['addresses'] == null ? undefined : ((json['addresses'] as Array<any>).map(ItemsFromJSON)),
        'registrationNumber': json['registrationNumber'] == null ? undefined : json['registrationNumber'],
        'taxNumber': json['taxNumber'] == null ? undefined : json['taxNumber'],
        'status': SupplierStatusFromJSON(json['status']),
        'defaultCurrency': json['defaultCurrency'] == null ? undefined : json['defaultCurrency'],
        'metadata': json['metadata'] == null ? undefined : MetadataFromJSON(json['metadata']),
        'supplementalData': json['supplementalData'] == null ? undefined : SupplementalDataFromJSON(json['supplementalData']),
    };
}

export function SupplierToJSON(json: any): Supplier {
    return SupplierToJSONTyped(json, false);
}

export function SupplierToJSONTyped(value?: Supplier | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'modifiedDate': value['modifiedDate'],
        'sourceModifiedDate': value['sourceModifiedDate'],
        'id': value['id'],
        'supplierName': value['supplierName'],
        'contactName': value['contactName'],
        'emailAddress': value['emailAddress'],
        'phone': value['phone'],
        'addresses': value['addresses'] == null ? undefined : ((value['addresses'] as Array<any>).map(ItemsToJSON)),
        'registrationNumber': value['registrationNumber'],
        'taxNumber': value['taxNumber'],
        'status': SupplierStatusToJSON(value['status']),
        'defaultCurrency': value['defaultCurrency'],
        'metadata': MetadataToJSON(value['metadata']),
        'supplementalData': SupplementalDataToJSON(value['supplementalData']),
    };
}


export interface AccountingSupplier {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the supplier, unique to the company in the accounting software.
     * @type {string}
     * @memberof Supplier
     */
    id?: string;
    /**
     * Name of the supplier as recorded in the accounting system, typically the company name.
     * @type {string}
     * @memberof Supplier
     */
    supplierName?: string | null;
    /**
     * Name of the main contact for the supplier.
     * @type {string}
     * @memberof Supplier
     */
    contactName?: string | null;
    /**
     * Email address that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    emailAddress?: string | null;
    /**
     * Phone number that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    phone?: string | null;
    /**
     * An array of Addresses.
     * @type {Array<Items>}
     * @memberof Supplier
     */
    addresses?: Array<Items> | null;
    /**
     * Company number of the supplier. In the UK, this is typically the company registration number issued by Companies House.
     * @type {string}
     * @memberof Supplier
     */
    registrationNumber?: string | null;
    /**
     * Supplier's company tax number.
     * @type {string}
     * @memberof Supplier
     */
    taxNumber?: string | null;
    /**
     * 
     * @type {SupplierStatus}
     * @memberof Supplier
     */
    status: SupplierStatus;
    /**
     * Default currency the supplier's transactional data is recorded in.
     * @type {string}
     * @memberof Supplier
     */
    defaultCurrency?: string | null;
    /**
     * 
     * @type {Metadata}
     * @memberof Supplier
     */
    metadata?: Metadata;
    /**
     * 
     * @type {SupplementalData}
     * @memberof Supplier
     */
    supplementalData?: SupplementalData;
}

export interface CreateSupplierRequestBody {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the supplier, unique to the company in the accounting software.
     * @type {string}
     * @memberof Supplier
     */
    id?: string;
    /**
     * Name of the supplier as recorded in the accounting system, typically the company name.
     * @type {string}
     * @memberof Supplier
     */
    supplierName?: string | null;
    /**
     * Name of the main contact for the supplier.
     * @type {string}
     * @memberof Supplier
     */
    contactName?: string | null;
    /**
     * Email address that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    emailAddress?: string | null;
    /**
     * Phone number that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    phone?: string | null;
    /**
     * An array of Addresses.
     * @type {Array<Items>}
     * @memberof Supplier
     */
    addresses?: Array<Items> | null;
    /**
     * Company number of the supplier. In the UK, this is typically the company registration number issued by Companies House.
     * @type {string}
     * @memberof Supplier
     */
    registrationNumber?: string | null;
    /**
     * Supplier's company tax number.
     * @type {string}
     * @memberof Supplier
     */
    taxNumber?: string | null;
    /**
     * 
     * @type {SupplierStatus}
     * @memberof Supplier
     */
    status: SupplierStatus;
    /**
     * Default currency the supplier's transactional data is recorded in.
     * @type {string}
     * @memberof Supplier
     */
    defaultCurrency?: string | null;
    /**
     * 
     * @type {Metadata}
     * @memberof Supplier
     */
    metadata?: Metadata;
    /**
     * 
     * @type {SupplementalData}
     * @memberof Supplier
     */
    supplementalData?: SupplementalData;
}

export interface CreateSupplierResponseAccountingSupplier {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the supplier, unique to the company in the accounting software.
     * @type {string}
     * @memberof Supplier
     */
    id?: string;
    /**
     * Name of the supplier as recorded in the accounting system, typically the company name.
     * @type {string}
     * @memberof Supplier
     */
    supplierName?: string | null;
    /**
     * Name of the main contact for the supplier.
     * @type {string}
     * @memberof Supplier
     */
    contactName?: string | null;
    /**
     * Email address that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    emailAddress?: string | null;
    /**
     * Phone number that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    phone?: string | null;
    /**
     * An array of Addresses.
     * @type {Array<Items>}
     * @memberof Supplier
     */
    addresses?: Array<Items> | null;
    /**
     * Company number of the supplier. In the UK, this is typically the company registration number issued by Companies House.
     * @type {string}
     * @memberof Supplier
     */
    registrationNumber?: string | null;
    /**
     * Supplier's company tax number.
     * @type {string}
     * @memberof Supplier
     */
    taxNumber?: string | null;
    /**
     * 
     * @type {SupplierStatus}
     * @memberof Supplier
     */
    status: SupplierStatus;
    /**
     * Default currency the supplier's transactional data is recorded in.
     * @type {string}
     * @memberof Supplier
     */
    defaultCurrency?: string | null;
    /**
     * 
     * @type {Metadata}
     * @memberof Supplier
     */
    metadata?: Metadata;
    /**
     * 
     * @type {SupplementalData}
     * @memberof Supplier
     */
    supplementalData?: SupplementalData;
}

export interface CreateSupplierResponseSupplier {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the supplier, unique to the company in the accounting software.
     * @type {string}
     * @memberof Supplier
     */
    id?: string;
    /**
     * Name of the supplier as recorded in the accounting system, typically the company name.
     * @type {string}
     * @memberof Supplier
     */
    supplierName?: string | null;
    /**
     * Name of the main contact for the supplier.
     * @type {string}
     * @memberof Supplier
     */
    contactName?: string | null;
    /**
     * Email address that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    emailAddress?: string | null;
    /**
     * Phone number that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    phone?: string | null;
    /**
     * An array of Addresses.
     * @type {Array<Items>}
     * @memberof Supplier
     */
    addresses?: Array<Items> | null;
    /**
     * Company number of the supplier. In the UK, this is typically the company registration number issued by Companies House.
     * @type {string}
     * @memberof Supplier
     */
    registrationNumber?: string | null;
    /**
     * Supplier's company tax number.
     * @type {string}
     * @memberof Supplier
     */
    taxNumber?: string | null;
    /**
     * 
     * @type {SupplierStatus}
     * @memberof Supplier
     */
    status: SupplierStatus;
    /**
     * Default currency the supplier's transactional data is recorded in.
     * @type {string}
     * @memberof Supplier
     */
    defaultCurrency?: string | null;
    /**
     * 
     * @type {Metadata}
     * @memberof Supplier
     */
    metadata?: Metadata;
    /**
     * 
     * @type {SupplementalData}
     * @memberof Supplier
     */
    supplementalData?: SupplementalData;
}

export interface CreateSupplierSupplier {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the supplier, unique to the company in the accounting software.
     * @type {string}
     * @memberof Supplier
     */
    id?: string;
    /**
     * Name of the supplier as recorded in the accounting system, typically the company name.
     * @type {string}
     * @memberof Supplier
     */
    supplierName?: string | null;
    /**
     * Name of the main contact for the supplier.
     * @type {string}
     * @memberof Supplier
     */
    contactName?: string | null;
    /**
     * Email address that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    emailAddress?: string | null;
    /**
     * Phone number that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    phone?: string | null;
    /**
     * An array of Addresses.
     * @type {Array<Items>}
     * @memberof Supplier
     */
    addresses?: Array<Items> | null;
    /**
     * Company number of the supplier. In the UK, this is typically the company registration number issued by Companies House.
     * @type {string}
     * @memberof Supplier
     */
    registrationNumber?: string | null;
    /**
     * Supplier's company tax number.
     * @type {string}
     * @memberof Supplier
     */
    taxNumber?: string | null;
    /**
     * 
     * @type {SupplierStatus}
     * @memberof Supplier
     */
    status: SupplierStatus;
    /**
     * Default currency the supplier's transactional data is recorded in.
     * @type {string}
     * @memberof Supplier
     */
    defaultCurrency?: string | null;
    /**
     * 
     * @type {Metadata}
     * @memberof Supplier
     */
    metadata?: Metadata;
    /**
     * 
     * @type {SupplementalData}
     * @memberof Supplier
     */
    supplementalData?: SupplementalData;
}

export interface GetSupplierResponseAccountingSupplier {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the supplier, unique to the company in the accounting software.
     * @type {string}
     * @memberof Supplier
     */
    id?: string;
    /**
     * Name of the supplier as recorded in the accounting system, typically the company name.
     * @type {string}
     * @memberof Supplier
     */
    supplierName?: string | null;
    /**
     * Name of the main contact for the supplier.
     * @type {string}
     * @memberof Supplier
     */
    contactName?: string | null;
    /**
     * Email address that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    emailAddress?: string | null;
    /**
     * Phone number that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    phone?: string | null;
    /**
     * An array of Addresses.
     * @type {Array<Items>}
     * @memberof Supplier
     */
    addresses?: Array<Items> | null;
    /**
     * Company number of the supplier. In the UK, this is typically the company registration number issued by Companies House.
     * @type {string}
     * @memberof Supplier
     */
    registrationNumber?: string | null;
    /**
     * Supplier's company tax number.
     * @type {string}
     * @memberof Supplier
     */
    taxNumber?: string | null;
    /**
     * 
     * @type {SupplierStatus}
     * @memberof Supplier
     */
    status: SupplierStatus;
    /**
     * Default currency the supplier's transactional data is recorded in.
     * @type {string}
     * @memberof Supplier
     */
    defaultCurrency?: string | null;
    /**
     * 
     * @type {Metadata}
     * @memberof Supplier
     */
    metadata?: Metadata;
    /**
     * 
     * @type {SupplementalData}
     * @memberof Supplier
     */
    supplementalData?: SupplementalData;
}

export interface GetSupplierResponseSupplier {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the supplier, unique to the company in the accounting software.
     * @type {string}
     * @memberof Supplier
     */
    id?: string;
    /**
     * Name of the supplier as recorded in the accounting system, typically the company name.
     * @type {string}
     * @memberof Supplier
     */
    supplierName?: string | null;
    /**
     * Name of the main contact for the supplier.
     * @type {string}
     * @memberof Supplier
     */
    contactName?: string | null;
    /**
     * Email address that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    emailAddress?: string | null;
    /**
     * Phone number that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    phone?: string | null;
    /**
     * An array of Addresses.
     * @type {Array<Items>}
     * @memberof Supplier
     */
    addresses?: Array<Items> | null;
    /**
     * Company number of the supplier. In the UK, this is typically the company registration number issued by Companies House.
     * @type {string}
     * @memberof Supplier
     */
    registrationNumber?: string | null;
    /**
     * Supplier's company tax number.
     * @type {string}
     * @memberof Supplier
     */
    taxNumber?: string | null;
    /**
     * 
     * @type {SupplierStatus}
     * @memberof Supplier
     */
    status: SupplierStatus;
    /**
     * Default currency the supplier's transactional data is recorded in.
     * @type {string}
     * @memberof Supplier
     */
    defaultCurrency?: string | null;
    /**
     * 
     * @type {Metadata}
     * @memberof Supplier
     */
    metadata?: Metadata;
    /**
     * 
     * @type {SupplementalData}
     * @memberof Supplier
     */
    supplementalData?: SupplementalData;
}

export interface ListSuppliersResponseAccountingSupplier {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the supplier, unique to the company in the accounting software.
     * @type {string}
     * @memberof Supplier
     */
    id?: string;
    /**
     * Name of the supplier as recorded in the accounting system, typically the company name.
     * @type {string}
     * @memberof Supplier
     */
    supplierName?: string | null;
    /**
     * Name of the main contact for the supplier.
     * @type {string}
     * @memberof Supplier
     */
    contactName?: string | null;
    /**
     * Email address that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    emailAddress?: string | null;
    /**
     * Phone number that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    phone?: string | null;
    /**
     * An array of Addresses.
     * @type {Array<Items>}
     * @memberof Supplier
     */
    addresses?: Array<Items> | null;
    /**
     * Company number of the supplier. In the UK, this is typically the company registration number issued by Companies House.
     * @type {string}
     * @memberof Supplier
     */
    registrationNumber?: string | null;
    /**
     * Supplier's company tax number.
     * @type {string}
     * @memberof Supplier
     */
    taxNumber?: string | null;
    /**
     * 
     * @type {SupplierStatus}
     * @memberof Supplier
     */
    status: SupplierStatus;
    /**
     * Default currency the supplier's transactional data is recorded in.
     * @type {string}
     * @memberof Supplier
     */
    defaultCurrency?: string | null;
    /**
     * 
     * @type {Metadata}
     * @memberof Supplier
     */
    metadata?: Metadata;
    /**
     * 
     * @type {SupplementalData}
     * @memberof Supplier
     */
    supplementalData?: SupplementalData;
}

export interface ListSuppliersResponseSupplier {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the supplier, unique to the company in the accounting software.
     * @type {string}
     * @memberof Supplier
     */
    id?: string;
    /**
     * Name of the supplier as recorded in the accounting system, typically the company name.
     * @type {string}
     * @memberof Supplier
     */
    supplierName?: string | null;
    /**
     * Name of the main contact for the supplier.
     * @type {string}
     * @memberof Supplier
     */
    contactName?: string | null;
    /**
     * Email address that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    emailAddress?: string | null;
    /**
     * Phone number that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    phone?: string | null;
    /**
     * An array of Addresses.
     * @type {Array<Items>}
     * @memberof Supplier
     */
    addresses?: Array<Items> | null;
    /**
     * Company number of the supplier. In the UK, this is typically the company registration number issued by Companies House.
     * @type {string}
     * @memberof Supplier
     */
    registrationNumber?: string | null;
    /**
     * Supplier's company tax number.
     * @type {string}
     * @memberof Supplier
     */
    taxNumber?: string | null;
    /**
     * 
     * @type {SupplierStatus}
     * @memberof Supplier
     */
    status: SupplierStatus;
    /**
     * Default currency the supplier's transactional data is recorded in.
     * @type {string}
     * @memberof Supplier
     */
    defaultCurrency?: string | null;
    /**
     * 
     * @type {Metadata}
     * @memberof Supplier
     */
    metadata?: Metadata;
    /**
     * 
     * @type {SupplementalData}
     * @memberof Supplier
     */
    supplementalData?: SupplementalData;
}

export interface SupplierAccountingSupplier {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the supplier, unique to the company in the accounting software.
     * @type {string}
     * @memberof Supplier
     */
    id?: string;
    /**
     * Name of the supplier as recorded in the accounting system, typically the company name.
     * @type {string}
     * @memberof Supplier
     */
    supplierName?: string | null;
    /**
     * Name of the main contact for the supplier.
     * @type {string}
     * @memberof Supplier
     */
    contactName?: string | null;
    /**
     * Email address that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    emailAddress?: string | null;
    /**
     * Phone number that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    phone?: string | null;
    /**
     * An array of Addresses.
     * @type {Array<Items>}
     * @memberof Supplier
     */
    addresses?: Array<Items> | null;
    /**
     * Company number of the supplier. In the UK, this is typically the company registration number issued by Companies House.
     * @type {string}
     * @memberof Supplier
     */
    registrationNumber?: string | null;
    /**
     * Supplier's company tax number.
     * @type {string}
     * @memberof Supplier
     */
    taxNumber?: string | null;
    /**
     * 
     * @type {SupplierStatus}
     * @memberof Supplier
     */
    status: SupplierStatus;
    /**
     * Default currency the supplier's transactional data is recorded in.
     * @type {string}
     * @memberof Supplier
     */
    defaultCurrency?: string | null;
    /**
     * 
     * @type {Metadata}
     * @memberof Supplier
     */
    metadata?: Metadata;
    /**
     * 
     * @type {SupplementalData}
     * @memberof Supplier
     */
    supplementalData?: SupplementalData;
}

export interface SupplierCreateRequestBody {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the supplier, unique to the company in the accounting software.
     * @type {string}
     * @memberof Supplier
     */
    id?: string;
    /**
     * Name of the supplier as recorded in the accounting system, typically the company name.
     * @type {string}
     * @memberof Supplier
     */
    supplierName?: string | null;
    /**
     * Name of the main contact for the supplier.
     * @type {string}
     * @memberof Supplier
     */
    contactName?: string | null;
    /**
     * Email address that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    emailAddress?: string | null;
    /**
     * Phone number that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    phone?: string | null;
    /**
     * An array of Addresses.
     * @type {Array<Items>}
     * @memberof Supplier
     */
    addresses?: Array<Items> | null;
    /**
     * Company number of the supplier. In the UK, this is typically the company registration number issued by Companies House.
     * @type {string}
     * @memberof Supplier
     */
    registrationNumber?: string | null;
    /**
     * Supplier's company tax number.
     * @type {string}
     * @memberof Supplier
     */
    taxNumber?: string | null;
    /**
     * 
     * @type {SupplierStatus}
     * @memberof Supplier
     */
    status: SupplierStatus;
    /**
     * Default currency the supplier's transactional data is recorded in.
     * @type {string}
     * @memberof Supplier
     */
    defaultCurrency?: string | null;
    /**
     * 
     * @type {Metadata}
     * @memberof Supplier
     */
    metadata?: Metadata;
    /**
     * 
     * @type {SupplementalData}
     * @memberof Supplier
     */
    supplementalData?: SupplementalData;
}

export interface SupplierCreateResponseAccountingSupplier {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the supplier, unique to the company in the accounting software.
     * @type {string}
     * @memberof Supplier
     */
    id?: string;
    /**
     * Name of the supplier as recorded in the accounting system, typically the company name.
     * @type {string}
     * @memberof Supplier
     */
    supplierName?: string | null;
    /**
     * Name of the main contact for the supplier.
     * @type {string}
     * @memberof Supplier
     */
    contactName?: string | null;
    /**
     * Email address that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    emailAddress?: string | null;
    /**
     * Phone number that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    phone?: string | null;
    /**
     * An array of Addresses.
     * @type {Array<Items>}
     * @memberof Supplier
     */
    addresses?: Array<Items> | null;
    /**
     * Company number of the supplier. In the UK, this is typically the company registration number issued by Companies House.
     * @type {string}
     * @memberof Supplier
     */
    registrationNumber?: string | null;
    /**
     * Supplier's company tax number.
     * @type {string}
     * @memberof Supplier
     */
    taxNumber?: string | null;
    /**
     * 
     * @type {SupplierStatus}
     * @memberof Supplier
     */
    status: SupplierStatus;
    /**
     * Default currency the supplier's transactional data is recorded in.
     * @type {string}
     * @memberof Supplier
     */
    defaultCurrency?: string | null;
    /**
     * 
     * @type {Metadata}
     * @memberof Supplier
     */
    metadata?: Metadata;
    /**
     * 
     * @type {SupplementalData}
     * @memberof Supplier
     */
    supplementalData?: SupplementalData;
}

export interface SupplierCreateResponseSupplier {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the supplier, unique to the company in the accounting software.
     * @type {string}
     * @memberof Supplier
     */
    id?: string;
    /**
     * Name of the supplier as recorded in the accounting system, typically the company name.
     * @type {string}
     * @memberof Supplier
     */
    supplierName?: string | null;
    /**
     * Name of the main contact for the supplier.
     * @type {string}
     * @memberof Supplier
     */
    contactName?: string | null;
    /**
     * Email address that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    emailAddress?: string | null;
    /**
     * Phone number that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    phone?: string | null;
    /**
     * An array of Addresses.
     * @type {Array<Items>}
     * @memberof Supplier
     */
    addresses?: Array<Items> | null;
    /**
     * Company number of the supplier. In the UK, this is typically the company registration number issued by Companies House.
     * @type {string}
     * @memberof Supplier
     */
    registrationNumber?: string | null;
    /**
     * Supplier's company tax number.
     * @type {string}
     * @memberof Supplier
     */
    taxNumber?: string | null;
    /**
     * 
     * @type {SupplierStatus}
     * @memberof Supplier
     */
    status: SupplierStatus;
    /**
     * Default currency the supplier's transactional data is recorded in.
     * @type {string}
     * @memberof Supplier
     */
    defaultCurrency?: string | null;
    /**
     * 
     * @type {Metadata}
     * @memberof Supplier
     */
    metadata?: Metadata;
    /**
     * 
     * @type {SupplementalData}
     * @memberof Supplier
     */
    supplementalData?: SupplementalData;
}

export interface SupplierGetResponseAccountingSupplier {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the supplier, unique to the company in the accounting software.
     * @type {string}
     * @memberof Supplier
     */
    id?: string;
    /**
     * Name of the supplier as recorded in the accounting system, typically the company name.
     * @type {string}
     * @memberof Supplier
     */
    supplierName?: string | null;
    /**
     * Name of the main contact for the supplier.
     * @type {string}
     * @memberof Supplier
     */
    contactName?: string | null;
    /**
     * Email address that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    emailAddress?: string | null;
    /**
     * Phone number that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    phone?: string | null;
    /**
     * An array of Addresses.
     * @type {Array<Items>}
     * @memberof Supplier
     */
    addresses?: Array<Items> | null;
    /**
     * Company number of the supplier. In the UK, this is typically the company registration number issued by Companies House.
     * @type {string}
     * @memberof Supplier
     */
    registrationNumber?: string | null;
    /**
     * Supplier's company tax number.
     * @type {string}
     * @memberof Supplier
     */
    taxNumber?: string | null;
    /**
     * 
     * @type {SupplierStatus}
     * @memberof Supplier
     */
    status: SupplierStatus;
    /**
     * Default currency the supplier's transactional data is recorded in.
     * @type {string}
     * @memberof Supplier
     */
    defaultCurrency?: string | null;
    /**
     * 
     * @type {Metadata}
     * @memberof Supplier
     */
    metadata?: Metadata;
    /**
     * 
     * @type {SupplementalData}
     * @memberof Supplier
     */
    supplementalData?: SupplementalData;
}

export interface SupplierGetResponseSupplier {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the supplier, unique to the company in the accounting software.
     * @type {string}
     * @memberof Supplier
     */
    id?: string;
    /**
     * Name of the supplier as recorded in the accounting system, typically the company name.
     * @type {string}
     * @memberof Supplier
     */
    supplierName?: string | null;
    /**
     * Name of the main contact for the supplier.
     * @type {string}
     * @memberof Supplier
     */
    contactName?: string | null;
    /**
     * Email address that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    emailAddress?: string | null;
    /**
     * Phone number that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    phone?: string | null;
    /**
     * An array of Addresses.
     * @type {Array<Items>}
     * @memberof Supplier
     */
    addresses?: Array<Items> | null;
    /**
     * Company number of the supplier. In the UK, this is typically the company registration number issued by Companies House.
     * @type {string}
     * @memberof Supplier
     */
    registrationNumber?: string | null;
    /**
     * Supplier's company tax number.
     * @type {string}
     * @memberof Supplier
     */
    taxNumber?: string | null;
    /**
     * 
     * @type {SupplierStatus}
     * @memberof Supplier
     */
    status: SupplierStatus;
    /**
     * Default currency the supplier's transactional data is recorded in.
     * @type {string}
     * @memberof Supplier
     */
    defaultCurrency?: string | null;
    /**
     * 
     * @type {Metadata}
     * @memberof Supplier
     */
    metadata?: Metadata;
    /**
     * 
     * @type {SupplementalData}
     * @memberof Supplier
     */
    supplementalData?: SupplementalData;
}

export interface SupplierUpdateRequestBody {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the supplier, unique to the company in the accounting software.
     * @type {string}
     * @memberof Supplier
     */
    id?: string;
    /**
     * Name of the supplier as recorded in the accounting system, typically the company name.
     * @type {string}
     * @memberof Supplier
     */
    supplierName?: string | null;
    /**
     * Name of the main contact for the supplier.
     * @type {string}
     * @memberof Supplier
     */
    contactName?: string | null;
    /**
     * Email address that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    emailAddress?: string | null;
    /**
     * Phone number that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    phone?: string | null;
    /**
     * An array of Addresses.
     * @type {Array<Items>}
     * @memberof Supplier
     */
    addresses?: Array<Items> | null;
    /**
     * Company number of the supplier. In the UK, this is typically the company registration number issued by Companies House.
     * @type {string}
     * @memberof Supplier
     */
    registrationNumber?: string | null;
    /**
     * Supplier's company tax number.
     * @type {string}
     * @memberof Supplier
     */
    taxNumber?: string | null;
    /**
     * 
     * @type {SupplierStatus}
     * @memberof Supplier
     */
    status: SupplierStatus;
    /**
     * Default currency the supplier's transactional data is recorded in.
     * @type {string}
     * @memberof Supplier
     */
    defaultCurrency?: string | null;
    /**
     * 
     * @type {Metadata}
     * @memberof Supplier
     */
    metadata?: Metadata;
    /**
     * 
     * @type {SupplementalData}
     * @memberof Supplier
     */
    supplementalData?: SupplementalData;
}

export interface SupplierUpdateResponseAccountingSupplier {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the supplier, unique to the company in the accounting software.
     * @type {string}
     * @memberof Supplier
     */
    id?: string;
    /**
     * Name of the supplier as recorded in the accounting system, typically the company name.
     * @type {string}
     * @memberof Supplier
     */
    supplierName?: string | null;
    /**
     * Name of the main contact for the supplier.
     * @type {string}
     * @memberof Supplier
     */
    contactName?: string | null;
    /**
     * Email address that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    emailAddress?: string | null;
    /**
     * Phone number that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    phone?: string | null;
    /**
     * An array of Addresses.
     * @type {Array<Items>}
     * @memberof Supplier
     */
    addresses?: Array<Items> | null;
    /**
     * Company number of the supplier. In the UK, this is typically the company registration number issued by Companies House.
     * @type {string}
     * @memberof Supplier
     */
    registrationNumber?: string | null;
    /**
     * Supplier's company tax number.
     * @type {string}
     * @memberof Supplier
     */
    taxNumber?: string | null;
    /**
     * 
     * @type {SupplierStatus}
     * @memberof Supplier
     */
    status: SupplierStatus;
    /**
     * Default currency the supplier's transactional data is recorded in.
     * @type {string}
     * @memberof Supplier
     */
    defaultCurrency?: string | null;
    /**
     * 
     * @type {Metadata}
     * @memberof Supplier
     */
    metadata?: Metadata;
    /**
     * 
     * @type {SupplementalData}
     * @memberof Supplier
     */
    supplementalData?: SupplementalData;
}

export interface SupplierUpdateResponseSupplier {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the supplier, unique to the company in the accounting software.
     * @type {string}
     * @memberof Supplier
     */
    id?: string;
    /**
     * Name of the supplier as recorded in the accounting system, typically the company name.
     * @type {string}
     * @memberof Supplier
     */
    supplierName?: string | null;
    /**
     * Name of the main contact for the supplier.
     * @type {string}
     * @memberof Supplier
     */
    contactName?: string | null;
    /**
     * Email address that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    emailAddress?: string | null;
    /**
     * Phone number that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    phone?: string | null;
    /**
     * An array of Addresses.
     * @type {Array<Items>}
     * @memberof Supplier
     */
    addresses?: Array<Items> | null;
    /**
     * Company number of the supplier. In the UK, this is typically the company registration number issued by Companies House.
     * @type {string}
     * @memberof Supplier
     */
    registrationNumber?: string | null;
    /**
     * Supplier's company tax number.
     * @type {string}
     * @memberof Supplier
     */
    taxNumber?: string | null;
    /**
     * 
     * @type {SupplierStatus}
     * @memberof Supplier
     */
    status: SupplierStatus;
    /**
     * Default currency the supplier's transactional data is recorded in.
     * @type {string}
     * @memberof Supplier
     */
    defaultCurrency?: string | null;
    /**
     * 
     * @type {Metadata}
     * @memberof Supplier
     */
    metadata?: Metadata;
    /**
     * 
     * @type {SupplementalData}
     * @memberof Supplier
     */
    supplementalData?: SupplementalData;
}

export interface SuppliersAccountingSupplier {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the supplier, unique to the company in the accounting software.
     * @type {string}
     * @memberof Supplier
     */
    id?: string;
    /**
     * Name of the supplier as recorded in the accounting system, typically the company name.
     * @type {string}
     * @memberof Supplier
     */
    supplierName?: string | null;
    /**
     * Name of the main contact for the supplier.
     * @type {string}
     * @memberof Supplier
     */
    contactName?: string | null;
    /**
     * Email address that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    emailAddress?: string | null;
    /**
     * Phone number that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    phone?: string | null;
    /**
     * An array of Addresses.
     * @type {Array<Items>}
     * @memberof Supplier
     */
    addresses?: Array<Items> | null;
    /**
     * Company number of the supplier. In the UK, this is typically the company registration number issued by Companies House.
     * @type {string}
     * @memberof Supplier
     */
    registrationNumber?: string | null;
    /**
     * Supplier's company tax number.
     * @type {string}
     * @memberof Supplier
     */
    taxNumber?: string | null;
    /**
     * 
     * @type {SupplierStatus}
     * @memberof Supplier
     */
    status: SupplierStatus;
    /**
     * Default currency the supplier's transactional data is recorded in.
     * @type {string}
     * @memberof Supplier
     */
    defaultCurrency?: string | null;
    /**
     * 
     * @type {Metadata}
     * @memberof Supplier
     */
    metadata?: Metadata;
    /**
     * 
     * @type {SupplementalData}
     * @memberof Supplier
     */
    supplementalData?: SupplementalData;
}

export interface SuppliersListResponseAccountingSupplier {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the supplier, unique to the company in the accounting software.
     * @type {string}
     * @memberof Supplier
     */
    id?: string;
    /**
     * Name of the supplier as recorded in the accounting system, typically the company name.
     * @type {string}
     * @memberof Supplier
     */
    supplierName?: string | null;
    /**
     * Name of the main contact for the supplier.
     * @type {string}
     * @memberof Supplier
     */
    contactName?: string | null;
    /**
     * Email address that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    emailAddress?: string | null;
    /**
     * Phone number that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    phone?: string | null;
    /**
     * An array of Addresses.
     * @type {Array<Items>}
     * @memberof Supplier
     */
    addresses?: Array<Items> | null;
    /**
     * Company number of the supplier. In the UK, this is typically the company registration number issued by Companies House.
     * @type {string}
     * @memberof Supplier
     */
    registrationNumber?: string | null;
    /**
     * Supplier's company tax number.
     * @type {string}
     * @memberof Supplier
     */
    taxNumber?: string | null;
    /**
     * 
     * @type {SupplierStatus}
     * @memberof Supplier
     */
    status: SupplierStatus;
    /**
     * Default currency the supplier's transactional data is recorded in.
     * @type {string}
     * @memberof Supplier
     */
    defaultCurrency?: string | null;
    /**
     * 
     * @type {Metadata}
     * @memberof Supplier
     */
    metadata?: Metadata;
    /**
     * 
     * @type {SupplementalData}
     * @memberof Supplier
     */
    supplementalData?: SupplementalData;
}

export interface SuppliersListResponseSupplier {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the supplier, unique to the company in the accounting software.
     * @type {string}
     * @memberof Supplier
     */
    id?: string;
    /**
     * Name of the supplier as recorded in the accounting system, typically the company name.
     * @type {string}
     * @memberof Supplier
     */
    supplierName?: string | null;
    /**
     * Name of the main contact for the supplier.
     * @type {string}
     * @memberof Supplier
     */
    contactName?: string | null;
    /**
     * Email address that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    emailAddress?: string | null;
    /**
     * Phone number that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    phone?: string | null;
    /**
     * An array of Addresses.
     * @type {Array<Items>}
     * @memberof Supplier
     */
    addresses?: Array<Items> | null;
    /**
     * Company number of the supplier. In the UK, this is typically the company registration number issued by Companies House.
     * @type {string}
     * @memberof Supplier
     */
    registrationNumber?: string | null;
    /**
     * Supplier's company tax number.
     * @type {string}
     * @memberof Supplier
     */
    taxNumber?: string | null;
    /**
     * 
     * @type {SupplierStatus}
     * @memberof Supplier
     */
    status: SupplierStatus;
    /**
     * Default currency the supplier's transactional data is recorded in.
     * @type {string}
     * @memberof Supplier
     */
    defaultCurrency?: string | null;
    /**
     * 
     * @type {Metadata}
     * @memberof Supplier
     */
    metadata?: Metadata;
    /**
     * 
     * @type {SupplementalData}
     * @memberof Supplier
     */
    supplementalData?: SupplementalData;
}

export interface SuppliersSupplier {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the supplier, unique to the company in the accounting software.
     * @type {string}
     * @memberof Supplier
     */
    id?: string;
    /**
     * Name of the supplier as recorded in the accounting system, typically the company name.
     * @type {string}
     * @memberof Supplier
     */
    supplierName?: string | null;
    /**
     * Name of the main contact for the supplier.
     * @type {string}
     * @memberof Supplier
     */
    contactName?: string | null;
    /**
     * Email address that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    emailAddress?: string | null;
    /**
     * Phone number that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    phone?: string | null;
    /**
     * An array of Addresses.
     * @type {Array<Items>}
     * @memberof Supplier
     */
    addresses?: Array<Items> | null;
    /**
     * Company number of the supplier. In the UK, this is typically the company registration number issued by Companies House.
     * @type {string}
     * @memberof Supplier
     */
    registrationNumber?: string | null;
    /**
     * Supplier's company tax number.
     * @type {string}
     * @memberof Supplier
     */
    taxNumber?: string | null;
    /**
     * 
     * @type {SupplierStatus}
     * @memberof Supplier
     */
    status: SupplierStatus;
    /**
     * Default currency the supplier's transactional data is recorded in.
     * @type {string}
     * @memberof Supplier
     */
    defaultCurrency?: string | null;
    /**
     * 
     * @type {Metadata}
     * @memberof Supplier
     */
    metadata?: Metadata;
    /**
     * 
     * @type {SupplementalData}
     * @memberof Supplier
     */
    supplementalData?: SupplementalData;
}

export interface UpdateSupplierRequestBody {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the supplier, unique to the company in the accounting software.
     * @type {string}
     * @memberof Supplier
     */
    id?: string;
    /**
     * Name of the supplier as recorded in the accounting system, typically the company name.
     * @type {string}
     * @memberof Supplier
     */
    supplierName?: string | null;
    /**
     * Name of the main contact for the supplier.
     * @type {string}
     * @memberof Supplier
     */
    contactName?: string | null;
    /**
     * Email address that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    emailAddress?: string | null;
    /**
     * Phone number that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    phone?: string | null;
    /**
     * An array of Addresses.
     * @type {Array<Items>}
     * @memberof Supplier
     */
    addresses?: Array<Items> | null;
    /**
     * Company number of the supplier. In the UK, this is typically the company registration number issued by Companies House.
     * @type {string}
     * @memberof Supplier
     */
    registrationNumber?: string | null;
    /**
     * Supplier's company tax number.
     * @type {string}
     * @memberof Supplier
     */
    taxNumber?: string | null;
    /**
     * 
     * @type {SupplierStatus}
     * @memberof Supplier
     */
    status: SupplierStatus;
    /**
     * Default currency the supplier's transactional data is recorded in.
     * @type {string}
     * @memberof Supplier
     */
    defaultCurrency?: string | null;
    /**
     * 
     * @type {Metadata}
     * @memberof Supplier
     */
    metadata?: Metadata;
    /**
     * 
     * @type {SupplementalData}
     * @memberof Supplier
     */
    supplementalData?: SupplementalData;
}

export interface UpdateSupplierResponseAccountingSupplier {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the supplier, unique to the company in the accounting software.
     * @type {string}
     * @memberof Supplier
     */
    id?: string;
    /**
     * Name of the supplier as recorded in the accounting system, typically the company name.
     * @type {string}
     * @memberof Supplier
     */
    supplierName?: string | null;
    /**
     * Name of the main contact for the supplier.
     * @type {string}
     * @memberof Supplier
     */
    contactName?: string | null;
    /**
     * Email address that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    emailAddress?: string | null;
    /**
     * Phone number that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    phone?: string | null;
    /**
     * An array of Addresses.
     * @type {Array<Items>}
     * @memberof Supplier
     */
    addresses?: Array<Items> | null;
    /**
     * Company number of the supplier. In the UK, this is typically the company registration number issued by Companies House.
     * @type {string}
     * @memberof Supplier
     */
    registrationNumber?: string | null;
    /**
     * Supplier's company tax number.
     * @type {string}
     * @memberof Supplier
     */
    taxNumber?: string | null;
    /**
     * 
     * @type {SupplierStatus}
     * @memberof Supplier
     */
    status: SupplierStatus;
    /**
     * Default currency the supplier's transactional data is recorded in.
     * @type {string}
     * @memberof Supplier
     */
    defaultCurrency?: string | null;
    /**
     * 
     * @type {Metadata}
     * @memberof Supplier
     */
    metadata?: Metadata;
    /**
     * 
     * @type {SupplementalData}
     * @memberof Supplier
     */
    supplementalData?: SupplementalData;
}

export interface UpdateSupplierResponseSupplier {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the supplier, unique to the company in the accounting software.
     * @type {string}
     * @memberof Supplier
     */
    id?: string;
    /**
     * Name of the supplier as recorded in the accounting system, typically the company name.
     * @type {string}
     * @memberof Supplier
     */
    supplierName?: string | null;
    /**
     * Name of the main contact for the supplier.
     * @type {string}
     * @memberof Supplier
     */
    contactName?: string | null;
    /**
     * Email address that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    emailAddress?: string | null;
    /**
     * Phone number that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    phone?: string | null;
    /**
     * An array of Addresses.
     * @type {Array<Items>}
     * @memberof Supplier
     */
    addresses?: Array<Items> | null;
    /**
     * Company number of the supplier. In the UK, this is typically the company registration number issued by Companies House.
     * @type {string}
     * @memberof Supplier
     */
    registrationNumber?: string | null;
    /**
     * Supplier's company tax number.
     * @type {string}
     * @memberof Supplier
     */
    taxNumber?: string | null;
    /**
     * 
     * @type {SupplierStatus}
     * @memberof Supplier
     */
    status: SupplierStatus;
    /**
     * Default currency the supplier's transactional data is recorded in.
     * @type {string}
     * @memberof Supplier
     */
    defaultCurrency?: string | null;
    /**
     * 
     * @type {Metadata}
     * @memberof Supplier
     */
    metadata?: Metadata;
    /**
     * 
     * @type {SupplementalData}
     * @memberof Supplier
     */
    supplementalData?: SupplementalData;
}

export interface UpdateSupplierSupplier {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof Supplier
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the supplier, unique to the company in the accounting software.
     * @type {string}
     * @memberof Supplier
     */
    id?: string;
    /**
     * Name of the supplier as recorded in the accounting system, typically the company name.
     * @type {string}
     * @memberof Supplier
     */
    supplierName?: string | null;
    /**
     * Name of the main contact for the supplier.
     * @type {string}
     * @memberof Supplier
     */
    contactName?: string | null;
    /**
     * Email address that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    emailAddress?: string | null;
    /**
     * Phone number that the supplier may be contacted on.
     * @type {string}
     * @memberof Supplier
     */
    phone?: string | null;
    /**
     * An array of Addresses.
     * @type {Array<Items>}
     * @memberof Supplier
     */
    addresses?: Array<Items> | null;
    /**
     * Company number of the supplier. In the UK, this is typically the company registration number issued by Companies House.
     * @type {string}
     * @memberof Supplier
     */
    registrationNumber?: string | null;
    /**
     * Supplier's company tax number.
     * @type {string}
     * @memberof Supplier
     */
    taxNumber?: string | null;
    /**
     * 
     * @type {SupplierStatus}
     * @memberof Supplier
     */
    status: SupplierStatus;
    /**
     * Default currency the supplier's transactional data is recorded in.
     * @type {string}
     * @memberof Supplier
     */
    defaultCurrency?: string | null;
    /**
     * 
     * @type {Metadata}
     * @memberof Supplier
     */
    metadata?: Metadata;
    /**
     * 
     * @type {SupplementalData}
     * @memberof Supplier
     */
    supplementalData?: SupplementalData;
}

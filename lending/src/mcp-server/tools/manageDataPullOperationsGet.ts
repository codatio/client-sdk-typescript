/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { manageDataPullOperationsGet } from "../../funcs/manageDataPullOperationsGet.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetPullOperationRequest$inboundSchema,
};

export const tool$manageDataPullOperationsGet: ToolDefinition<typeof args> = {
  name: "manage-data-pull-operations-get",
  description: `Get pull operation

Retrieve information about a single dataset or pull operation.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await manageDataPullOperationsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

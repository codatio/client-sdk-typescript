/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { transactionsDirectCostsGetAttachment } from "../../funcs/transactionsDirectCostsGetAttachment.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAccountingDirectCostAttachmentRequest$inboundSchema,
};

export const tool$transactionsDirectCostsGetAttachment: ToolDefinition<
  typeof args
> = {
  name: "transactions-direct-costs-get-attachment",
  description: `Get direct cost attachment

The *Get direct cost attachment* endpoint returns a specific attachment for a given \`directCostId\` and \`attachmentId\`.

[Direct costs](https://docs.codat.io/lending-api#/schemas/DirectCost) are business expenses that don't impact Accounts Payable.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await transactionsDirectCostsGetAttachment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

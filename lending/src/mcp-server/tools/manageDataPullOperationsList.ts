/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { manageDataPullOperationsList } from "../../funcs/manageDataPullOperationsList.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListPullOperationsRequest$inboundSchema,
};

export const tool$manageDataPullOperationsList: ToolDefinition<typeof args> = {
  name: "manage-data-pull-operations-list",
  description: `List pull operations

Gets the pull operation history (datasets) for a given company.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await manageDataPullOperationsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

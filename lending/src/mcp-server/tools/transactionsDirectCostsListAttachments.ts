/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { transactionsDirectCostsListAttachments } from "../../funcs/transactionsDirectCostsListAttachments.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAccountingDirectCostAttachmentsRequest$inboundSchema,
};

export const tool$transactionsDirectCostsListAttachments: ToolDefinition<
  typeof args
> = {
  name: "transactions-direct-costs-list-attachments",
  description: `List direct cost attachments

The *List direct cost attachments* endpoint returns a list of attachments available to download for given \`directCostId\`.

[Direct costs](https://docs.codat.io/lending-api#/schemas/DirectCost) are purchases of items that are paid off at the point of the purchase.
`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await transactionsDirectCostsListAttachments(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

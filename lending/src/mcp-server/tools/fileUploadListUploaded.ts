/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { fileUploadListUploaded } from "../../funcs/fileUploadListUploaded.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListFilesRequest$inboundSchema,
};

export const tool$fileUploadListUploaded: ToolDefinition<typeof args> = {
  name: "file-upload-list-uploaded",
  description: `List all files uploaded by a company

The *List files* endpoint returns a list of all files uploaded to Codat by the SMB. `,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await fileUploadListUploaded(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

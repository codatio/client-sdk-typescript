/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountsReceivableCustomersGetAttachment } from "../../funcs/accountsReceivableCustomersGetAttachment.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAccountingCustomerAttachmentRequest$inboundSchema,
};

export const tool$accountsReceivableCustomersGetAttachment: ToolDefinition<
  typeof args
> = {
  name: "accounts-receivable-customers-get-attachment",
  description: `Get customer attachment

The *Get customer attachment* endpoint returns a specific attachment for a given \`customerId\` and \`attachmentId\`.

[Customers](https://docs.codat.io/lending-api#/schemas/Customer) are people or organizations that buy goods or services from the SMB.
`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountsReceivableCustomersGetAttachment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

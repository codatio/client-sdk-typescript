/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { salesMetricsGetLifetimeValue } from "../../funcs/salesMetricsGetLifetimeValue.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetCommerceLifetimeValueMetricsRequest$inboundSchema,
};

export const tool$salesMetricsGetLifetimeValue: ToolDefinition<typeof args> = {
  name: "sales-metrics-get-lifetime-value",
  description: `Get lifetime value metrics

The *Get lifetime value metrics* endpoint returns the average revenue that a specific company will generate throughout its lifespan over one or more periods of time.

This detail helps you assess a merchant's health and advise them on performance improvement strategies. It also provides you with key insights you need to assess the credit risk of a company.

[Learn more](https://docs.codat.io/lending/features/sales-overview#metrics) about the formulas used to calculate the lifetime value metrics.

#### Response structure

The Lifetime value report's dimensions and measures are:

| Index         | Dimensions             |
|---------------|------------------------|
| \`index\` = 0   | Period                 |
| \`index\` = 1   | Lifetime value metrics |

| Index             | Measures |
|-------------------|---------|
|   \`index\` = 1     | Value   |

The report data then combines multiple reporting dimensions and measures and outputs the value of each combination.
`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await salesMetricsGetLifetimeValue(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

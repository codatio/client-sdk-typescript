/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { transactionsDirectCostsDownloadAttachment } from "../../funcs/transactionsDirectCostsDownloadAttachment.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.DownloadAccountingDirectCostAttachmentRequest$inboundSchema,
};

export const tool$transactionsDirectCostsDownloadAttachment: ToolDefinition<
  typeof args
> = {
  name: "transactions-direct-costs-download-attachment",
  description: `Download direct cost attachment

The *Download direct cost attachment* endpoint downloads a specific attachment for a given \`directCostId\` and \`attachmentId\`.

[Direct costs](https://docs.codat.io/lending-api#/schemas/DirectCost) are business expenses that don't impact Accounts Payable.
`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await transactionsDirectCostsDownloadAttachment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

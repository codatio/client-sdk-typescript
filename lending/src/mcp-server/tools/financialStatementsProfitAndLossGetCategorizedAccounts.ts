/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { financialStatementsProfitAndLossGetCategorizedAccounts } from "../../funcs/financialStatementsProfitAndLossGetCategorizedAccounts.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetCategorizedProfitAndLossStatementRequest$inboundSchema,
};

export const tool$financialStatementsProfitAndLossGetCategorizedAccounts:
  ToolDefinition<typeof args> = {
    name: "financial-statements-profit-and-loss-get-categorized-accounts",
    description: `Get categorized profit and loss statement

The *Get categorized profit and loss statement* endpoint returns a list of categorized accounts that appear on a company’s Profit and Loss statement. It also includes a balance as of the financial statement date.

Codat suggests a category for each account automatically, but you can [change it](https://docs.codat.io/lending/features/financial-statements-overview#recategorizing-accounts) to a more suitable one.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await financialStatementsProfitAndLossGetCategorizedAccounts(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };

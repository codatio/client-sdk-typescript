/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { loanWritebackPaymentsGetCreateModel } from "../../funcs/loanWritebackPaymentsGetCreateModel.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetCreatePaymentModelRequest$inboundSchema,
};

export const tool$loanWritebackPaymentsGetCreateModel: ToolDefinition<
  typeof args
> = {
  name: "loan-writeback-payments-get-create-model",
  description: `Get create payment model

The *Get create payment model* endpoint returns the expected data for the request payload when creating a [payment](https://docs.codat.io/lending-api#/schemas/Payment) for a given company and integration.

[Payments](https://docs.codat.io/lending-api#/schemas/Payment) represent an allocation of money within any customer accounts receivable account.

**Integration-specific behavior**

See the *response examples* for integration-specific indicative models.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await loanWritebackPaymentsGetCreateModel(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { financialStatementsBalanceSheetGet } from "../../funcs/financialStatementsBalanceSheetGet.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAccountingBalanceSheetRequest$inboundSchema,
};

export const tool$financialStatementsBalanceSheetGet: ToolDefinition<
  typeof args
> = {
  name: "financial-statements-balance-sheet-get",
  description: `Get balance sheet

Gets the latest balance sheet for a company.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await financialStatementsBalanceSheetGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

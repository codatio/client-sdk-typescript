/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountsReceivableReportsIsAgedCreditorsAvailable } from "../../funcs/accountsReceivableReportsIsAgedCreditorsAvailable.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.IsAgedCreditorsReportAvailableRequest$inboundSchema,
};

export const tool$accountsReceivableReportsIsAgedCreditorsAvailable:
  ToolDefinition<typeof args> = {
    name: "accounts-receivable-reports-is-aged-creditors-available",
    description: `Aged creditors report available

Indicates whether the aged creditor report is available for the company.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await accountsReceivableReportsIsAgedCreditorsAvailable(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };

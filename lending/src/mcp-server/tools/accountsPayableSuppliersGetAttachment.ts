/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountsPayableSuppliersGetAttachment } from "../../funcs/accountsPayableSuppliersGetAttachment.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAccountingSupplierAttachmentRequest$inboundSchema,
};

export const tool$accountsPayableSuppliersGetAttachment: ToolDefinition<
  typeof args
> = {
  name: "accounts-payable-suppliers-get-attachment",
  description: `Get supplier attachment

The *Get supplier attachment* endpoint returns a specific attachment for a given \`supplierId\` and \`attachmentId\`.

[Suppliers](https://docs.codat.io/lending-api#/schemas/Supplier) are people or organizations that provide something, such as a product or service.
`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountsPayableSuppliersGetAttachment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

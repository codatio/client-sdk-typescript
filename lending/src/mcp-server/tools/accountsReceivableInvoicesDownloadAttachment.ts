/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountsReceivableInvoicesDownloadAttachment } from "../../funcs/accountsReceivableInvoicesDownloadAttachment.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DownloadAccountingInvoiceAttachmentRequest$inboundSchema,
};

export const tool$accountsReceivableInvoicesDownloadAttachment: ToolDefinition<
  typeof args
> = {
  name: "accounts-receivable-invoices-download-attachment",
  description: `Download invoice attachment

The *Download invoice attachment* endpoint downloads a specific attachment for a given \`invoiceId\` and \`attachmentId\`.

[Invoices](https://docs.codat.io/lending-api#/schemas/Invoice) are itemized records of goods sold or services provided to a customer.
`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] =
      await accountsReceivableInvoicesDownloadAttachment(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

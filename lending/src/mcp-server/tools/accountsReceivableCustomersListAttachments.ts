/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountsReceivableCustomersListAttachments } from "../../funcs/accountsReceivableCustomersListAttachments.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAccountingCustomerAttachmentsRequest$inboundSchema,
};

export const tool$accountsReceivableCustomersListAttachments: ToolDefinition<
  typeof args
> = {
  name: "accounts-receivable-customers-list-attachments",
  description: `List customer attachments

The *List customer attachments* endpoint returns a list of attachments avialable to download for given \`customerId\`.

[Customers](https://docs.codat.io/lending-api#/schemas/Customer) are people or organizations that buy goods or services from the SMB.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountsReceivableCustomersListAttachments(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

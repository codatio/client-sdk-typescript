/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountsPayableBillsListAttachments } from "../../funcs/accountsPayableBillsListAttachments.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAccountingBillAttachmentsRequest$inboundSchema,
};

export const tool$accountsPayableBillsListAttachments: ToolDefinition<
  typeof args
> = {
  name: "accounts-payable-bills-list-attachments",
  description: `List bill attachments

The *List bill attachments* endpoint returns a list of attachments available to download for a given \`billId\`.

[Bills](https://docs.codat.io/lending-api#/schemas/Bill) are invoices that represent the SMB's financial obligations to their supplier for a purchase of goods or services.
`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountsPayableBillsListAttachments(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

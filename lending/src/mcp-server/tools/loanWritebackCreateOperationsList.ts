/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { loanWritebackCreateOperationsList } from "../../funcs/loanWritebackCreateOperationsList.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListCreateOperationsRequest$inboundSchema,
};

export const tool$loanWritebackCreateOperationsList: ToolDefinition<
  typeof args
> = {
  name: "loan-writeback-create-operations-list",
  description: `List create operations

List create operations.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await loanWritebackCreateOperationsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

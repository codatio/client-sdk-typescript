/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountsReceivableReportsGetAgedCreditors } from "../../funcs/accountsReceivableReportsGetAgedCreditors.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAccountingAgedCreditorsReportRequest$inboundSchema,
};

export const tool$accountsReceivableReportsGetAgedCreditors: ToolDefinition<
  typeof args
> = {
  name: "accounts-receivable-reports-get-aged-creditors",
  description: `Aged creditors report

Returns aged creditors report for company that shows the total balance owed by a business to its suppliers over time.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountsReceivableReportsGetAgedCreditors(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { financialStatementsProfitAndLossGet } from "../../funcs/financialStatementsProfitAndLossGet.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAccountingProfitAndLossRequest$inboundSchema,
};

export const tool$financialStatementsProfitAndLossGet: ToolDefinition<
  typeof args
> = {
  name: "financial-statements-profit-and-loss-get",
  description: `Get profit and loss

Gets the latest profit and loss for a company.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await financialStatementsProfitAndLossGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountsPayableSuppliersListAttachments } from "../../funcs/accountsPayableSuppliersListAttachments.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAccountingSupplierAttachmentsRequest$inboundSchema,
};

export const tool$accountsPayableSuppliersListAttachments: ToolDefinition<
  typeof args
> = {
  name: "accounts-payable-suppliers-list-attachments",
  description: `List supplier attachments

The *List supplier attachments* endpoint returns a list of attachments available to download for given \`supplierId\`.

[Suppliers](https://docs.codat.io/lending-api#/schemas/Supplier) are people or organizations that provide something, such as a product or service.
`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountsPayableSuppliersListAttachments(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

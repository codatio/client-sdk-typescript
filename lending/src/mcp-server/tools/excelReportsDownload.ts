/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { excelReportsDownload } from "../../funcs/excelReportsDownload.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DownloadExcelReportRequest$inboundSchema,
};

export const tool$excelReportsDownload: ToolDefinition<typeof args> = {
  name: "excel-reports-download",
  description: `Download Excel report

The *Download Excel report* endpoint downloads the latest successfully generated Excel report of a specified report type for a given company. 

The downloadable Excel file is returned in the response. You can save it to your local machine.

You can [learn more](https://docs.codat.io/lending/features/excel-download-overview#feature-components) about valid Excel report types.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await excelReportsDownload(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

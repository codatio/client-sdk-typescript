/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { fileUploadDownload } from "../../funcs/fileUploadDownload.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DownloadFilesRequest$inboundSchema,
};

export const tool$fileUploadDownload: ToolDefinition<typeof args> = {
  name: "file-upload-download",
  description: `Download all files for a company

The *Download files* endpoint downloads all files that have  been uploaded by to SMB to Codat. A \`date\` may be specified to download any files uploaded on the date provided.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await fileUploadDownload(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

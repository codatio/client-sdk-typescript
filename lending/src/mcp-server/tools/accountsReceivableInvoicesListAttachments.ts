/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountsReceivableInvoicesListAttachments } from "../../funcs/accountsReceivableInvoicesListAttachments.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAccountingInvoiceAttachmentsRequest$inboundSchema,
};

export const tool$accountsReceivableInvoicesListAttachments: ToolDefinition<
  typeof args
> = {
  name: "accounts-receivable-invoices-list-attachments",
  description: `List invoice attachments

The *List invoice attachments* endpoint returns a list of attachments available to download for given \`invoiceId\`.

[Invoices](https://docs.codat.io/lending-api#/schemas/Invoice) are itemized records of goods sold or services provided to a customer.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountsReceivableInvoicesListAttachments(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

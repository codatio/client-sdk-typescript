/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountsReceivableInvoicesGetAttachment } from "../../funcs/accountsReceivableInvoicesGetAttachment.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAccountingInvoiceAttachmentRequest$inboundSchema,
};

export const tool$accountsReceivableInvoicesGetAttachment: ToolDefinition<
  typeof args
> = {
  name: "accounts-receivable-invoices-get-attachment",
  description: `Get invoice attachment

The *Get invoice attachment* endpoint returns a specific attachment for a given \`invoiceId\` and \`attachmentId\`.

[Invoices](https://docs.codat.io/lending-api#/schemas/Invoice) are itemized records of goods sold or services provided to a customer.
`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountsReceivableInvoicesGetAttachment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

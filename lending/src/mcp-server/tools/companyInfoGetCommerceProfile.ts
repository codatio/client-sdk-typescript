/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { companyInfoGetCommerceProfile } from "../../funcs/companyInfoGetCommerceProfile.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetCommerceProfileRequest$inboundSchema,
};

export const tool$companyInfoGetCommerceProfile: ToolDefinition<typeof args> = {
  name: "company-info-get-commerce-profile",
  description: `Get company commerce profile

Retrieve information about the company, as seen in the commerce
platform.

This may include information like addresses, tax registration details and
social media or website information."`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await companyInfoGetCommerceProfile(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

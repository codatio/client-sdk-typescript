/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountsReceivableDirectIncomesGetAttachment } from "../../funcs/accountsReceivableDirectIncomesGetAttachment.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAccountingDirectIncomeAttachmentRequest$inboundSchema,
};

export const tool$accountsReceivableDirectIncomesGetAttachment: ToolDefinition<
  typeof args
> = {
  name: "accounts-receivable-direct-incomes-get-attachment",
  description: `Get direct income attachment

The *Get direct income attachment* endpoint returns a specific attachment for a given \`directIncomeId\` and \`attachmentId\`.

[Direct incomes](https://docs.codat.io/lending-api#/schemas/DirectIncome) are sales of items directly to a customer where payment is received at the point of the sale.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] =
      await accountsReceivableDirectIncomesGetAttachment(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

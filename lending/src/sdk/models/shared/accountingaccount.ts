/* tslint:disable */
/* eslint-disable */
/**
 * Lending
 * Our Lending solution helps you make smarter credit decisions on small businesses by enabling you to pull your customers\' latest data from accounting, banking, and commerce software they are already using. It also includes features to help providers verify the accuracy of data and process it more efficiently.  The Lending solution is built on top of the latest accounting, commerce, and banking data, providing you with the most important data points you need to get a full picture of SMB creditworthiness and make a comprehensive assessment of your customers.  [Explore solution](https://docs.codat.io/lending/overview) | [See OpenAPI spec](https://github.com/codatio/oas)  <!-- Start Codat Tags Table --> ## Endpoints  | Endpoints | Description | | :- |:- | | Companies | Create and manage your SMB users\' companies. | | Connections | Create new and manage existing data connections for a company. | | Bank statements | Retrieve banking data from linked bank accounts. | | Sales | Retrieve standardized sales data from a linked commerce software. | | Financial statements | Financial data and reports from a linked accounting software. | | Liabilities | Debt and other liabilities. | | Accounts payable | Data from a linked accounting software representing money the business owes money to its suppliers. | | Accounts receivable | Data from a linked accounting software representing money owed to the business for sold goods or services. | | Transactions | Data from a linked accounting software representing transactions. | | Company info | View company information fetched from the source platform. | | Data integrity | Match mutable accounting data with immutable banking data to increase confidence in financial data. | | Excel reports | Download reports in Excel format. | | Manage data | Control how data is retrieved from an integration. | | File upload | Endpoints to manage uploaded files. | | Loan writeback | Implement the [loan writeback](https://docs.codat.io/lending/guides/loan-writeback/introduction) procedure in your lending process to maintain an accurate position of a loan during the entire lending cycle. | <!-- End Codat Tags Table -->
 *
 * The version of the OpenAPI document: 3.0.0
 * Contact: support@codat.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import type { AccountingCreateAccountResponseValidDataTypeLinks } from './validdatatypelinks';
import type { AccountType } from '../_speakeasy_enums';
import { mapValues } from '../../../runtime';
import { Decimal, toDecimal, fromDecimal } from '../../../runtime';
import type { AccountStatus } from './accountstatus';
import {
    AccountStatusFromJSON,
    AccountStatusFromJSONTyped,
    AccountStatusToJSON,
    AccountStatusToJSONTyped,
} from './accountstatus';
import type { SupplementalData } from './supplementaldata';
import {
    SupplementalDataFromJSON,
    SupplementalDataFromJSONTyped,
    SupplementalDataToJSON,
    SupplementalDataToJSONTyped,
} from './supplementaldata';
import type { Metadata } from './metadata';
import {
    MetadataFromJSON,
    MetadataFromJSONTyped,
    MetadataToJSON,
    MetadataToJSONTyped,
} from './metadata';
import type { ValidDataTypeLinks } from './validdatatypelinks';
import {
    ValidDataTypeLinksFromJSON,
    ValidDataTypeLinksFromJSONTyped,
    ValidDataTypeLinksToJSON,
    ValidDataTypeLinksToJSONTyped,
} from './validdatatypelinks';

/**
 * ﻿> **Language tip:** Accounts are also referred to as **chart of accounts**, **nominal accounts**, and **general ledger**.
 * 
 * ## Overview
 * 
 * Accounts are the categories a business uses to record accounting transactions. From the Accounts endpoints, you can retrieve a list of all accounts for a specified company.
 * 
 * The categories for an account include:
 * * Asset
 * * Expense
 * * Income
 * * Liability
 * * Equity.
 * 
 * The same account may have a different category based on the integration it is used in. For example, a current account (known as checking in the US) should be categorized as `Asset.Current` for Xero, and `Asset.Bank.Checking` for QuickBooks Online.
 * 
 * At the same time, each integration may have its own requirements to the categories. For example, a Paypal account in Xero is of the `Asset.Bank` category and therefore requires additional properties to be provided.
 * 
 * To determine the list of allowed categories for a specific integration, you can:
 * - Follow our [Create, update, delete data](https://docs.codat.io/using-the-api/push) guide and use the [Get create account model](https://docs.codat.io/lending-api#/operations/get-create-chartOfAccounts-model).
 * - Refer to the integration's own documentation.
 * 
 * > **Accounts with no category**
 * >
 * > If an account is pulled from the chart of accounts and its nominal code does not lie within the category layout for the company's accounts, then the **type** is `Unknown`. The **fullyQualifiedCategory** and **fullyQualifiedName** fields return `null`.
 * >
 * > This approach gives a true representation of the company's accounts whilst preventing distorting financials such as a company's profit and loss and balance sheet reports.
 * @export
 * @interface AccountingAccount
 */
export interface AccountingAccount {
    /**
     * Reference given to each nominal account for a business. It ensures money is allocated to the correct account. This code isn't a unique identifier in the Codat system.
     * @type {string}
     * @memberof AccountingAccount
     */
    nominalCode?: string | null;
    /**
     * Name of the account.
     * @type {string}
     * @memberof AccountingAccount
     */
    name?: string | null;
    /**
     * Description for the account.
     * @type {string}
     * @memberof AccountingAccount
     */
    description?: string | null;
    /**
     * Full category of the account. 
     * 
     * For example, `Liability.Current` or `Income.Revenue`. To determine a list of possible categories for each integration, see our examples, follow our [Create, update, delete data](https://docs.codat.io/using-the-api/push) guide, or refer to the integration's own documentation.
     * @type {string}
     * @memberof AccountingAccount
     */
    fullyQualifiedCategory?: string | null;
    /**
     * Full name of the account, for example:
     * - `Cash On Hand`
     * - `Rents Held In Trust`
     * - `Fixed Asset`
     * @type {string}
     * @memberof AccountingAccount
     */
    fullyQualifiedName?: string | null;
    /**
     * The currency data type in Codat is the [ISO 4217](https://en.wikipedia.org/wiki/ISO_4217) currency code, e.g. _GBP_.
     * 
     * ## Unknown currencies
     * 
     * In line with the ISO 4217 specification, the code _XXX_ is used when the data source does not return a currency for a transaction. 
     * 
     * There are only a very small number of edge cases where this currency code is returned by the Codat system.
     * @type {string}
     * @memberof AccountingAccount
     */
    currency?: string;
    /**
     * Current balance in the account.
     * @type {string}
     * @memberof AccountingAccount
     */
    currentBalance?: Decimal | number | null;
    /**
     * The type of bank account e.g. Credit.
     * @type {string}
     * @memberof AccountingAccount
     */
    type?: AccountType;
    /**
     * 
     * @type {AccountStatus}
     * @memberof AccountingAccount
     */
    status?: AccountStatus;
    /**
     * Confirms whether the account is a bank account or not.
     * @type {boolean}
     * @memberof AccountingAccount
     */
    isBankAccount?: boolean;
    /**
     * The validDatatypeLinks can be used to determine whether an account can be correctly mapped to another object; for example, accounts with a `type` of `income` might only support being used on an Invoice and Direct Income. For more information, see [Valid Data Type Links](/lending-api#/schemas/ValidDataTypeLinks).
     * @type {Array<ValidDataTypeLinks>}
     * @memberof AccountingAccount
     */
    validDatatypeLinks?: Array<ValidDataTypeLinks> | null;
    /**
     * 
     * @type {SupplementalData}
     * @memberof AccountingAccount
     */
    supplementalData?: SupplementalData;
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingAccount
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingAccount
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the account, unique for the company.
     * @type {string}
     * @memberof AccountingAccount
     */
    id?: string;
    /**
     * 
     * @type {Metadata}
     * @memberof AccountingAccount
     */
    metadata?: Metadata;
}



/**
 * Check if a given object implements the AccountingAccount interface.
 */
export function instanceOfAccountingAccount(value: object): value is AccountingAccount {
    return true;
}

export function AccountingAccountFromJSON(json: any): AccountingAccount {
    return AccountingAccountFromJSONTyped(json, false);
}

export function AccountingAccountFromJSONTyped(json: any, ignoreDiscriminator: boolean): AccountingAccount {
    if (json == null) {
        return json;
    }
    return {
        
        'nominalCode': json['nominalCode'] == null ? undefined : json['nominalCode'],
        'name': json['name'] == null ? undefined : json['name'],
        'description': json['description'] == null ? undefined : json['description'],
        'fullyQualifiedCategory': json['fullyQualifiedCategory'] == null ? undefined : json['fullyQualifiedCategory'],
        'fullyQualifiedName': json['fullyQualifiedName'] == null ? undefined : json['fullyQualifiedName'],
        'currency': json['currency'] == null ? undefined : json['currency'],
        'currentBalance': json['currentBalance'] == null ? undefined : toDecimal(json['currentBalance']),
        'type': json['type'] == null ? undefined : json['type'],
        'status': json['status'] == null ? undefined : AccountStatusFromJSON(json['status']),
        'isBankAccount': json['isBankAccount'] == null ? undefined : json['isBankAccount'],
        'validDatatypeLinks': json['validDatatypeLinks'] == null ? undefined : ((json['validDatatypeLinks'] as Array<any>).map(ValidDataTypeLinksFromJSON)),
        'supplementalData': json['supplementalData'] == null ? undefined : SupplementalDataFromJSON(json['supplementalData']),
        'modifiedDate': json['modifiedDate'] == null ? undefined : json['modifiedDate'],
        'sourceModifiedDate': json['sourceModifiedDate'] == null ? undefined : json['sourceModifiedDate'],
        'id': json['id'] == null ? undefined : json['id'],
        'metadata': json['metadata'] == null ? undefined : MetadataFromJSON(json['metadata']),
    };
}

export function AccountingAccountToJSON(json: any): AccountingAccount {
    return AccountingAccountToJSONTyped(json, false);
}

export function AccountingAccountToJSONTyped(value?: AccountingAccount | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'nominalCode': value['nominalCode'],
        'name': value['name'],
        'description': value['description'],
        'fullyQualifiedCategory': value['fullyQualifiedCategory'],
        'fullyQualifiedName': value['fullyQualifiedName'],
        'currency': value['currency'],
        'currentBalance': fromDecimal(value['currentBalance']),
        'type': value['type'],
        'status': AccountStatusToJSON(value['status']),
        'isBankAccount': value['isBankAccount'],
        'validDatatypeLinks': value['validDatatypeLinks'] == null ? undefined : ((value['validDatatypeLinks'] as Array<any>).map(ValidDataTypeLinksToJSON)),
        'supplementalData': SupplementalDataToJSON(value['supplementalData']),
        'modifiedDate': value['modifiedDate'],
        'sourceModifiedDate': value['sourceModifiedDate'],
        'id': value['id'],
        'metadata': MetadataToJSON(value['metadata']),
    };
}


export interface AccountCreateResponseAccountingAccount {
    /**
     * Reference given to each nominal account for a business. It ensures money is allocated to the correct account. This code isn't a unique identifier in the Codat system.
     * @type {string}
     * @memberof AccountingAccount
     */
    nominalCode?: string | null;
    /**
     * Name of the account.
     * @type {string}
     * @memberof AccountingAccount
     */
    name?: string | null;
    /**
     * Description for the account.
     * @type {string}
     * @memberof AccountingAccount
     */
    description?: string | null;
    /**
     * Full category of the account. 
     * 
     * For example, `Liability.Current` or `Income.Revenue`. To determine a list of possible categories for each integration, see our examples, follow our [Create, update, delete data](https://docs.codat.io/using-the-api/push) guide, or refer to the integration's own documentation.
     * @type {string}
     * @memberof AccountingAccount
     */
    fullyQualifiedCategory?: string | null;
    /**
     * Full name of the account, for example:
     * - `Cash On Hand`
     * - `Rents Held In Trust`
     * - `Fixed Asset`
     * @type {string}
     * @memberof AccountingAccount
     */
    fullyQualifiedName?: string | null;
    /**
     * The currency data type in Codat is the [ISO 4217](https://en.wikipedia.org/wiki/ISO_4217) currency code, e.g. _GBP_.
     * 
     * ## Unknown currencies
     * 
     * In line with the ISO 4217 specification, the code _XXX_ is used when the data source does not return a currency for a transaction. 
     * 
     * There are only a very small number of edge cases where this currency code is returned by the Codat system.
     * @type {string}
     * @memberof AccountingAccount
     */
    currency?: string;
    /**
     * Current balance in the account.
     * @type {string}
     * @memberof AccountingAccount
     */
    currentBalance?: Decimal | number | null;
    /**
     * The type of bank account e.g. Credit.
     * @type {string}
     * @memberof AccountingAccount
     */
    type?: string;
    /**
     * 
     * @type {AccountStatus}
     * @memberof AccountingAccount
     */
    status?: AccountStatus;
    /**
     * Confirms whether the account is a bank account or not.
     * @type {boolean}
     * @memberof AccountingAccount
     */
    isBankAccount?: boolean;
    /**
     * The validDatatypeLinks can be used to determine whether an account can be correctly mapped to another object; for example, accounts with a `type` of `income` might only support being used on an Invoice and Direct Income. For more information, see [Valid Data Type Links](/lending-api#/schemas/ValidDataTypeLinks).
     * @type {Array<ValidDataTypeLinks>}
     * @memberof AccountingAccount
     */
    validDatatypeLinks?: Array<ValidDataTypeLinks> | null;
    /**
     * 
     * @type {SupplementalData}
     * @memberof AccountingAccount
     */
    supplementalData?: SupplementalData;
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingAccount
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingAccount
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the account, unique for the company.
     * @type {string}
     * @memberof AccountingAccount
     */
    id?: string;
    /**
     * 
     * @type {Metadata}
     * @memberof AccountingAccount
     */
    metadata?: Metadata;
}

export interface AccountingAccountGetResponseAccountingAccount {
    /**
     * Reference given to each nominal account for a business. It ensures money is allocated to the correct account. This code isn't a unique identifier in the Codat system.
     * @type {string}
     * @memberof AccountingAccount
     */
    nominalCode?: string | null;
    /**
     * Name of the account.
     * @type {string}
     * @memberof AccountingAccount
     */
    name?: string | null;
    /**
     * Description for the account.
     * @type {string}
     * @memberof AccountingAccount
     */
    description?: string | null;
    /**
     * Full category of the account. 
     * 
     * For example, `Liability.Current` or `Income.Revenue`. To determine a list of possible categories for each integration, see our examples, follow our [Create, update, delete data](https://docs.codat.io/using-the-api/push) guide, or refer to the integration's own documentation.
     * @type {string}
     * @memberof AccountingAccount
     */
    fullyQualifiedCategory?: string | null;
    /**
     * Full name of the account, for example:
     * - `Cash On Hand`
     * - `Rents Held In Trust`
     * - `Fixed Asset`
     * @type {string}
     * @memberof AccountingAccount
     */
    fullyQualifiedName?: string | null;
    /**
     * The currency data type in Codat is the [ISO 4217](https://en.wikipedia.org/wiki/ISO_4217) currency code, e.g. _GBP_.
     * 
     * ## Unknown currencies
     * 
     * In line with the ISO 4217 specification, the code _XXX_ is used when the data source does not return a currency for a transaction. 
     * 
     * There are only a very small number of edge cases where this currency code is returned by the Codat system.
     * @type {string}
     * @memberof AccountingAccount
     */
    currency?: string;
    /**
     * Current balance in the account.
     * @type {string}
     * @memberof AccountingAccount
     */
    currentBalance?: Decimal | number | null;
    /**
     * The type of bank account e.g. Credit.
     * @type {string}
     * @memberof AccountingAccount
     */
    type?: string;
    /**
     * 
     * @type {AccountStatus}
     * @memberof AccountingAccount
     */
    status?: AccountStatus;
    /**
     * Confirms whether the account is a bank account or not.
     * @type {boolean}
     * @memberof AccountingAccount
     */
    isBankAccount?: boolean;
    /**
     * The validDatatypeLinks can be used to determine whether an account can be correctly mapped to another object; for example, accounts with a `type` of `income` might only support being used on an Invoice and Direct Income. For more information, see [Valid Data Type Links](/lending-api#/schemas/ValidDataTypeLinks).
     * @type {Array<ValidDataTypeLinks>}
     * @memberof AccountingAccount
     */
    validDatatypeLinks?: Array<ValidDataTypeLinks> | null;
    /**
     * 
     * @type {SupplementalData}
     * @memberof AccountingAccount
     */
    supplementalData?: SupplementalData;
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingAccount
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingAccount
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the account, unique for the company.
     * @type {string}
     * @memberof AccountingAccount
     */
    id?: string;
    /**
     * 
     * @type {Metadata}
     * @memberof AccountingAccount
     */
    metadata?: Metadata;
}

export interface AccountingAccountsAccountingAccount {
    /**
     * Reference given to each nominal account for a business. It ensures money is allocated to the correct account. This code isn't a unique identifier in the Codat system.
     * @type {string}
     * @memberof AccountingAccount
     */
    nominalCode?: string | null;
    /**
     * Name of the account.
     * @type {string}
     * @memberof AccountingAccount
     */
    name?: string | null;
    /**
     * Description for the account.
     * @type {string}
     * @memberof AccountingAccount
     */
    description?: string | null;
    /**
     * Full category of the account. 
     * 
     * For example, `Liability.Current` or `Income.Revenue`. To determine a list of possible categories for each integration, see our examples, follow our [Create, update, delete data](https://docs.codat.io/using-the-api/push) guide, or refer to the integration's own documentation.
     * @type {string}
     * @memberof AccountingAccount
     */
    fullyQualifiedCategory?: string | null;
    /**
     * Full name of the account, for example:
     * - `Cash On Hand`
     * - `Rents Held In Trust`
     * - `Fixed Asset`
     * @type {string}
     * @memberof AccountingAccount
     */
    fullyQualifiedName?: string | null;
    /**
     * The currency data type in Codat is the [ISO 4217](https://en.wikipedia.org/wiki/ISO_4217) currency code, e.g. _GBP_.
     * 
     * ## Unknown currencies
     * 
     * In line with the ISO 4217 specification, the code _XXX_ is used when the data source does not return a currency for a transaction. 
     * 
     * There are only a very small number of edge cases where this currency code is returned by the Codat system.
     * @type {string}
     * @memberof AccountingAccount
     */
    currency?: string;
    /**
     * Current balance in the account.
     * @type {string}
     * @memberof AccountingAccount
     */
    currentBalance?: Decimal | number | null;
    /**
     * The type of bank account e.g. Credit.
     * @type {string}
     * @memberof AccountingAccount
     */
    type?: string;
    /**
     * 
     * @type {AccountStatus}
     * @memberof AccountingAccount
     */
    status?: AccountStatus;
    /**
     * Confirms whether the account is a bank account or not.
     * @type {boolean}
     * @memberof AccountingAccount
     */
    isBankAccount?: boolean;
    /**
     * The validDatatypeLinks can be used to determine whether an account can be correctly mapped to another object; for example, accounts with a `type` of `income` might only support being used on an Invoice and Direct Income. For more information, see [Valid Data Type Links](/lending-api#/schemas/ValidDataTypeLinks).
     * @type {Array<ValidDataTypeLinks>}
     * @memberof AccountingAccount
     */
    validDatatypeLinks?: Array<ValidDataTypeLinks> | null;
    /**
     * 
     * @type {SupplementalData}
     * @memberof AccountingAccount
     */
    supplementalData?: SupplementalData;
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingAccount
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingAccount
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the account, unique for the company.
     * @type {string}
     * @memberof AccountingAccount
     */
    id?: string;
    /**
     * 
     * @type {Metadata}
     * @memberof AccountingAccount
     */
    metadata?: Metadata;
}

export interface AccountingAccountsListResponseAccountingAccount {
    /**
     * Reference given to each nominal account for a business. It ensures money is allocated to the correct account. This code isn't a unique identifier in the Codat system.
     * @type {string}
     * @memberof AccountingAccount
     */
    nominalCode?: string | null;
    /**
     * Name of the account.
     * @type {string}
     * @memberof AccountingAccount
     */
    name?: string | null;
    /**
     * Description for the account.
     * @type {string}
     * @memberof AccountingAccount
     */
    description?: string | null;
    /**
     * Full category of the account. 
     * 
     * For example, `Liability.Current` or `Income.Revenue`. To determine a list of possible categories for each integration, see our examples, follow our [Create, update, delete data](https://docs.codat.io/using-the-api/push) guide, or refer to the integration's own documentation.
     * @type {string}
     * @memberof AccountingAccount
     */
    fullyQualifiedCategory?: string | null;
    /**
     * Full name of the account, for example:
     * - `Cash On Hand`
     * - `Rents Held In Trust`
     * - `Fixed Asset`
     * @type {string}
     * @memberof AccountingAccount
     */
    fullyQualifiedName?: string | null;
    /**
     * The currency data type in Codat is the [ISO 4217](https://en.wikipedia.org/wiki/ISO_4217) currency code, e.g. _GBP_.
     * 
     * ## Unknown currencies
     * 
     * In line with the ISO 4217 specification, the code _XXX_ is used when the data source does not return a currency for a transaction. 
     * 
     * There are only a very small number of edge cases where this currency code is returned by the Codat system.
     * @type {string}
     * @memberof AccountingAccount
     */
    currency?: string;
    /**
     * Current balance in the account.
     * @type {string}
     * @memberof AccountingAccount
     */
    currentBalance?: Decimal | number | null;
    /**
     * The type of bank account e.g. Credit.
     * @type {string}
     * @memberof AccountingAccount
     */
    type?: string;
    /**
     * 
     * @type {AccountStatus}
     * @memberof AccountingAccount
     */
    status?: AccountStatus;
    /**
     * Confirms whether the account is a bank account or not.
     * @type {boolean}
     * @memberof AccountingAccount
     */
    isBankAccount?: boolean;
    /**
     * The validDatatypeLinks can be used to determine whether an account can be correctly mapped to another object; for example, accounts with a `type` of `income` might only support being used on an Invoice and Direct Income. For more information, see [Valid Data Type Links](/lending-api#/schemas/ValidDataTypeLinks).
     * @type {Array<ValidDataTypeLinks>}
     * @memberof AccountingAccount
     */
    validDatatypeLinks?: Array<ValidDataTypeLinks> | null;
    /**
     * 
     * @type {SupplementalData}
     * @memberof AccountingAccount
     */
    supplementalData?: SupplementalData;
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingAccount
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingAccount
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the account, unique for the company.
     * @type {string}
     * @memberof AccountingAccount
     */
    id?: string;
    /**
     * 
     * @type {Metadata}
     * @memberof AccountingAccount
     */
    metadata?: Metadata;
}

export interface AccountingCreateAccountResponseAccountingAccount {
    /**
     * Reference given to each nominal account for a business. It ensures money is allocated to the correct account. This code isn't a unique identifier in the Codat system.
     * @type {string}
     * @memberof AccountingAccount
     */
    nominalCode?: string | null;
    /**
     * Name of the account.
     * @type {string}
     * @memberof AccountingAccount
     */
    name?: string | null;
    /**
     * Description for the account.
     * @type {string}
     * @memberof AccountingAccount
     */
    description?: string | null;
    /**
     * Full category of the account. 
     * 
     * For example, `Liability.Current` or `Income.Revenue`. To determine a list of possible categories for each integration, see our examples, follow our [Create, update, delete data](https://docs.codat.io/using-the-api/push) guide, or refer to the integration's own documentation.
     * @type {string}
     * @memberof AccountingAccount
     */
    fullyQualifiedCategory?: string | null;
    /**
     * Full name of the account, for example:
     * - `Cash On Hand`
     * - `Rents Held In Trust`
     * - `Fixed Asset`
     * @type {string}
     * @memberof AccountingAccount
     */
    fullyQualifiedName?: string | null;
    /**
     * The currency data type in Codat is the [ISO 4217](https://en.wikipedia.org/wiki/ISO_4217) currency code, e.g. _GBP_.
     * 
     * ## Unknown currencies
     * 
     * In line with the ISO 4217 specification, the code _XXX_ is used when the data source does not return a currency for a transaction. 
     * 
     * There are only a very small number of edge cases where this currency code is returned by the Codat system.
     * @type {string}
     * @memberof AccountingAccount
     */
    currency?: string;
    /**
     * Current balance in the account.
     * @type {string}
     * @memberof AccountingAccount
     */
    currentBalance?: Decimal | number | null;
    /**
     * The type of bank account e.g. Credit.
     * @type {string}
     * @memberof AccountingAccount
     */
    type?: AccountType;
    /**
     * 
     * @type {AccountStatus}
     * @memberof AccountingAccount
     */
    status?: AccountStatus;
    /**
     * Confirms whether the account is a bank account or not.
     * @type {boolean}
     * @memberof AccountingAccount
     */
    isBankAccount?: boolean;
    /**
     * The validDatatypeLinks can be used to determine whether an account can be correctly mapped to another object; for example, accounts with a `type` of `income` might only support being used on an Invoice and Direct Income. For more information, see [Valid Data Type Links](/lending-api#/schemas/ValidDataTypeLinks).
     * @type {Array<ValidDataTypeLinks>}
     * @memberof AccountingAccount
     */
    validDatatypeLinks?: Array<AccountingCreateAccountResponseValidDataTypeLinks> | null;
    /**
     * 
     * @type {SupplementalData}
     * @memberof AccountingAccount
     */
    supplementalData?: SupplementalData;
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingAccount
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingAccount
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the account, unique for the company.
     * @type {string}
     * @memberof AccountingAccount
     */
    id?: string;
    /**
     * 
     * @type {Metadata}
     * @memberof AccountingAccount
     */
    metadata?: Metadata;
}

export interface CreateAccountResponseAccountingAccount {
    /**
     * Reference given to each nominal account for a business. It ensures money is allocated to the correct account. This code isn't a unique identifier in the Codat system.
     * @type {string}
     * @memberof AccountingAccount
     */
    nominalCode?: string | null;
    /**
     * Name of the account.
     * @type {string}
     * @memberof AccountingAccount
     */
    name?: string | null;
    /**
     * Description for the account.
     * @type {string}
     * @memberof AccountingAccount
     */
    description?: string | null;
    /**
     * Full category of the account. 
     * 
     * For example, `Liability.Current` or `Income.Revenue`. To determine a list of possible categories for each integration, see our examples, follow our [Create, update, delete data](https://docs.codat.io/using-the-api/push) guide, or refer to the integration's own documentation.
     * @type {string}
     * @memberof AccountingAccount
     */
    fullyQualifiedCategory?: string | null;
    /**
     * Full name of the account, for example:
     * - `Cash On Hand`
     * - `Rents Held In Trust`
     * - `Fixed Asset`
     * @type {string}
     * @memberof AccountingAccount
     */
    fullyQualifiedName?: string | null;
    /**
     * The currency data type in Codat is the [ISO 4217](https://en.wikipedia.org/wiki/ISO_4217) currency code, e.g. _GBP_.
     * 
     * ## Unknown currencies
     * 
     * In line with the ISO 4217 specification, the code _XXX_ is used when the data source does not return a currency for a transaction. 
     * 
     * There are only a very small number of edge cases where this currency code is returned by the Codat system.
     * @type {string}
     * @memberof AccountingAccount
     */
    currency?: string;
    /**
     * Current balance in the account.
     * @type {string}
     * @memberof AccountingAccount
     */
    currentBalance?: Decimal | number | null;
    /**
     * The type of bank account e.g. Credit.
     * @type {string}
     * @memberof AccountingAccount
     */
    type?: string;
    /**
     * 
     * @type {AccountStatus}
     * @memberof AccountingAccount
     */
    status?: AccountStatus;
    /**
     * Confirms whether the account is a bank account or not.
     * @type {boolean}
     * @memberof AccountingAccount
     */
    isBankAccount?: boolean;
    /**
     * The validDatatypeLinks can be used to determine whether an account can be correctly mapped to another object; for example, accounts with a `type` of `income` might only support being used on an Invoice and Direct Income. For more information, see [Valid Data Type Links](/lending-api#/schemas/ValidDataTypeLinks).
     * @type {Array<ValidDataTypeLinks>}
     * @memberof AccountingAccount
     */
    validDatatypeLinks?: Array<ValidDataTypeLinks> | null;
    /**
     * 
     * @type {SupplementalData}
     * @memberof AccountingAccount
     */
    supplementalData?: SupplementalData;
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingAccount
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingAccount
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the account, unique for the company.
     * @type {string}
     * @memberof AccountingAccount
     */
    id?: string;
    /**
     * 
     * @type {Metadata}
     * @memberof AccountingAccount
     */
    metadata?: Metadata;
}

export interface GetAccountingAccountResponseAccountingAccount {
    /**
     * Reference given to each nominal account for a business. It ensures money is allocated to the correct account. This code isn't a unique identifier in the Codat system.
     * @type {string}
     * @memberof AccountingAccount
     */
    nominalCode?: string | null;
    /**
     * Name of the account.
     * @type {string}
     * @memberof AccountingAccount
     */
    name?: string | null;
    /**
     * Description for the account.
     * @type {string}
     * @memberof AccountingAccount
     */
    description?: string | null;
    /**
     * Full category of the account. 
     * 
     * For example, `Liability.Current` or `Income.Revenue`. To determine a list of possible categories for each integration, see our examples, follow our [Create, update, delete data](https://docs.codat.io/using-the-api/push) guide, or refer to the integration's own documentation.
     * @type {string}
     * @memberof AccountingAccount
     */
    fullyQualifiedCategory?: string | null;
    /**
     * Full name of the account, for example:
     * - `Cash On Hand`
     * - `Rents Held In Trust`
     * - `Fixed Asset`
     * @type {string}
     * @memberof AccountingAccount
     */
    fullyQualifiedName?: string | null;
    /**
     * The currency data type in Codat is the [ISO 4217](https://en.wikipedia.org/wiki/ISO_4217) currency code, e.g. _GBP_.
     * 
     * ## Unknown currencies
     * 
     * In line with the ISO 4217 specification, the code _XXX_ is used when the data source does not return a currency for a transaction. 
     * 
     * There are only a very small number of edge cases where this currency code is returned by the Codat system.
     * @type {string}
     * @memberof AccountingAccount
     */
    currency?: string;
    /**
     * Current balance in the account.
     * @type {string}
     * @memberof AccountingAccount
     */
    currentBalance?: Decimal | number | null;
    /**
     * The type of bank account e.g. Credit.
     * @type {string}
     * @memberof AccountingAccount
     */
    type?: string;
    /**
     * 
     * @type {AccountStatus}
     * @memberof AccountingAccount
     */
    status?: AccountStatus;
    /**
     * Confirms whether the account is a bank account or not.
     * @type {boolean}
     * @memberof AccountingAccount
     */
    isBankAccount?: boolean;
    /**
     * The validDatatypeLinks can be used to determine whether an account can be correctly mapped to another object; for example, accounts with a `type` of `income` might only support being used on an Invoice and Direct Income. For more information, see [Valid Data Type Links](/lending-api#/schemas/ValidDataTypeLinks).
     * @type {Array<ValidDataTypeLinks>}
     * @memberof AccountingAccount
     */
    validDatatypeLinks?: Array<ValidDataTypeLinks> | null;
    /**
     * 
     * @type {SupplementalData}
     * @memberof AccountingAccount
     */
    supplementalData?: SupplementalData;
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingAccount
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingAccount
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the account, unique for the company.
     * @type {string}
     * @memberof AccountingAccount
     */
    id?: string;
    /**
     * 
     * @type {Metadata}
     * @memberof AccountingAccount
     */
    metadata?: Metadata;
}

export interface ListAccountingAccountsResponseAccountingAccount {
    /**
     * Reference given to each nominal account for a business. It ensures money is allocated to the correct account. This code isn't a unique identifier in the Codat system.
     * @type {string}
     * @memberof AccountingAccount
     */
    nominalCode?: string | null;
    /**
     * Name of the account.
     * @type {string}
     * @memberof AccountingAccount
     */
    name?: string | null;
    /**
     * Description for the account.
     * @type {string}
     * @memberof AccountingAccount
     */
    description?: string | null;
    /**
     * Full category of the account. 
     * 
     * For example, `Liability.Current` or `Income.Revenue`. To determine a list of possible categories for each integration, see our examples, follow our [Create, update, delete data](https://docs.codat.io/using-the-api/push) guide, or refer to the integration's own documentation.
     * @type {string}
     * @memberof AccountingAccount
     */
    fullyQualifiedCategory?: string | null;
    /**
     * Full name of the account, for example:
     * - `Cash On Hand`
     * - `Rents Held In Trust`
     * - `Fixed Asset`
     * @type {string}
     * @memberof AccountingAccount
     */
    fullyQualifiedName?: string | null;
    /**
     * The currency data type in Codat is the [ISO 4217](https://en.wikipedia.org/wiki/ISO_4217) currency code, e.g. _GBP_.
     * 
     * ## Unknown currencies
     * 
     * In line with the ISO 4217 specification, the code _XXX_ is used when the data source does not return a currency for a transaction. 
     * 
     * There are only a very small number of edge cases where this currency code is returned by the Codat system.
     * @type {string}
     * @memberof AccountingAccount
     */
    currency?: string;
    /**
     * Current balance in the account.
     * @type {string}
     * @memberof AccountingAccount
     */
    currentBalance?: Decimal | number | null;
    /**
     * The type of bank account e.g. Credit.
     * @type {string}
     * @memberof AccountingAccount
     */
    type?: string;
    /**
     * 
     * @type {AccountStatus}
     * @memberof AccountingAccount
     */
    status?: AccountStatus;
    /**
     * Confirms whether the account is a bank account or not.
     * @type {boolean}
     * @memberof AccountingAccount
     */
    isBankAccount?: boolean;
    /**
     * The validDatatypeLinks can be used to determine whether an account can be correctly mapped to another object; for example, accounts with a `type` of `income` might only support being used on an Invoice and Direct Income. For more information, see [Valid Data Type Links](/lending-api#/schemas/ValidDataTypeLinks).
     * @type {Array<ValidDataTypeLinks>}
     * @memberof AccountingAccount
     */
    validDatatypeLinks?: Array<ValidDataTypeLinks> | null;
    /**
     * 
     * @type {SupplementalData}
     * @memberof AccountingAccount
     */
    supplementalData?: SupplementalData;
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingAccount
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingAccount
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the account, unique for the company.
     * @type {string}
     * @memberof AccountingAccount
     */
    id?: string;
    /**
     * 
     * @type {Metadata}
     * @memberof AccountingAccount
     */
    metadata?: Metadata;
}

/* tslint:disable */
/* eslint-disable */
/**
 * Lending
 * Our Lending solution helps you make smarter credit decisions on small businesses by enabling you to pull your customers\' latest data from accounting, banking, and commerce software they are already using. It also includes features to help providers verify the accuracy of data and process it more efficiently.  The Lending solution is built on top of the latest accounting, commerce, and banking data, providing you with the most important data points you need to get a full picture of SMB creditworthiness and make a comprehensive assessment of your customers.  [Explore solution](https://docs.codat.io/lending/overview) | [See OpenAPI spec](https://github.com/codatio/oas)  <!-- Start Codat Tags Table --> ## Endpoints  | Endpoints | Description | | :- |:- | | Companies | Create and manage your SMB users\' companies. | | Connections | Create new and manage existing data connections for a company. | | Bank statements | Retrieve banking data from linked bank accounts. | | Sales | Retrieve standardized sales data from a linked commerce software. | | Financial statements | Financial data and reports from a linked accounting software. | | Liabilities | Debt and other liabilities. | | Accounts payable | Data from a linked accounting software representing money the business owes money to its suppliers. | | Accounts receivable | Data from a linked accounting software representing money owed to the business for sold goods or services. | | Transactions | Data from a linked accounting software representing transactions. | | Company info | View company information fetched from the source platform. | | Data integrity | Match mutable accounting data with immutable banking data to increase confidence in financial data. | | Excel reports | Download reports in Excel format. | | Manage data | Control how data is retrieved from an integration. | | File upload | Endpoints to manage uploaded files. | | Loan writeback | Implement the [loan writeback](https://docs.codat.io/lending/guides/loan-writeback/introduction) procedure in your lending process to maintain an accurate position of a loan during the entire lending cycle. | <!-- End Codat Tags Table -->
 *
 * The version of the OpenAPI document: 3.0.0
 * Contact: support@codat.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../../../runtime';
import { Decimal, toDecimal, fromDecimal } from '../../../runtime';
import type { BankTransactionType } from './banktransactiontype';
import {
    BankTransactionTypeFromJSON,
    BankTransactionTypeFromJSONTyped,
    BankTransactionTypeToJSON,
    BankTransactionTypeToJSONTyped,
} from './banktransactiontype';

/**
 * > **Accessing Bank Accounts through Banking API**
 * > 
 * > This datatype was originally used for accessing bank account data both in accounting integrations and open banking aggregators. 
 * >
 * > To view bank account data through the Banking API, please refer to the new [Banking: Transaction](https://docs.codat.io/lending-api#/operations/list-transactions) data type.
 * 
 * ## Overview
 * 
 * Transactional banking data for a specific company and account.
 * 
 * Bank transactions include the:
 * * Amount of the transaction.
 * * Current account balance.
 * * Transaction type, for example, credit, debit, or transfer.
 * @export
 * @interface AccountingBankTransaction
 */
export interface AccountingBankTransaction {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the bank transaction, unique to the company in the accounting software.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    id?: string;
    /**
     * Unique identifier to the `accountId` the bank transactions originates from.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    accountId?: string | null;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    clearedOnDate?: string;
    /**
     * Description of the bank transaction.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    description?: string | null;
    /**
     * `True` if the bank transaction has been [reconciled](https://www.xero.com/uk/guides/what-is-bank-reconciliation/) in the accounting software.
     * @type {boolean}
     * @memberof AccountingBankTransaction
     */
    reconciled?: boolean;
    /**
     * The amount transacted in the bank transaction.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    amount?: Decimal | number;
    /**
     * The remaining balance in the account with ID `accountId`.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    balance?: Decimal | number;
    /**
     * 
     * @type {BankTransactionType}
     * @memberof AccountingBankTransaction
     */
    transactionType?: BankTransactionType;
}



/**
 * Check if a given object implements the AccountingBankTransaction interface.
 */
export function instanceOfAccountingBankTransaction(value: object): value is AccountingBankTransaction {
    return true;
}

export function AccountingBankTransactionFromJSON(json: any): AccountingBankTransaction {
    return AccountingBankTransactionFromJSONTyped(json, false);
}

export function AccountingBankTransactionFromJSONTyped(json: any, ignoreDiscriminator: boolean): AccountingBankTransaction {
    if (json == null) {
        return json;
    }
    return {
        
        ...(json['modifiedDate'] !== undefined ? { 'modifiedDate': json['modifiedDate'] == null ? undefined : json['modifiedDate'] } : {}),
        ...(json['sourceModifiedDate'] !== undefined ? { 'sourceModifiedDate': json['sourceModifiedDate'] == null ? undefined : json['sourceModifiedDate'] } : {}),
        ...(json['id'] !== undefined ? { 'id': json['id'] == null ? undefined : json['id'] } : {}),
        ...(json['accountId'] !== undefined ? { 'accountId': json['accountId'] } : {}),
        ...(json['clearedOnDate'] !== undefined ? { 'clearedOnDate': json['clearedOnDate'] == null ? undefined : json['clearedOnDate'] } : {}),
        ...(json['description'] !== undefined ? { 'description': json['description'] } : {}),
        ...(json['reconciled'] !== undefined ? { 'reconciled': json['reconciled'] == null ? undefined : json['reconciled'] } : {}),
        ...(json['amount'] !== undefined ? { 'amount': json['amount'] == null ? undefined : toDecimal(json['amount']) } : {}),
        ...(json['balance'] !== undefined ? { 'balance': json['balance'] == null ? undefined : toDecimal(json['balance']) } : {}),
        ...(json['transactionType'] !== undefined ? { 'transactionType': json['transactionType'] == null ? undefined : BankTransactionTypeFromJSON(json['transactionType']) } : {}),
    };
}

export function AccountingBankTransactionToJSON(json: any): AccountingBankTransaction {
    return AccountingBankTransactionToJSONTyped(json, false);
}

export function AccountingBankTransactionToJSONTyped(value?: AccountingBankTransaction | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'modifiedDate': value['modifiedDate'],
        'sourceModifiedDate': value['sourceModifiedDate'],
        'id': value['id'],
        'accountId': value['accountId'],
        'clearedOnDate': value['clearedOnDate'],
        'description': value['description'],
        'reconciled': value['reconciled'],
        'amount': fromDecimal(value['amount']),
        'balance': fromDecimal(value['balance']),
        'transactionType': BankTransactionTypeToJSON(value['transactionType']),
    };
}


export interface AccountingAccountingBankTransactionsAccountingBankAccountTransaction {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the bank transaction, unique to the company in the accounting software.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    id?: string;
    /**
     * Unique identifier to the `accountId` the bank transactions originates from.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    accountId?: string | null;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    clearedOnDate?: string;
    /**
     * Description of the bank transaction.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    description?: string | null;
    /**
     * `True` if the bank transaction has been [reconciled](https://www.xero.com/uk/guides/what-is-bank-reconciliation/) in the accounting software.
     * @type {boolean}
     * @memberof AccountingBankTransaction
     */
    reconciled?: boolean;
    /**
     * The amount transacted in the bank transaction.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    amount?: Decimal | number;
    /**
     * The remaining balance in the account with ID `accountId`.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    balance?: Decimal | number;
    /**
     * 
     * @type {BankTransactionType}
     * @memberof AccountingBankTransaction
     */
    transactionType?: BankTransactionType;
}

export interface AccountingAccountingBankTransactionsAccountingBankTransaction {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the bank transaction, unique to the company in the accounting software.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    id?: string;
    /**
     * Unique identifier to the `accountId` the bank transactions originates from.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    accountId?: string | null;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    clearedOnDate?: string;
    /**
     * Description of the bank transaction.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    description?: string | null;
    /**
     * `True` if the bank transaction has been [reconciled](https://www.xero.com/uk/guides/what-is-bank-reconciliation/) in the accounting software.
     * @type {boolean}
     * @memberof AccountingBankTransaction
     */
    reconciled?: boolean;
    /**
     * The amount transacted in the bank transaction.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    amount?: Decimal | number;
    /**
     * The remaining balance in the account with ID `accountId`.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    balance?: Decimal | number;
    /**
     * 
     * @type {BankTransactionType}
     * @memberof AccountingBankTransaction
     */
    transactionType?: BankTransactionType;
}

export interface AccountingBankAccountTransaction {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the bank transaction, unique to the company in the accounting software.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    id?: string;
    /**
     * Unique identifier to the `accountId` the bank transactions originates from.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    accountId?: string | null;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    clearedOnDate?: string;
    /**
     * Description of the bank transaction.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    description?: string | null;
    /**
     * `True` if the bank transaction has been [reconciled](https://www.xero.com/uk/guides/what-is-bank-reconciliation/) in the accounting software.
     * @type {boolean}
     * @memberof AccountingBankTransaction
     */
    reconciled?: boolean;
    /**
     * The amount transacted in the bank transaction.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    amount?: Decimal | number;
    /**
     * The remaining balance in the account with ID `accountId`.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    balance?: Decimal | number;
    /**
     * 
     * @type {BankTransactionType}
     * @memberof AccountingBankTransaction
     */
    transactionType?: BankTransactionType;
}

export interface AccountingBankAccountTransactionsListResponseAccountingBankAccountTransaction {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the bank transaction, unique to the company in the accounting software.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    id?: string;
    /**
     * Unique identifier to the `accountId` the bank transactions originates from.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    accountId?: string | null;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    clearedOnDate?: string;
    /**
     * Description of the bank transaction.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    description?: string | null;
    /**
     * `True` if the bank transaction has been [reconciled](https://www.xero.com/uk/guides/what-is-bank-reconciliation/) in the accounting software.
     * @type {boolean}
     * @memberof AccountingBankTransaction
     */
    reconciled?: boolean;
    /**
     * The amount transacted in the bank transaction.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    amount?: Decimal | number;
    /**
     * The remaining balance in the account with ID `accountId`.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    balance?: Decimal | number;
    /**
     * 
     * @type {BankTransactionType}
     * @memberof AccountingBankTransaction
     */
    transactionType?: BankTransactionType;
}

export interface AccountingBankAccountTransactionsListResponseAccountingBankTransaction {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the bank transaction, unique to the company in the accounting software.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    id?: string;
    /**
     * Unique identifier to the `accountId` the bank transactions originates from.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    accountId?: string | null;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    clearedOnDate?: string;
    /**
     * Description of the bank transaction.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    description?: string | null;
    /**
     * `True` if the bank transaction has been [reconciled](https://www.xero.com/uk/guides/what-is-bank-reconciliation/) in the accounting software.
     * @type {boolean}
     * @memberof AccountingBankTransaction
     */
    reconciled?: boolean;
    /**
     * The amount transacted in the bank transaction.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    amount?: Decimal | number;
    /**
     * The remaining balance in the account with ID `accountId`.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    balance?: Decimal | number;
    /**
     * 
     * @type {BankTransactionType}
     * @memberof AccountingBankTransaction
     */
    transactionType?: BankTransactionType;
}

export interface AccountingBankTransactionAccountingBankAccountTransaction {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the bank transaction, unique to the company in the accounting software.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    id?: string;
    /**
     * Unique identifier to the `accountId` the bank transactions originates from.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    accountId?: string | null;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    clearedOnDate?: string;
    /**
     * Description of the bank transaction.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    description?: string | null;
    /**
     * `True` if the bank transaction has been [reconciled](https://www.xero.com/uk/guides/what-is-bank-reconciliation/) in the accounting software.
     * @type {boolean}
     * @memberof AccountingBankTransaction
     */
    reconciled?: boolean;
    /**
     * The amount transacted in the bank transaction.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    amount?: Decimal | number;
    /**
     * The remaining balance in the account with ID `accountId`.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    balance?: Decimal | number;
    /**
     * 
     * @type {BankTransactionType}
     * @memberof AccountingBankTransaction
     */
    transactionType?: BankTransactionType;
}

export interface AccountingBankTransactionsAccountingBankAccountTransaction {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the bank transaction, unique to the company in the accounting software.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    id?: string;
    /**
     * Unique identifier to the `accountId` the bank transactions originates from.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    accountId?: string | null;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    clearedOnDate?: string;
    /**
     * Description of the bank transaction.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    description?: string | null;
    /**
     * `True` if the bank transaction has been [reconciled](https://www.xero.com/uk/guides/what-is-bank-reconciliation/) in the accounting software.
     * @type {boolean}
     * @memberof AccountingBankTransaction
     */
    reconciled?: boolean;
    /**
     * The amount transacted in the bank transaction.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    amount?: Decimal | number;
    /**
     * The remaining balance in the account with ID `accountId`.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    balance?: Decimal | number;
    /**
     * 
     * @type {BankTransactionType}
     * @memberof AccountingBankTransaction
     */
    transactionType?: BankTransactionType;
}

export interface AccountingBankTransactionsAccountingBankTransaction {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the bank transaction, unique to the company in the accounting software.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    id?: string;
    /**
     * Unique identifier to the `accountId` the bank transactions originates from.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    accountId?: string | null;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    clearedOnDate?: string;
    /**
     * Description of the bank transaction.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    description?: string | null;
    /**
     * `True` if the bank transaction has been [reconciled](https://www.xero.com/uk/guides/what-is-bank-reconciliation/) in the accounting software.
     * @type {boolean}
     * @memberof AccountingBankTransaction
     */
    reconciled?: boolean;
    /**
     * The amount transacted in the bank transaction.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    amount?: Decimal | number;
    /**
     * The remaining balance in the account with ID `accountId`.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    balance?: Decimal | number;
    /**
     * 
     * @type {BankTransactionType}
     * @memberof AccountingBankTransaction
     */
    transactionType?: BankTransactionType;
}

export interface ListAccountingBankAccountTransactionsResponseAccountingBankAccountTransaction {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the bank transaction, unique to the company in the accounting software.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    id?: string;
    /**
     * Unique identifier to the `accountId` the bank transactions originates from.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    accountId?: string | null;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    clearedOnDate?: string;
    /**
     * Description of the bank transaction.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    description?: string | null;
    /**
     * `True` if the bank transaction has been [reconciled](https://www.xero.com/uk/guides/what-is-bank-reconciliation/) in the accounting software.
     * @type {boolean}
     * @memberof AccountingBankTransaction
     */
    reconciled?: boolean;
    /**
     * The amount transacted in the bank transaction.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    amount?: Decimal | number;
    /**
     * The remaining balance in the account with ID `accountId`.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    balance?: Decimal | number;
    /**
     * 
     * @type {BankTransactionType}
     * @memberof AccountingBankTransaction
     */
    transactionType?: BankTransactionType;
}

export interface ListAccountingBankAccountTransactionsResponseAccountingBankTransaction {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    sourceModifiedDate?: string;
    /**
     * Identifier for the bank transaction, unique to the company in the accounting software.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    id?: string;
    /**
     * Unique identifier to the `accountId` the bank transactions originates from.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    accountId?: string | null;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    clearedOnDate?: string;
    /**
     * Description of the bank transaction.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    description?: string | null;
    /**
     * `True` if the bank transaction has been [reconciled](https://www.xero.com/uk/guides/what-is-bank-reconciliation/) in the accounting software.
     * @type {boolean}
     * @memberof AccountingBankTransaction
     */
    reconciled?: boolean;
    /**
     * The amount transacted in the bank transaction.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    amount?: Decimal | number;
    /**
     * The remaining balance in the account with ID `accountId`.
     * @type {string}
     * @memberof AccountingBankTransaction
     */
    balance?: Decimal | number;
    /**
     * 
     * @type {BankTransactionType}
     * @memberof AccountingBankTransaction
     */
    transactionType?: BankTransactionType;
}

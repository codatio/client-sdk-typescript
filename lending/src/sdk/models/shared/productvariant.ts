/* tslint:disable */
/* eslint-disable */
/**
 * Lending
 * Our Lending solution helps you make smarter credit decisions on small businesses by enabling you to pull your customers\' latest data from accounting, banking, and commerce software they are already using. It also includes features to help providers verify the accuracy of data and process it more efficiently.  The Lending solution is built on top of the latest accounting, commerce, and banking data, providing you with the most important data points you need to get a full picture of SMB creditworthiness and make a comprehensive assessment of your customers.  [Explore solution](https://docs.codat.io/lending/overview) | [See OpenAPI spec](https://github.com/codatio/oas)  <!-- Start Codat Tags Table --> ## Endpoints  | Endpoints | Description | | :- |:- | | Companies | Create and manage your SMB users\' companies. | | Connections | Create new and manage existing data connections for a company. | | Bank statements | Retrieve banking data from linked bank accounts. | | Sales | Retrieve standardized sales data from a linked commerce software. | | Financial statements | Financial data and reports from a linked accounting software. | | Liabilities | Debt and other liabilities. | | Accounts payable | Data from a linked accounting software representing money the business owes money to its suppliers. | | Accounts receivable | Data from a linked accounting software representing money owed to the business for sold goods or services. | | Transactions | Data from a linked accounting software representing transactions. | | Company info | View company information fetched from the source platform. | | Data integrity | Match mutable accounting data with immutable banking data to increase confidence in financial data. | | Excel reports | Download reports in Excel format. | | Manage data | Control how data is retrieved from an integration. | | File upload | Endpoints to manage uploaded files. | | Loan writeback | Implement the [loan writeback](https://docs.codat.io/lending/guides/loan-writeback/introduction) procedure in your lending process to maintain an accurate position of a loan during the entire lending cycle. | <!-- End Codat Tags Table -->
 *
 * The version of the OpenAPI document: 3.0.0
 * Contact: support@codat.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../../../runtime';
import { Decimal, toDecimal, fromDecimal } from '../../../runtime';
import type { ProductInventory } from './productinventory';
import {
    ProductInventoryFromJSON,
    ProductInventoryFromJSONTyped,
    ProductInventoryToJSON,
    ProductInventoryToJSONTyped,
} from './productinventory';
import type { ProductVariantStatus } from './productvariantstatus';
import {
    ProductVariantStatusFromJSON,
    ProductVariantStatusFromJSONTyped,
    ProductVariantStatusToJSON,
    ProductVariantStatusToJSONTyped,
} from './productvariantstatus';
import type { ProductPrice } from './productprice';
import {
    ProductPriceFromJSON,
    ProductPriceFromJSONTyped,
    ProductPriceToJSON,
    ProductPriceToJSONTyped,
} from './productprice';

/**
 * Represents a variation of a product available for sale, for example an item of clothing that may be available for sale in multiple sizes and colors.
 * 
 * @export
 * @interface ProductVariant
 */
export interface ProductVariant {
    /**
     * A unique, persistent identifier for this record
     * @type {string}
     * @memberof ProductVariant
     */
    id: string;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof ProductVariant
     */
    createdDate?: string;
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof ProductVariant
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof ProductVariant
     */
    sourceModifiedDate?: string;
    /**
     * Name of the product recorded in the commerce or point of sale platform.
     * @type {string}
     * @memberof ProductVariant
     */
    name?: string;
    /**
     * Whether sales taxes are enabled for this product variant.
     * @type {boolean}
     * @memberof ProductVariant
     */
    isTaxEnabled?: boolean;
    /**
     * SKU (stock keeping unit) of the variant, as defined by the merchant.
     * @type {string}
     * @memberof ProductVariant
     */
    sku?: string;
    /**
     * Unique product number of the variant. This might be a barcode, UPC, ISBN, etc.
     * @type {string}
     * @memberof ProductVariant
     */
    barcode?: string;
    /**
     * Unit of measure for the variant, such as `kg` or `meters`.
     * @type {string}
     * @memberof ProductVariant
     */
    unitOfMeasure?: string;
    /**
     * VAT rate for the product variant if sales taxes are enabled.
     * @type {string}
     * @memberof ProductVariant
     */
    vatPercentage?: Decimal | number;
    /**
     * Prices for the product variants in different currencies.
     * @type {Array<ProductPrice>}
     * @memberof ProductVariant
     */
    prices?: Array<ProductPrice>;
    /**
     * 
     * @type {ProductInventory}
     * @memberof ProductVariant
     */
    inventory?: ProductInventory;
    /**
     * Indicates whether or not the product requires physical delivery.
     * @type {boolean}
     * @memberof ProductVariant
     */
    shippingRequired?: boolean;
    /**
     * 
     * @type {ProductVariantStatus}
     * @memberof ProductVariant
     */
    status?: ProductVariantStatus;
}



/**
 * Check if a given object implements the ProductVariant interface.
 */
export function instanceOfProductVariant(value: object): value is ProductVariant {
    if (!('id' in value) || value['id'] === undefined) return false;
    return true;
}

export function ProductVariantFromJSON(json: any): ProductVariant {
    return ProductVariantFromJSONTyped(json, false);
}

export function ProductVariantFromJSONTyped(json: any, ignoreDiscriminator: boolean): ProductVariant {
    if (json == null) {
        return json;
    }
    return {
        
        'id': json['id'],
        ...(json['createdDate'] !== undefined ? { 'createdDate': json['createdDate'] == null ? undefined : json['createdDate'] } : {}),
        ...(json['modifiedDate'] !== undefined ? { 'modifiedDate': json['modifiedDate'] == null ? undefined : json['modifiedDate'] } : {}),
        ...(json['sourceModifiedDate'] !== undefined ? { 'sourceModifiedDate': json['sourceModifiedDate'] == null ? undefined : json['sourceModifiedDate'] } : {}),
        ...(json['name'] !== undefined ? { 'name': json['name'] == null ? undefined : json['name'] } : {}),
        ...(json['isTaxEnabled'] !== undefined ? { 'isTaxEnabled': json['isTaxEnabled'] == null ? undefined : json['isTaxEnabled'] } : {}),
        ...(json['sku'] !== undefined ? { 'sku': json['sku'] == null ? undefined : json['sku'] } : {}),
        ...(json['barcode'] !== undefined ? { 'barcode': json['barcode'] == null ? undefined : json['barcode'] } : {}),
        ...(json['unitOfMeasure'] !== undefined ? { 'unitOfMeasure': json['unitOfMeasure'] == null ? undefined : json['unitOfMeasure'] } : {}),
        ...(json['vatPercentage'] !== undefined ? { 'vatPercentage': json['vatPercentage'] == null ? undefined : toDecimal(json['vatPercentage']) } : {}),
        ...(json['prices'] !== undefined ? { 'prices': json['prices'] == null ? undefined : ((json['prices'] as Array<any>).map(ProductPriceFromJSON)) } : {}),
        ...(json['inventory'] !== undefined ? { 'inventory': json['inventory'] == null ? undefined : ProductInventoryFromJSON(json['inventory']) } : {}),
        ...(json['shippingRequired'] !== undefined ? { 'shippingRequired': json['shippingRequired'] == null ? undefined : json['shippingRequired'] } : {}),
        ...(json['status'] !== undefined ? { 'status': json['status'] == null ? undefined : ProductVariantStatusFromJSON(json['status']) } : {}),
    };
}

export function ProductVariantToJSON(json: any): ProductVariant {
    return ProductVariantToJSONTyped(json, false);
}

export function ProductVariantToJSONTyped(value?: ProductVariant | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'id': value['id'],
        'createdDate': value['createdDate'],
        'modifiedDate': value['modifiedDate'],
        'sourceModifiedDate': value['sourceModifiedDate'],
        'name': value['name'],
        'isTaxEnabled': value['isTaxEnabled'],
        'sku': value['sku'],
        'barcode': value['barcode'],
        'unitOfMeasure': value['unitOfMeasure'],
        'vatPercentage': fromDecimal(value['vatPercentage']),
        'prices': value['prices'] == null ? undefined : ((value['prices'] as Array<any>).map(ProductPriceToJSON)),
        'inventory': ProductInventoryToJSON(value['inventory']),
        'shippingRequired': value['shippingRequired'],
        'status': ProductVariantStatusToJSON(value['status']),
    };
}


export interface CommerceProductGetResponseProductVariant {
    /**
     * A unique, persistent identifier for this record
     * @type {string}
     * @memberof ProductVariant
     */
    id: string;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof ProductVariant
     */
    createdDate?: string;
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof ProductVariant
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof ProductVariant
     */
    sourceModifiedDate?: string;
    /**
     * Name of the product recorded in the commerce or point of sale platform.
     * @type {string}
     * @memberof ProductVariant
     */
    name?: string;
    /**
     * Whether sales taxes are enabled for this product variant.
     * @type {boolean}
     * @memberof ProductVariant
     */
    isTaxEnabled?: boolean;
    /**
     * SKU (stock keeping unit) of the variant, as defined by the merchant.
     * @type {string}
     * @memberof ProductVariant
     */
    sku?: string;
    /**
     * Unique product number of the variant. This might be a barcode, UPC, ISBN, etc.
     * @type {string}
     * @memberof ProductVariant
     */
    barcode?: string;
    /**
     * Unit of measure for the variant, such as `kg` or `meters`.
     * @type {string}
     * @memberof ProductVariant
     */
    unitOfMeasure?: string;
    /**
     * VAT rate for the product variant if sales taxes are enabled.
     * @type {string}
     * @memberof ProductVariant
     */
    vatPercentage?: Decimal | number;
    /**
     * Prices for the product variants in different currencies.
     * @type {Array<ProductPrice>}
     * @memberof ProductVariant
     */
    prices?: Array<ProductPrice>;
    /**
     * 
     * @type {ProductInventory}
     * @memberof ProductVariant
     */
    inventory?: ProductInventory;
    /**
     * Indicates whether or not the product requires physical delivery.
     * @type {boolean}
     * @memberof ProductVariant
     */
    shippingRequired?: boolean;
    /**
     * 
     * @type {ProductVariantStatus}
     * @memberof ProductVariant
     */
    status?: ProductVariantStatus;
}

export interface CommerceProductProductVariant {
    /**
     * A unique, persistent identifier for this record
     * @type {string}
     * @memberof ProductVariant
     */
    id: string;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof ProductVariant
     */
    createdDate?: string;
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof ProductVariant
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof ProductVariant
     */
    sourceModifiedDate?: string;
    /**
     * Name of the product recorded in the commerce or point of sale platform.
     * @type {string}
     * @memberof ProductVariant
     */
    name?: string;
    /**
     * Whether sales taxes are enabled for this product variant.
     * @type {boolean}
     * @memberof ProductVariant
     */
    isTaxEnabled?: boolean;
    /**
     * SKU (stock keeping unit) of the variant, as defined by the merchant.
     * @type {string}
     * @memberof ProductVariant
     */
    sku?: string;
    /**
     * Unique product number of the variant. This might be a barcode, UPC, ISBN, etc.
     * @type {string}
     * @memberof ProductVariant
     */
    barcode?: string;
    /**
     * Unit of measure for the variant, such as `kg` or `meters`.
     * @type {string}
     * @memberof ProductVariant
     */
    unitOfMeasure?: string;
    /**
     * VAT rate for the product variant if sales taxes are enabled.
     * @type {string}
     * @memberof ProductVariant
     */
    vatPercentage?: Decimal | number;
    /**
     * Prices for the product variants in different currencies.
     * @type {Array<ProductPrice>}
     * @memberof ProductVariant
     */
    prices?: Array<ProductPrice>;
    /**
     * 
     * @type {ProductInventory}
     * @memberof ProductVariant
     */
    inventory?: ProductInventory;
    /**
     * Indicates whether or not the product requires physical delivery.
     * @type {boolean}
     * @memberof ProductVariant
     */
    shippingRequired?: boolean;
    /**
     * 
     * @type {ProductVariantStatus}
     * @memberof ProductVariant
     */
    status?: ProductVariantStatus;
}

export interface GetCommerceProductResponseProductVariant {
    /**
     * A unique, persistent identifier for this record
     * @type {string}
     * @memberof ProductVariant
     */
    id: string;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof ProductVariant
     */
    createdDate?: string;
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof ProductVariant
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof ProductVariant
     */
    sourceModifiedDate?: string;
    /**
     * Name of the product recorded in the commerce or point of sale platform.
     * @type {string}
     * @memberof ProductVariant
     */
    name?: string;
    /**
     * Whether sales taxes are enabled for this product variant.
     * @type {boolean}
     * @memberof ProductVariant
     */
    isTaxEnabled?: boolean;
    /**
     * SKU (stock keeping unit) of the variant, as defined by the merchant.
     * @type {string}
     * @memberof ProductVariant
     */
    sku?: string;
    /**
     * Unique product number of the variant. This might be a barcode, UPC, ISBN, etc.
     * @type {string}
     * @memberof ProductVariant
     */
    barcode?: string;
    /**
     * Unit of measure for the variant, such as `kg` or `meters`.
     * @type {string}
     * @memberof ProductVariant
     */
    unitOfMeasure?: string;
    /**
     * VAT rate for the product variant if sales taxes are enabled.
     * @type {string}
     * @memberof ProductVariant
     */
    vatPercentage?: Decimal | number;
    /**
     * Prices for the product variants in different currencies.
     * @type {Array<ProductPrice>}
     * @memberof ProductVariant
     */
    prices?: Array<ProductPrice>;
    /**
     * 
     * @type {ProductInventory}
     * @memberof ProductVariant
     */
    inventory?: ProductInventory;
    /**
     * Indicates whether or not the product requires physical delivery.
     * @type {boolean}
     * @memberof ProductVariant
     */
    shippingRequired?: boolean;
    /**
     * 
     * @type {ProductVariantStatus}
     * @memberof ProductVariant
     */
    status?: ProductVariantStatus;
}

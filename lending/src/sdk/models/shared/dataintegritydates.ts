/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { safeParse } from "../../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Only returned for transactions. For accounts, there is nothing returned.
 */
export type DataIntegrityDates = {
  /**
   * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
   *
   * @remarks
   *
   * ```
   * 2020-10-08T22:40:50Z
   * 2021-01-01T00:00:00
   * ```
   *
   * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
   *
   * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
   * - Unqualified local time: `2021-11-15T01:00:00`
   * - UTC time offsets: `2021-11-15T01:00:00-05:00`
   *
   * > Time zones
   * >
   * > Not all dates from Codat will contain information about time zones.
   * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
   */
  minDate?: string | undefined;
  /**
   * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
   *
   * @remarks
   *
   * ```
   * 2020-10-08T22:40:50Z
   * 2021-01-01T00:00:00
   * ```
   *
   * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
   *
   * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
   * - Unqualified local time: `2021-11-15T01:00:00`
   * - UTC time offsets: `2021-11-15T01:00:00-05:00`
   *
   * > Time zones
   * >
   * > Not all dates from Codat will contain information about time zones.
   * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
   */
  maxDate?: string | undefined;
  /**
   * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
   *
   * @remarks
   *
   * ```
   * 2020-10-08T22:40:50Z
   * 2021-01-01T00:00:00
   * ```
   *
   * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
   *
   * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
   * - Unqualified local time: `2021-11-15T01:00:00`
   * - UTC time offsets: `2021-11-15T01:00:00-05:00`
   *
   * > Time zones
   * >
   * > Not all dates from Codat will contain information about time zones.
   * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
   */
  minOverlappingDate?: string | undefined;
  /**
   * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
   *
   * @remarks
   *
   * ```
   * 2020-10-08T22:40:50Z
   * 2021-01-01T00:00:00
   * ```
   *
   * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
   *
   * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
   * - Unqualified local time: `2021-11-15T01:00:00`
   * - UTC time offsets: `2021-11-15T01:00:00-05:00`
   *
   * > Time zones
   * >
   * > Not all dates from Codat will contain information about time zones.
   * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
   */
  maxOverlappingDate?: string | undefined;
};

/** @internal */
export const DataIntegrityDates$inboundSchema: z.ZodType<
  DataIntegrityDates,
  z.ZodTypeDef,
  unknown
> = z.object({
  minDate: z.string().optional(),
  maxDate: z.string().optional(),
  minOverlappingDate: z.string().optional(),
  maxOverlappingDate: z.string().optional(),
});

/** @internal */
export type DataIntegrityDates$Outbound = {
  minDate?: string | undefined;
  maxDate?: string | undefined;
  minOverlappingDate?: string | undefined;
  maxOverlappingDate?: string | undefined;
};

/** @internal */
export const DataIntegrityDates$outboundSchema: z.ZodType<
  DataIntegrityDates$Outbound,
  z.ZodTypeDef,
  DataIntegrityDates
> = z.object({
  minDate: z.string().optional(),
  maxDate: z.string().optional(),
  minOverlappingDate: z.string().optional(),
  maxOverlappingDate: z.string().optional(),
});

/**
 * @internal
 * @deprecated This namespace will be removed in future versions. Use schemas and types that are exported directly from this module.
 */
export namespace DataIntegrityDates$ {
  /** @deprecated use `DataIntegrityDates$inboundSchema` instead. */
  export const inboundSchema = DataIntegrityDates$inboundSchema;
  /** @deprecated use `DataIntegrityDates$outboundSchema` instead. */
  export const outboundSchema = DataIntegrityDates$outboundSchema;
  /** @deprecated use `DataIntegrityDates$Outbound` instead. */
  export type Outbound = DataIntegrityDates$Outbound;
}

export function dataIntegrityDatesToJSON(
  dataIntegrityDates: DataIntegrityDates,
): string {
  return JSON.stringify(
    DataIntegrityDates$outboundSchema.parse(dataIntegrityDates),
  );
}

export function dataIntegrityDatesFromJSON(
  jsonString: string,
): SafeParseResult<DataIntegrityDates, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DataIntegrityDates$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DataIntegrityDates' from JSON`,
  );
}

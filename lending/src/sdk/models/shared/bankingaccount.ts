/* tslint:disable */
/* eslint-disable */
/**
 * Lending
 * Our Lending solution helps you make smarter credit decisions on small businesses by enabling you to pull your customers\' latest data from accounting, banking, and commerce software they are already using. It also includes features to help providers verify the accuracy of data and process it more efficiently.  The Lending solution is built on top of the latest accounting, commerce, and banking data, providing you with the most important data points you need to get a full picture of SMB creditworthiness and make a comprehensive assessment of your customers.  [Explore solution](https://docs.codat.io/lending/overview) | [See OpenAPI spec](https://github.com/codatio/oas)  <!-- Start Codat Tags Table --> ## Endpoints  | Endpoints | Description | | :- |:- | | Companies | Create and manage your SMB users\' companies. | | Connections | Create new and manage existing data connections for a company. | | Bank statements | Retrieve banking data from linked bank accounts. | | Sales | Retrieve standardized sales data from a linked commerce software. | | Financial statements | Financial data and reports from a linked accounting software. | | Liabilities | Debt and other liabilities. | | Accounts payable | Data from a linked accounting software representing money the business owes money to its suppliers. | | Accounts receivable | Data from a linked accounting software representing money owed to the business for sold goods or services. | | Transactions | Data from a linked accounting software representing transactions. | | Company info | View company information fetched from the source platform. | | Data integrity | Match mutable accounting data with immutable banking data to increase confidence in financial data. | | Excel reports | Download reports in Excel format. | | Manage data | Control how data is retrieved from an integration. | | File upload | Endpoints to manage uploaded files. | | Loan writeback | Implement the [loan writeback](https://docs.codat.io/lending/guides/loan-writeback/introduction) procedure in your lending process to maintain an accurate position of a loan during the entire lending cycle. | <!-- End Codat Tags Table -->
 *
 * The version of the OpenAPI document: 3.0.0
 * Contact: support@codat.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../../../runtime';
import type { AccountBalanceAmounts } from './accountbalanceamounts';
import {
    AccountBalanceAmountsFromJSON,
    AccountBalanceAmountsFromJSONTyped,
    AccountBalanceAmountsToJSON,
    AccountBalanceAmountsToJSONTyped,
} from './accountbalanceamounts';
import type { AccountingBankAccountType } from './accountingbankaccounttype';
import {
    AccountingBankAccountTypeFromJSON,
    AccountingBankAccountTypeFromJSONTyped,
    AccountingBankAccountTypeToJSON,
    AccountingBankAccountTypeToJSONTyped,
} from './accountingbankaccounttype';
import type { AccountInstitution } from './accountinstitution';
import {
    AccountInstitutionFromJSON,
    AccountInstitutionFromJSONTyped,
    AccountInstitutionToJSON,
    AccountInstitutionToJSONTyped,
} from './accountinstitution';
import type { AccountIdentifiers } from './accountidentifiers';
import {
    AccountIdentifiersFromJSON,
    AccountIdentifiersFromJSONTyped,
    AccountIdentifiersToJSON,
    AccountIdentifiersToJSONTyped,
} from './accountidentifiers';

/**
 * This data type provides a list of all the SMB's bank accounts, with rich data like balances, account numbers, and institutions holding the accounts.
 * 
 * Responses are paged, so you should provide `page` and `pageSize` query parameters in your request.
 * @export
 * @interface BankingAccount
 */
export interface BankingAccount {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof BankingAccount
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof BankingAccount
     */
    sourceModifiedDate?: string;
    /**
     * The ID of the account from the provider.
     * @type {string}
     * @memberof BankingAccount
     */
    id: string;
    /**
     * The name of the account according to the provider.
     * @type {string}
     * @memberof BankingAccount
     */
    name: string;
    /**
     * The friendly name of the account, chosen by the holder. This may not have been set by the account holder and therefore is not always available.
     * @type {string}
     * @memberof BankingAccount
     */
    informalName?: string | null;
    /**
     * The name of the person or company who holds the account.
     * @type {string}
     * @memberof BankingAccount
     */
    holder?: string | null;
    /**
     * 
     * @type {AccountingBankAccountType}
     * @memberof BankingAccount
     */
    type: AccountingBankAccountType;
    /**
     * An object containing bank balance data.
     * @type {AccountBalanceAmounts}
     * @memberof BankingAccount
     */
    balance: AccountBalanceAmounts;
    /**
     * 
     * @type {AccountIdentifiers}
     * @memberof BankingAccount
     */
    identifiers: AccountIdentifiers;
    /**
     * The currency code for the account.
     * @type {string}
     * @memberof BankingAccount
     */
    currency: string;
    /**
     * 
     * @type {AccountInstitution}
     * @memberof BankingAccount
     */
    institution: AccountInstitution;
}



/**
 * Check if a given object implements the BankingAccount interface.
 */
export function instanceOfBankingAccount(value: object): value is BankingAccount {
    if (!('id' in value) || value['id'] === undefined) return false;
    if (!('name' in value) || value['name'] === undefined) return false;
    if (!('type' in value) || value['type'] === undefined) return false;
    if (!('balance' in value) || value['balance'] === undefined) return false;
    if (!('identifiers' in value) || value['identifiers'] === undefined) return false;
    if (!('currency' in value) || value['currency'] === undefined) return false;
    if (!('institution' in value) || value['institution'] === undefined) return false;
    return true;
}

export function BankingAccountFromJSON(json: any): BankingAccount {
    return BankingAccountFromJSONTyped(json, false);
}

export function BankingAccountFromJSONTyped(json: any, ignoreDiscriminator: boolean): BankingAccount {
    if (json == null) {
        return json;
    }
    return {
        
        ...(json['modifiedDate'] !== undefined ? { 'modifiedDate': json['modifiedDate'] == null ? undefined : json['modifiedDate'] } : {}),
        ...(json['sourceModifiedDate'] !== undefined ? { 'sourceModifiedDate': json['sourceModifiedDate'] == null ? undefined : json['sourceModifiedDate'] } : {}),
        'id': json['id'],
        'name': json['name'],
        ...(json['informalName'] !== undefined ? { 'informalName': json['informalName'] } : {}),
        ...(json['holder'] !== undefined ? { 'holder': json['holder'] } : {}),
        'type': AccountingBankAccountTypeFromJSON(json['type']),
        'balance': AccountBalanceAmountsFromJSON(json['balance']),
        'identifiers': AccountIdentifiersFromJSON(json['identifiers']),
        'currency': json['currency'],
        'institution': AccountInstitutionFromJSON(json['institution']),
    };
}

export function BankingAccountToJSON(json: any): BankingAccount {
    return BankingAccountToJSONTyped(json, false);
}

export function BankingAccountToJSONTyped(value?: BankingAccount | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'modifiedDate': value['modifiedDate'],
        'sourceModifiedDate': value['sourceModifiedDate'],
        'id': value['id'],
        'name': value['name'],
        'informalName': value['informalName'],
        'holder': value['holder'],
        'type': AccountingBankAccountTypeToJSON(value['type']),
        'balance': AccountBalanceAmountsToJSON(value['balance']),
        'identifiers': AccountIdentifiersToJSON(value['identifiers']),
        'currency': value['currency'],
        'institution': AccountInstitutionToJSON(value['institution']),
    };
}


export interface BankingAccountBankingBankAccount {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof BankingAccount
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof BankingAccount
     */
    sourceModifiedDate?: string;
    /**
     * The ID of the account from the provider.
     * @type {string}
     * @memberof BankingAccount
     */
    id: string;
    /**
     * The name of the account according to the provider.
     * @type {string}
     * @memberof BankingAccount
     */
    name: string;
    /**
     * The friendly name of the account, chosen by the holder. This may not have been set by the account holder and therefore is not always available.
     * @type {string}
     * @memberof BankingAccount
     */
    informalName?: string | null;
    /**
     * The name of the person or company who holds the account.
     * @type {string}
     * @memberof BankingAccount
     */
    holder?: string | null;
    /**
     * 
     * @type {AccountingBankAccountType}
     * @memberof BankingAccount
     */
    type: AccountingBankAccountType;
    /**
     * An object containing bank balance data.
     * @type {AccountBalanceAmounts}
     * @memberof BankingAccount
     */
    balance: AccountBalanceAmounts;
    /**
     * 
     * @type {AccountIdentifiers}
     * @memberof BankingAccount
     */
    identifiers: AccountIdentifiers;
    /**
     * The currency code for the account.
     * @type {string}
     * @memberof BankingAccount
     */
    currency: string;
    /**
     * 
     * @type {AccountInstitution}
     * @memberof BankingAccount
     */
    institution: AccountInstitution;
}

export interface BankingAccountGetResponseBankingAccount {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof BankingAccount
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof BankingAccount
     */
    sourceModifiedDate?: string;
    /**
     * The ID of the account from the provider.
     * @type {string}
     * @memberof BankingAccount
     */
    id: string;
    /**
     * The name of the account according to the provider.
     * @type {string}
     * @memberof BankingAccount
     */
    name: string;
    /**
     * The friendly name of the account, chosen by the holder. This may not have been set by the account holder and therefore is not always available.
     * @type {string}
     * @memberof BankingAccount
     */
    informalName?: string | null;
    /**
     * The name of the person or company who holds the account.
     * @type {string}
     * @memberof BankingAccount
     */
    holder?: string | null;
    /**
     * 
     * @type {AccountingBankAccountType}
     * @memberof BankingAccount
     */
    type: AccountingBankAccountType;
    /**
     * An object containing bank balance data.
     * @type {AccountBalanceAmounts}
     * @memberof BankingAccount
     */
    balance: AccountBalanceAmounts;
    /**
     * 
     * @type {AccountIdentifiers}
     * @memberof BankingAccount
     */
    identifiers: AccountIdentifiers;
    /**
     * The currency code for the account.
     * @type {string}
     * @memberof BankingAccount
     */
    currency: string;
    /**
     * 
     * @type {AccountInstitution}
     * @memberof BankingAccount
     */
    institution: AccountInstitution;
}

export interface BankingAccountGetResponseBankingBankAccount {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof BankingAccount
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof BankingAccount
     */
    sourceModifiedDate?: string;
    /**
     * The ID of the account from the provider.
     * @type {string}
     * @memberof BankingAccount
     */
    id: string;
    /**
     * The name of the account according to the provider.
     * @type {string}
     * @memberof BankingAccount
     */
    name: string;
    /**
     * The friendly name of the account, chosen by the holder. This may not have been set by the account holder and therefore is not always available.
     * @type {string}
     * @memberof BankingAccount
     */
    informalName?: string | null;
    /**
     * The name of the person or company who holds the account.
     * @type {string}
     * @memberof BankingAccount
     */
    holder?: string | null;
    /**
     * 
     * @type {AccountingBankAccountType}
     * @memberof BankingAccount
     */
    type: AccountingBankAccountType;
    /**
     * An object containing bank balance data.
     * @type {AccountBalanceAmounts}
     * @memberof BankingAccount
     */
    balance: AccountBalanceAmounts;
    /**
     * 
     * @type {AccountIdentifiers}
     * @memberof BankingAccount
     */
    identifiers: AccountIdentifiers;
    /**
     * The currency code for the account.
     * @type {string}
     * @memberof BankingAccount
     */
    currency: string;
    /**
     * 
     * @type {AccountInstitution}
     * @memberof BankingAccount
     */
    institution: AccountInstitution;
}

export interface BankingAccountsBankingAccount {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof BankingAccount
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof BankingAccount
     */
    sourceModifiedDate?: string;
    /**
     * The ID of the account from the provider.
     * @type {string}
     * @memberof BankingAccount
     */
    id: string;
    /**
     * The name of the account according to the provider.
     * @type {string}
     * @memberof BankingAccount
     */
    name: string;
    /**
     * The friendly name of the account, chosen by the holder. This may not have been set by the account holder and therefore is not always available.
     * @type {string}
     * @memberof BankingAccount
     */
    informalName?: string | null;
    /**
     * The name of the person or company who holds the account.
     * @type {string}
     * @memberof BankingAccount
     */
    holder?: string | null;
    /**
     * 
     * @type {AccountingBankAccountType}
     * @memberof BankingAccount
     */
    type: AccountingBankAccountType;
    /**
     * An object containing bank balance data.
     * @type {AccountBalanceAmounts}
     * @memberof BankingAccount
     */
    balance: AccountBalanceAmounts;
    /**
     * 
     * @type {AccountIdentifiers}
     * @memberof BankingAccount
     */
    identifiers: AccountIdentifiers;
    /**
     * The currency code for the account.
     * @type {string}
     * @memberof BankingAccount
     */
    currency: string;
    /**
     * 
     * @type {AccountInstitution}
     * @memberof BankingAccount
     */
    institution: AccountInstitution;
}

export interface BankingAccountsBankingBankAccount {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof BankingAccount
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof BankingAccount
     */
    sourceModifiedDate?: string;
    /**
     * The ID of the account from the provider.
     * @type {string}
     * @memberof BankingAccount
     */
    id: string;
    /**
     * The name of the account according to the provider.
     * @type {string}
     * @memberof BankingAccount
     */
    name: string;
    /**
     * The friendly name of the account, chosen by the holder. This may not have been set by the account holder and therefore is not always available.
     * @type {string}
     * @memberof BankingAccount
     */
    informalName?: string | null;
    /**
     * The name of the person or company who holds the account.
     * @type {string}
     * @memberof BankingAccount
     */
    holder?: string | null;
    /**
     * 
     * @type {AccountingBankAccountType}
     * @memberof BankingAccount
     */
    type: AccountingBankAccountType;
    /**
     * An object containing bank balance data.
     * @type {AccountBalanceAmounts}
     * @memberof BankingAccount
     */
    balance: AccountBalanceAmounts;
    /**
     * 
     * @type {AccountIdentifiers}
     * @memberof BankingAccount
     */
    identifiers: AccountIdentifiers;
    /**
     * The currency code for the account.
     * @type {string}
     * @memberof BankingAccount
     */
    currency: string;
    /**
     * 
     * @type {AccountInstitution}
     * @memberof BankingAccount
     */
    institution: AccountInstitution;
}

export interface BankingAccountsListResponseBankingAccount {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof BankingAccount
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof BankingAccount
     */
    sourceModifiedDate?: string;
    /**
     * The ID of the account from the provider.
     * @type {string}
     * @memberof BankingAccount
     */
    id: string;
    /**
     * The name of the account according to the provider.
     * @type {string}
     * @memberof BankingAccount
     */
    name: string;
    /**
     * The friendly name of the account, chosen by the holder. This may not have been set by the account holder and therefore is not always available.
     * @type {string}
     * @memberof BankingAccount
     */
    informalName?: string | null;
    /**
     * The name of the person or company who holds the account.
     * @type {string}
     * @memberof BankingAccount
     */
    holder?: string | null;
    /**
     * 
     * @type {AccountingBankAccountType}
     * @memberof BankingAccount
     */
    type: AccountingBankAccountType;
    /**
     * An object containing bank balance data.
     * @type {AccountBalanceAmounts}
     * @memberof BankingAccount
     */
    balance: AccountBalanceAmounts;
    /**
     * 
     * @type {AccountIdentifiers}
     * @memberof BankingAccount
     */
    identifiers: AccountIdentifiers;
    /**
     * The currency code for the account.
     * @type {string}
     * @memberof BankingAccount
     */
    currency: string;
    /**
     * 
     * @type {AccountInstitution}
     * @memberof BankingAccount
     */
    institution: AccountInstitution;
}

export interface BankingAccountsListResponseBankingBankAccount {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof BankingAccount
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof BankingAccount
     */
    sourceModifiedDate?: string;
    /**
     * The ID of the account from the provider.
     * @type {string}
     * @memberof BankingAccount
     */
    id: string;
    /**
     * The name of the account according to the provider.
     * @type {string}
     * @memberof BankingAccount
     */
    name: string;
    /**
     * The friendly name of the account, chosen by the holder. This may not have been set by the account holder and therefore is not always available.
     * @type {string}
     * @memberof BankingAccount
     */
    informalName?: string | null;
    /**
     * The name of the person or company who holds the account.
     * @type {string}
     * @memberof BankingAccount
     */
    holder?: string | null;
    /**
     * 
     * @type {AccountingBankAccountType}
     * @memberof BankingAccount
     */
    type: AccountingBankAccountType;
    /**
     * An object containing bank balance data.
     * @type {AccountBalanceAmounts}
     * @memberof BankingAccount
     */
    balance: AccountBalanceAmounts;
    /**
     * 
     * @type {AccountIdentifiers}
     * @memberof BankingAccount
     */
    identifiers: AccountIdentifiers;
    /**
     * The currency code for the account.
     * @type {string}
     * @memberof BankingAccount
     */
    currency: string;
    /**
     * 
     * @type {AccountInstitution}
     * @memberof BankingAccount
     */
    institution: AccountInstitution;
}

export interface BankingBankAccount {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof BankingAccount
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof BankingAccount
     */
    sourceModifiedDate?: string;
    /**
     * The ID of the account from the provider.
     * @type {string}
     * @memberof BankingAccount
     */
    id: string;
    /**
     * The name of the account according to the provider.
     * @type {string}
     * @memberof BankingAccount
     */
    name: string;
    /**
     * The friendly name of the account, chosen by the holder. This may not have been set by the account holder and therefore is not always available.
     * @type {string}
     * @memberof BankingAccount
     */
    informalName?: string | null;
    /**
     * The name of the person or company who holds the account.
     * @type {string}
     * @memberof BankingAccount
     */
    holder?: string | null;
    /**
     * 
     * @type {AccountingBankAccountType}
     * @memberof BankingAccount
     */
    type: AccountingBankAccountType;
    /**
     * An object containing bank balance data.
     * @type {AccountBalanceAmounts}
     * @memberof BankingAccount
     */
    balance: AccountBalanceAmounts;
    /**
     * 
     * @type {AccountIdentifiers}
     * @memberof BankingAccount
     */
    identifiers: AccountIdentifiers;
    /**
     * The currency code for the account.
     * @type {string}
     * @memberof BankingAccount
     */
    currency: string;
    /**
     * 
     * @type {AccountInstitution}
     * @memberof BankingAccount
     */
    institution: AccountInstitution;
}

export interface BankingBankAccountsBankingAccount {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof BankingAccount
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof BankingAccount
     */
    sourceModifiedDate?: string;
    /**
     * The ID of the account from the provider.
     * @type {string}
     * @memberof BankingAccount
     */
    id: string;
    /**
     * The name of the account according to the provider.
     * @type {string}
     * @memberof BankingAccount
     */
    name: string;
    /**
     * The friendly name of the account, chosen by the holder. This may not have been set by the account holder and therefore is not always available.
     * @type {string}
     * @memberof BankingAccount
     */
    informalName?: string | null;
    /**
     * The name of the person or company who holds the account.
     * @type {string}
     * @memberof BankingAccount
     */
    holder?: string | null;
    /**
     * 
     * @type {AccountingBankAccountType}
     * @memberof BankingAccount
     */
    type: AccountingBankAccountType;
    /**
     * An object containing bank balance data.
     * @type {AccountBalanceAmounts}
     * @memberof BankingAccount
     */
    balance: AccountBalanceAmounts;
    /**
     * 
     * @type {AccountIdentifiers}
     * @memberof BankingAccount
     */
    identifiers: AccountIdentifiers;
    /**
     * The currency code for the account.
     * @type {string}
     * @memberof BankingAccount
     */
    currency: string;
    /**
     * 
     * @type {AccountInstitution}
     * @memberof BankingAccount
     */
    institution: AccountInstitution;
}

export interface BankingBankAccountsBankingBankAccount {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof BankingAccount
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof BankingAccount
     */
    sourceModifiedDate?: string;
    /**
     * The ID of the account from the provider.
     * @type {string}
     * @memberof BankingAccount
     */
    id: string;
    /**
     * The name of the account according to the provider.
     * @type {string}
     * @memberof BankingAccount
     */
    name: string;
    /**
     * The friendly name of the account, chosen by the holder. This may not have been set by the account holder and therefore is not always available.
     * @type {string}
     * @memberof BankingAccount
     */
    informalName?: string | null;
    /**
     * The name of the person or company who holds the account.
     * @type {string}
     * @memberof BankingAccount
     */
    holder?: string | null;
    /**
     * 
     * @type {AccountingBankAccountType}
     * @memberof BankingAccount
     */
    type: AccountingBankAccountType;
    /**
     * An object containing bank balance data.
     * @type {AccountBalanceAmounts}
     * @memberof BankingAccount
     */
    balance: AccountBalanceAmounts;
    /**
     * 
     * @type {AccountIdentifiers}
     * @memberof BankingAccount
     */
    identifiers: AccountIdentifiers;
    /**
     * The currency code for the account.
     * @type {string}
     * @memberof BankingAccount
     */
    currency: string;
    /**
     * 
     * @type {AccountInstitution}
     * @memberof BankingAccount
     */
    institution: AccountInstitution;
}

export interface GetBankingAccountResponseBankingAccount {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof BankingAccount
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof BankingAccount
     */
    sourceModifiedDate?: string;
    /**
     * The ID of the account from the provider.
     * @type {string}
     * @memberof BankingAccount
     */
    id: string;
    /**
     * The name of the account according to the provider.
     * @type {string}
     * @memberof BankingAccount
     */
    name: string;
    /**
     * The friendly name of the account, chosen by the holder. This may not have been set by the account holder and therefore is not always available.
     * @type {string}
     * @memberof BankingAccount
     */
    informalName?: string | null;
    /**
     * The name of the person or company who holds the account.
     * @type {string}
     * @memberof BankingAccount
     */
    holder?: string | null;
    /**
     * 
     * @type {AccountingBankAccountType}
     * @memberof BankingAccount
     */
    type: AccountingBankAccountType;
    /**
     * An object containing bank balance data.
     * @type {AccountBalanceAmounts}
     * @memberof BankingAccount
     */
    balance: AccountBalanceAmounts;
    /**
     * 
     * @type {AccountIdentifiers}
     * @memberof BankingAccount
     */
    identifiers: AccountIdentifiers;
    /**
     * The currency code for the account.
     * @type {string}
     * @memberof BankingAccount
     */
    currency: string;
    /**
     * 
     * @type {AccountInstitution}
     * @memberof BankingAccount
     */
    institution: AccountInstitution;
}

export interface GetBankingAccountResponseBankingBankAccount {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof BankingAccount
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof BankingAccount
     */
    sourceModifiedDate?: string;
    /**
     * The ID of the account from the provider.
     * @type {string}
     * @memberof BankingAccount
     */
    id: string;
    /**
     * The name of the account according to the provider.
     * @type {string}
     * @memberof BankingAccount
     */
    name: string;
    /**
     * The friendly name of the account, chosen by the holder. This may not have been set by the account holder and therefore is not always available.
     * @type {string}
     * @memberof BankingAccount
     */
    informalName?: string | null;
    /**
     * The name of the person or company who holds the account.
     * @type {string}
     * @memberof BankingAccount
     */
    holder?: string | null;
    /**
     * 
     * @type {AccountingBankAccountType}
     * @memberof BankingAccount
     */
    type: AccountingBankAccountType;
    /**
     * An object containing bank balance data.
     * @type {AccountBalanceAmounts}
     * @memberof BankingAccount
     */
    balance: AccountBalanceAmounts;
    /**
     * 
     * @type {AccountIdentifiers}
     * @memberof BankingAccount
     */
    identifiers: AccountIdentifiers;
    /**
     * The currency code for the account.
     * @type {string}
     * @memberof BankingAccount
     */
    currency: string;
    /**
     * 
     * @type {AccountInstitution}
     * @memberof BankingAccount
     */
    institution: AccountInstitution;
}

export interface ListBankingAccountsResponseBankingAccount {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof BankingAccount
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof BankingAccount
     */
    sourceModifiedDate?: string;
    /**
     * The ID of the account from the provider.
     * @type {string}
     * @memberof BankingAccount
     */
    id: string;
    /**
     * The name of the account according to the provider.
     * @type {string}
     * @memberof BankingAccount
     */
    name: string;
    /**
     * The friendly name of the account, chosen by the holder. This may not have been set by the account holder and therefore is not always available.
     * @type {string}
     * @memberof BankingAccount
     */
    informalName?: string | null;
    /**
     * The name of the person or company who holds the account.
     * @type {string}
     * @memberof BankingAccount
     */
    holder?: string | null;
    /**
     * 
     * @type {AccountingBankAccountType}
     * @memberof BankingAccount
     */
    type: AccountingBankAccountType;
    /**
     * An object containing bank balance data.
     * @type {AccountBalanceAmounts}
     * @memberof BankingAccount
     */
    balance: AccountBalanceAmounts;
    /**
     * 
     * @type {AccountIdentifiers}
     * @memberof BankingAccount
     */
    identifiers: AccountIdentifiers;
    /**
     * The currency code for the account.
     * @type {string}
     * @memberof BankingAccount
     */
    currency: string;
    /**
     * 
     * @type {AccountInstitution}
     * @memberof BankingAccount
     */
    institution: AccountInstitution;
}

export interface ListBankingAccountsResponseBankingBankAccount {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof BankingAccount
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof BankingAccount
     */
    sourceModifiedDate?: string;
    /**
     * The ID of the account from the provider.
     * @type {string}
     * @memberof BankingAccount
     */
    id: string;
    /**
     * The name of the account according to the provider.
     * @type {string}
     * @memberof BankingAccount
     */
    name: string;
    /**
     * The friendly name of the account, chosen by the holder. This may not have been set by the account holder and therefore is not always available.
     * @type {string}
     * @memberof BankingAccount
     */
    informalName?: string | null;
    /**
     * The name of the person or company who holds the account.
     * @type {string}
     * @memberof BankingAccount
     */
    holder?: string | null;
    /**
     * 
     * @type {AccountingBankAccountType}
     * @memberof BankingAccount
     */
    type: AccountingBankAccountType;
    /**
     * An object containing bank balance data.
     * @type {AccountBalanceAmounts}
     * @memberof BankingAccount
     */
    balance: AccountBalanceAmounts;
    /**
     * 
     * @type {AccountIdentifiers}
     * @memberof BankingAccount
     */
    identifiers: AccountIdentifiers;
    /**
     * The currency code for the account.
     * @type {string}
     * @memberof BankingAccount
     */
    currency: string;
    /**
     * 
     * @type {AccountInstitution}
     * @memberof BankingAccount
     */
    institution: AccountInstitution;
}

export interface UploadBankStatementDataRequestBodyBankingAccount {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof BankingAccount
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof BankingAccount
     */
    sourceModifiedDate?: string;
    /**
     * The ID of the account from the provider.
     * @type {string}
     * @memberof BankingAccount
     */
    id: string;
    /**
     * The name of the account according to the provider.
     * @type {string}
     * @memberof BankingAccount
     */
    name: string;
    /**
     * The friendly name of the account, chosen by the holder. This may not have been set by the account holder and therefore is not always available.
     * @type {string}
     * @memberof BankingAccount
     */
    informalName?: string | null;
    /**
     * The name of the person or company who holds the account.
     * @type {string}
     * @memberof BankingAccount
     */
    holder?: string | null;
    /**
     * 
     * @type {AccountingBankAccountType}
     * @memberof BankingAccount
     */
    type: AccountingBankAccountType;
    /**
     * An object containing bank balance data.
     * @type {AccountBalanceAmounts}
     * @memberof BankingAccount
     */
    balance: AccountBalanceAmounts;
    /**
     * 
     * @type {AccountIdentifiers}
     * @memberof BankingAccount
     */
    identifiers: AccountIdentifiers;
    /**
     * The currency code for the account.
     * @type {string}
     * @memberof BankingAccount
     */
    currency: string;
    /**
     * 
     * @type {AccountInstitution}
     * @memberof BankingAccount
     */
    institution: AccountInstitution;
}

export interface UploadBankStatementDataRequestBodyBankingBankAccount {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof BankingAccount
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof BankingAccount
     */
    sourceModifiedDate?: string;
    /**
     * The ID of the account from the provider.
     * @type {string}
     * @memberof BankingAccount
     */
    id: string;
    /**
     * The name of the account according to the provider.
     * @type {string}
     * @memberof BankingAccount
     */
    name: string;
    /**
     * The friendly name of the account, chosen by the holder. This may not have been set by the account holder and therefore is not always available.
     * @type {string}
     * @memberof BankingAccount
     */
    informalName?: string | null;
    /**
     * The name of the person or company who holds the account.
     * @type {string}
     * @memberof BankingAccount
     */
    holder?: string | null;
    /**
     * 
     * @type {AccountingBankAccountType}
     * @memberof BankingAccount
     */
    type: AccountingBankAccountType;
    /**
     * An object containing bank balance data.
     * @type {AccountBalanceAmounts}
     * @memberof BankingAccount
     */
    balance: AccountBalanceAmounts;
    /**
     * 
     * @type {AccountIdentifiers}
     * @memberof BankingAccount
     */
    identifiers: AccountIdentifiers;
    /**
     * The currency code for the account.
     * @type {string}
     * @memberof BankingAccount
     */
    currency: string;
    /**
     * 
     * @type {AccountInstitution}
     * @memberof BankingAccount
     */
    institution: AccountInstitution;
}

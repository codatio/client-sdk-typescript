/* tslint:disable */
/* eslint-disable */
/**
 * Lending
 * Our Lending solution helps you make smarter credit decisions on small businesses by enabling you to pull your customers\' latest data from accounting, banking, and commerce software they are already using. It also includes features to help providers verify the accuracy of data and process it more efficiently.  The Lending solution is built on top of the latest accounting, commerce, and banking data, providing you with the most important data points you need to get a full picture of SMB creditworthiness and make a comprehensive assessment of your customers.  [Explore solution](https://docs.codat.io/lending/overview) | [See OpenAPI spec](https://github.com/codatio/oas)  <!-- Start Codat Tags Table --> ## Endpoints  | Endpoints | Description | | :- |:- | | Companies | Create and manage your SMB users\' companies. | | Connections | Create new and manage existing data connections for a company. | | Bank statements | Retrieve banking data from linked bank accounts. | | Sales | Retrieve standardized sales data from a linked commerce software. | | Financial statements | Financial data and reports from a linked accounting software. | | Liabilities | Debt and other liabilities. | | Accounts payable | Data from a linked accounting software representing money the business owes money to its suppliers. | | Accounts receivable | Data from a linked accounting software representing money owed to the business for sold goods or services. | | Transactions | Data from a linked accounting software representing transactions. | | Company info | View company information fetched from the source platform. | | Data integrity | Match mutable accounting data with immutable banking data to increase confidence in financial data. | | Excel reports | Download reports in Excel format. | | Manage data | Control how data is retrieved from an integration. | | File upload | Endpoints to manage uploaded files. | | Loan writeback | Implement the [loan writeback](https://docs.codat.io/lending/guides/loan-writeback/introduction) procedure in your lending process to maintain an accurate position of a loan during the entire lending cycle. | <!-- End Codat Tags Table -->
 *
 * The version of the OpenAPI document: 3.0.0
 * Contact: support@codat.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import type { DatasetStatus } from '../_speakeasy_enums';
import { mapValues } from '../../../runtime';
/**
 * Information about a queued, in progress or completed pull operation.
 * *Formally called `dataset`*
 * @export
 * @interface PullOperation
 */
export interface PullOperation {
    /**
     * Unique identifier of the pull operation.
     * @type {string}
     * @memberof PullOperation
     */
    id: string;
    /**
     * Unique identifier of the company associated to this pull operation.
     * @type {string}
     * @memberof PullOperation
     */
    companyId: string;
    /**
     * Unique identifier of the connection associated to this pull operation.
     * @type {string}
     * @memberof PullOperation
     */
    connectionId: string;
    /**
     * The data type you are requesting in a pull operation.
     * @type {string}
     * @memberof PullOperation
     */
    dataType: string;
    /**
     * The current status of the dataset.
     * @type {string}
     * @memberof PullOperation
     */
    status: DatasetStatus;
    /**
     * Additional information about the dataset status.
     * @type {string}
     * @memberof PullOperation
     */
    statusDescription?: string | null;
    /**
     * A message about a transient or persistent error returned by Codat or the source platform.
     * @type {string}
     * @memberof PullOperation
     */
    errorMessage?: string | null;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof PullOperation
     */
    requested: string;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof PullOperation
     */
    completed?: string;
    /**
     * An integer signifying the progress of the pull operation.
     * @type {number}
     * @memberof PullOperation
     */
    progress: number;
    /**
     * `True` if the pull operation is completed successfully. The `isCompleted` property is not queryable. To filter failed pull operations, query by `status!=Complete&&status!=NotSupported` instead.
     * @type {boolean}
     * @memberof PullOperation
     */
    isCompleted: boolean;
    /**
     * `True` if the pull operation entered an error state.
     * @type {boolean}
     * @memberof PullOperation
     */
    isErrored: boolean;
}


/**
 * @export
 */
export const PullOperationStatusEnum = {
    Initial: 'Initial',
    Queued: 'Queued',
    Fetching: 'Fetching',
    MapQueued: 'MapQueued',
    Mapping: 'Mapping',
    Complete: 'Complete',
    FetchError: 'FetchError',
    MapError: 'MapError',
    InternalError: 'InternalError',
    ProcessingQueued: 'ProcessingQueued',
    Processing: 'Processing',
    ProcessingError: 'ProcessingError',
    ValidationQueued: 'ValidationQueued',
    Validating: 'Validating',
    ValidationError: 'ValidationError',
    AuthError: 'AuthError',
    Cancelled: 'Cancelled',
    NotSupported: 'NotSupported',
    RateLimitError: 'RateLimitError',
    PermissionsError: 'PermissionsError',
    PrerequisiteNotMet: 'PrerequisiteNotMet'
} as const;
export type PullOperationStatusEnum = typeof PullOperationStatusEnum[keyof typeof PullOperationStatusEnum];


/**
 * Check if a given object implements the PullOperation interface.
 */
export function instanceOfPullOperation(value: object): value is PullOperation {
    if (!('id' in value) || value['id'] === undefined) return false;
    if (!('companyId' in value) || value['companyId'] === undefined) return false;
    if (!('connectionId' in value) || value['connectionId'] === undefined) return false;
    if (!('dataType' in value) || value['dataType'] === undefined) return false;
    if (!('status' in value) || value['status'] === undefined) return false;
    if (!('requested' in value) || value['requested'] === undefined) return false;
    if (!('progress' in value) || value['progress'] === undefined) return false;
    if (!('isCompleted' in value) || value['isCompleted'] === undefined) return false;
    if (!('isErrored' in value) || value['isErrored'] === undefined) return false;
    return true;
}

export function PullOperationFromJSON(json: any): PullOperation {
    return PullOperationFromJSONTyped(json, false);
}

export function PullOperationFromJSONTyped(json: any, ignoreDiscriminator: boolean): PullOperation {
    if (json == null) {
        return json;
    }
    return {
        
        'id': json['id'],
        'companyId': json['companyId'],
        'connectionId': json['connectionId'],
        'dataType': json['dataType'],
        'status': json['status'],
        ...(json['statusDescription'] !== undefined ? { 'statusDescription': json['statusDescription'] } : {}),
        ...(json['errorMessage'] !== undefined ? { 'errorMessage': json['errorMessage'] } : {}),
        'requested': json['requested'],
        ...(json['completed'] !== undefined ? { 'completed': json['completed'] == null ? undefined : json['completed'] } : {}),
        'progress': json['progress'],
        'isCompleted': json['isCompleted'],
        'isErrored': json['isErrored'],
    };
}

export function PullOperationToJSON(json: any): PullOperation {
    return PullOperationToJSONTyped(json, false);
}

export function PullOperationToJSONTyped(value?: PullOperation | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'id': value['id'],
        'companyId': value['companyId'],
        'connectionId': value['connectionId'],
        'dataType': value['dataType'],
        'status': value['status'],
        'statusDescription': value['statusDescription'],
        'errorMessage': value['errorMessage'],
        'requested': value['requested'],
        'completed': value['completed'],
        'progress': value['progress'],
        'isCompleted': value['isCompleted'],
        'isErrored': value['isErrored'],
    };
}


export interface BankStatementUploadSessionStartResponsePullOperation {
    /**
     * Unique identifier of the pull operation.
     * @type {string}
     * @memberof PullOperation
     */
    id: string;
    /**
     * Unique identifier of the company associated to this pull operation.
     * @type {string}
     * @memberof PullOperation
     */
    companyId: string;
    /**
     * Unique identifier of the connection associated to this pull operation.
     * @type {string}
     * @memberof PullOperation
     */
    connectionId: string;
    /**
     * The data type you are requesting in a pull operation.
     * @type {string}
     * @memberof PullOperation
     */
    dataType: string;
    /**
     * The current status of the dataset.
     * @type {string}
     * @memberof PullOperation
     */
    status: DatasetStatus;
    /**
     * Additional information about the dataset status.
     * @type {string}
     * @memberof PullOperation
     */
    statusDescription?: string | null;
    /**
     * A message about a transient or persistent error returned by Codat or the source platform.
     * @type {string}
     * @memberof PullOperation
     */
    errorMessage?: string | null;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof PullOperation
     */
    requested: string;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof PullOperation
     */
    completed?: string;
    /**
     * An integer signifying the progress of the pull operation.
     * @type {number}
     * @memberof PullOperation
     */
    progress: number;
    /**
     * `True` if the pull operation is completed successfully. The `isCompleted` property is not queryable. To filter failed pull operations, query by `status!=Complete&&status!=NotSupported` instead.
     * @type {boolean}
     * @memberof PullOperation
     */
    isCompleted: boolean;
    /**
     * `True` if the pull operation entered an error state.
     * @type {boolean}
     * @memberof PullOperation
     */
    isErrored: boolean;
}

export interface DataTypeRefreshResponsePullOperation {
    /**
     * Unique identifier of the pull operation.
     * @type {string}
     * @memberof PullOperation
     */
    id: string;
    /**
     * Unique identifier of the company associated to this pull operation.
     * @type {string}
     * @memberof PullOperation
     */
    companyId: string;
    /**
     * Unique identifier of the connection associated to this pull operation.
     * @type {string}
     * @memberof PullOperation
     */
    connectionId: string;
    /**
     * The data type you are requesting in a pull operation.
     * @type {string}
     * @memberof PullOperation
     */
    dataType: string;
    /**
     * The current status of the dataset.
     * @type {string}
     * @memberof PullOperation
     */
    status: DatasetStatus;
    /**
     * Additional information about the dataset status.
     * @type {string}
     * @memberof PullOperation
     */
    statusDescription?: string | null;
    /**
     * A message about a transient or persistent error returned by Codat or the source platform.
     * @type {string}
     * @memberof PullOperation
     */
    errorMessage?: string | null;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof PullOperation
     */
    requested: string;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof PullOperation
     */
    completed?: string;
    /**
     * An integer signifying the progress of the pull operation.
     * @type {number}
     * @memberof PullOperation
     */
    progress: number;
    /**
     * `True` if the pull operation is completed successfully. The `isCompleted` property is not queryable. To filter failed pull operations, query by `status!=Complete&&status!=NotSupported` instead.
     * @type {boolean}
     * @memberof PullOperation
     */
    isCompleted: boolean;
    /**
     * `True` if the pull operation entered an error state.
     * @type {boolean}
     * @memberof PullOperation
     */
    isErrored: boolean;
}

export interface GetPullOperationResponsePullOperation {
    /**
     * Unique identifier of the pull operation.
     * @type {string}
     * @memberof PullOperation
     */
    id: string;
    /**
     * Unique identifier of the company associated to this pull operation.
     * @type {string}
     * @memberof PullOperation
     */
    companyId: string;
    /**
     * Unique identifier of the connection associated to this pull operation.
     * @type {string}
     * @memberof PullOperation
     */
    connectionId: string;
    /**
     * The data type you are requesting in a pull operation.
     * @type {string}
     * @memberof PullOperation
     */
    dataType: string;
    /**
     * The current status of the dataset.
     * @type {string}
     * @memberof PullOperation
     */
    status: DatasetStatus;
    /**
     * Additional information about the dataset status.
     * @type {string}
     * @memberof PullOperation
     */
    statusDescription?: string | null;
    /**
     * A message about a transient or persistent error returned by Codat or the source platform.
     * @type {string}
     * @memberof PullOperation
     */
    errorMessage?: string | null;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof PullOperation
     */
    requested: string;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof PullOperation
     */
    completed?: string;
    /**
     * An integer signifying the progress of the pull operation.
     * @type {number}
     * @memberof PullOperation
     */
    progress: number;
    /**
     * `True` if the pull operation is completed successfully. The `isCompleted` property is not queryable. To filter failed pull operations, query by `status!=Complete&&status!=NotSupported` instead.
     * @type {boolean}
     * @memberof PullOperation
     */
    isCompleted: boolean;
    /**
     * `True` if the pull operation entered an error state.
     * @type {boolean}
     * @memberof PullOperation
     */
    isErrored: boolean;
}

export interface ListPullOperationsResponsePullOperation {
    /**
     * Unique identifier of the pull operation.
     * @type {string}
     * @memberof PullOperation
     */
    id: string;
    /**
     * Unique identifier of the company associated to this pull operation.
     * @type {string}
     * @memberof PullOperation
     */
    companyId: string;
    /**
     * Unique identifier of the connection associated to this pull operation.
     * @type {string}
     * @memberof PullOperation
     */
    connectionId: string;
    /**
     * The data type you are requesting in a pull operation.
     * @type {string}
     * @memberof PullOperation
     */
    dataType: string;
    /**
     * The current status of the dataset.
     * @type {string}
     * @memberof PullOperation
     */
    status: DatasetStatus;
    /**
     * Additional information about the dataset status.
     * @type {string}
     * @memberof PullOperation
     */
    statusDescription?: string | null;
    /**
     * A message about a transient or persistent error returned by Codat or the source platform.
     * @type {string}
     * @memberof PullOperation
     */
    errorMessage?: string | null;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof PullOperation
     */
    requested: string;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof PullOperation
     */
    completed?: string;
    /**
     * An integer signifying the progress of the pull operation.
     * @type {number}
     * @memberof PullOperation
     */
    progress: number;
    /**
     * `True` if the pull operation is completed successfully. The `isCompleted` property is not queryable. To filter failed pull operations, query by `status!=Complete&&status!=NotSupported` instead.
     * @type {boolean}
     * @memberof PullOperation
     */
    isCompleted: boolean;
    /**
     * `True` if the pull operation entered an error state.
     * @type {boolean}
     * @memberof PullOperation
     */
    isErrored: boolean;
}

export interface PullOperationGetResponsePullOperation {
    /**
     * Unique identifier of the pull operation.
     * @type {string}
     * @memberof PullOperation
     */
    id: string;
    /**
     * Unique identifier of the company associated to this pull operation.
     * @type {string}
     * @memberof PullOperation
     */
    companyId: string;
    /**
     * Unique identifier of the connection associated to this pull operation.
     * @type {string}
     * @memberof PullOperation
     */
    connectionId: string;
    /**
     * The data type you are requesting in a pull operation.
     * @type {string}
     * @memberof PullOperation
     */
    dataType: string;
    /**
     * The current status of the dataset.
     * @type {string}
     * @memberof PullOperation
     */
    status: DatasetStatus;
    /**
     * Additional information about the dataset status.
     * @type {string}
     * @memberof PullOperation
     */
    statusDescription?: string | null;
    /**
     * A message about a transient or persistent error returned by Codat or the source platform.
     * @type {string}
     * @memberof PullOperation
     */
    errorMessage?: string | null;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof PullOperation
     */
    requested: string;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof PullOperation
     */
    completed?: string;
    /**
     * An integer signifying the progress of the pull operation.
     * @type {number}
     * @memberof PullOperation
     */
    progress: number;
    /**
     * `True` if the pull operation is completed successfully. The `isCompleted` property is not queryable. To filter failed pull operations, query by `status!=Complete&&status!=NotSupported` instead.
     * @type {boolean}
     * @memberof PullOperation
     */
    isCompleted: boolean;
    /**
     * `True` if the pull operation entered an error state.
     * @type {boolean}
     * @memberof PullOperation
     */
    isErrored: boolean;
}

export interface PullOperationsListResponsePullOperation {
    /**
     * Unique identifier of the pull operation.
     * @type {string}
     * @memberof PullOperation
     */
    id: string;
    /**
     * Unique identifier of the company associated to this pull operation.
     * @type {string}
     * @memberof PullOperation
     */
    companyId: string;
    /**
     * Unique identifier of the connection associated to this pull operation.
     * @type {string}
     * @memberof PullOperation
     */
    connectionId: string;
    /**
     * The data type you are requesting in a pull operation.
     * @type {string}
     * @memberof PullOperation
     */
    dataType: string;
    /**
     * The current status of the dataset.
     * @type {string}
     * @memberof PullOperation
     */
    status: DatasetStatus;
    /**
     * Additional information about the dataset status.
     * @type {string}
     * @memberof PullOperation
     */
    statusDescription?: string | null;
    /**
     * A message about a transient or persistent error returned by Codat or the source platform.
     * @type {string}
     * @memberof PullOperation
     */
    errorMessage?: string | null;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof PullOperation
     */
    requested: string;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof PullOperation
     */
    completed?: string;
    /**
     * An integer signifying the progress of the pull operation.
     * @type {number}
     * @memberof PullOperation
     */
    progress: number;
    /**
     * `True` if the pull operation is completed successfully. The `isCompleted` property is not queryable. To filter failed pull operations, query by `status!=Complete&&status!=NotSupported` instead.
     * @type {boolean}
     * @memberof PullOperation
     */
    isCompleted: boolean;
    /**
     * `True` if the pull operation entered an error state.
     * @type {boolean}
     * @memberof PullOperation
     */
    isErrored: boolean;
}

export interface PullOperationsPullOperation {
    /**
     * Unique identifier of the pull operation.
     * @type {string}
     * @memberof PullOperation
     */
    id: string;
    /**
     * Unique identifier of the company associated to this pull operation.
     * @type {string}
     * @memberof PullOperation
     */
    companyId: string;
    /**
     * Unique identifier of the connection associated to this pull operation.
     * @type {string}
     * @memberof PullOperation
     */
    connectionId: string;
    /**
     * The data type you are requesting in a pull operation.
     * @type {string}
     * @memberof PullOperation
     */
    dataType: string;
    /**
     * The current status of the dataset.
     * @type {string}
     * @memberof PullOperation
     */
    status: DatasetStatus;
    /**
     * Additional information about the dataset status.
     * @type {string}
     * @memberof PullOperation
     */
    statusDescription?: string | null;
    /**
     * A message about a transient or persistent error returned by Codat or the source platform.
     * @type {string}
     * @memberof PullOperation
     */
    errorMessage?: string | null;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof PullOperation
     */
    requested: string;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof PullOperation
     */
    completed?: string;
    /**
     * An integer signifying the progress of the pull operation.
     * @type {number}
     * @memberof PullOperation
     */
    progress: number;
    /**
     * `True` if the pull operation is completed successfully. The `isCompleted` property is not queryable. To filter failed pull operations, query by `status!=Complete&&status!=NotSupported` instead.
     * @type {boolean}
     * @memberof PullOperation
     */
    isCompleted: boolean;
    /**
     * `True` if the pull operation entered an error state.
     * @type {boolean}
     * @memberof PullOperation
     */
    isErrored: boolean;
}

export interface RefreshDataTypeResponsePullOperation {
    /**
     * Unique identifier of the pull operation.
     * @type {string}
     * @memberof PullOperation
     */
    id: string;
    /**
     * Unique identifier of the company associated to this pull operation.
     * @type {string}
     * @memberof PullOperation
     */
    companyId: string;
    /**
     * Unique identifier of the connection associated to this pull operation.
     * @type {string}
     * @memberof PullOperation
     */
    connectionId: string;
    /**
     * The data type you are requesting in a pull operation.
     * @type {string}
     * @memberof PullOperation
     */
    dataType: string;
    /**
     * The current status of the dataset.
     * @type {string}
     * @memberof PullOperation
     */
    status: DatasetStatus;
    /**
     * Additional information about the dataset status.
     * @type {string}
     * @memberof PullOperation
     */
    statusDescription?: string | null;
    /**
     * A message about a transient or persistent error returned by Codat or the source platform.
     * @type {string}
     * @memberof PullOperation
     */
    errorMessage?: string | null;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof PullOperation
     */
    requested: string;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof PullOperation
     */
    completed?: string;
    /**
     * An integer signifying the progress of the pull operation.
     * @type {number}
     * @memberof PullOperation
     */
    progress: number;
    /**
     * `True` if the pull operation is completed successfully. The `isCompleted` property is not queryable. To filter failed pull operations, query by `status!=Complete&&status!=NotSupported` instead.
     * @type {boolean}
     * @memberof PullOperation
     */
    isCompleted: boolean;
    /**
     * `True` if the pull operation entered an error state.
     * @type {boolean}
     * @memberof PullOperation
     */
    isErrored: boolean;
}

export interface StartBankStatementUploadSessionResponsePullOperation {
    /**
     * Unique identifier of the pull operation.
     * @type {string}
     * @memberof PullOperation
     */
    id: string;
    /**
     * Unique identifier of the company associated to this pull operation.
     * @type {string}
     * @memberof PullOperation
     */
    companyId: string;
    /**
     * Unique identifier of the connection associated to this pull operation.
     * @type {string}
     * @memberof PullOperation
     */
    connectionId: string;
    /**
     * The data type you are requesting in a pull operation.
     * @type {string}
     * @memberof PullOperation
     */
    dataType: string;
    /**
     * The current status of the dataset.
     * @type {string}
     * @memberof PullOperation
     */
    status: DatasetStatus;
    /**
     * Additional information about the dataset status.
     * @type {string}
     * @memberof PullOperation
     */
    statusDescription?: string | null;
    /**
     * A message about a transient or persistent error returned by Codat or the source platform.
     * @type {string}
     * @memberof PullOperation
     */
    errorMessage?: string | null;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof PullOperation
     */
    requested: string;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof PullOperation
     */
    completed?: string;
    /**
     * An integer signifying the progress of the pull operation.
     * @type {number}
     * @memberof PullOperation
     */
    progress: number;
    /**
     * `True` if the pull operation is completed successfully. The `isCompleted` property is not queryable. To filter failed pull operations, query by `status!=Complete&&status!=NotSupported` instead.
     * @type {boolean}
     * @memberof PullOperation
     */
    isCompleted: boolean;
    /**
     * `True` if the pull operation entered an error state.
     * @type {boolean}
     * @memberof PullOperation
     */
    isErrored: boolean;
}

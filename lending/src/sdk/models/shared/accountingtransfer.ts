/* tslint:disable */
/* eslint-disable */
/**
 * Lending
 * Our Lending solution helps you make smarter credit decisions on small businesses by enabling you to pull your customers\' latest data from accounting, banking, and commerce software they are already using. It also includes features to help providers verify the accuracy of data and process it more efficiently.  The Lending solution is built on top of the latest accounting, commerce, and banking data, providing you with the most important data points you need to get a full picture of SMB creditworthiness and make a comprehensive assessment of your customers.  [Explore solution](https://docs.codat.io/lending/overview) | [See OpenAPI spec](https://github.com/codatio/oas)  <!-- Start Codat Tags Table --> ## Endpoints  | Endpoints | Description | | :- |:- | | Companies | Create and manage your SMB users\' companies. | | Connections | Create new and manage existing data connections for a company. | | Bank statements | Retrieve banking data from linked bank accounts. | | Sales | Retrieve standardized sales data from a linked commerce software. | | Financial statements | Financial data and reports from a linked accounting software. | | Liabilities | Debt and other liabilities. | | Accounts payable | Data from a linked accounting software representing money the business owes money to its suppliers. | | Accounts receivable | Data from a linked accounting software representing money owed to the business for sold goods or services. | | Transactions | Data from a linked accounting software representing transactions. | | Company info | View company information fetched from the source platform. | | Data integrity | Match mutable accounting data with immutable banking data to increase confidence in financial data. | | Excel reports | Download reports in Excel format. | | Manage data | Control how data is retrieved from an integration. | | File upload | Endpoints to manage uploaded files. | | Loan writeback | Implement the [loan writeback](https://docs.codat.io/lending/guides/loan-writeback/introduction) procedure in your lending process to maintain an accurate position of a loan during the entire lending cycle. | <!-- End Codat Tags Table -->
 *
 * The version of the OpenAPI document: 3.0.0
 * Contact: support@codat.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import type { AccountingCreateTransferResponseStatus } from '../_speakeasy_enums';
import type { AccountingTransferStatus } from '../_speakeasy_enums';
import { mapValues } from '../../../runtime';
import type { TrackingCategoryRef } from './trackingcategoryref';
import {
    TrackingCategoryRefFromJSON,
    TrackingCategoryRefFromJSONTyped,
    TrackingCategoryRefToJSON,
    TrackingCategoryRefToJSONTyped,
} from './trackingcategoryref';
import type { AccountingRecordRef } from './accountingrecordref';
import {
    AccountingRecordRefFromJSON,
    AccountingRecordRefFromJSONTyped,
    AccountingRecordRefToJSON,
    AccountingRecordRefToJSONTyped,
} from './accountingrecordref';
import type { SupplementalData } from './supplementaldata';
import {
    SupplementalDataFromJSON,
    SupplementalDataFromJSONTyped,
    SupplementalDataToJSON,
    SupplementalDataToJSONTyped,
} from './supplementaldata';
import type { Metadata } from './metadata';
import {
    MetadataFromJSON,
    MetadataFromJSONTyped,
    MetadataToJSON,
    MetadataToJSONTyped,
} from './metadata';
import type { ContactRef } from './contactref';
import {
    ContactRefFromJSON,
    ContactRefFromJSONTyped,
    ContactRefToJSON,
    ContactRefToJSONTyped,
} from './contactref';
import type { TransferAccount } from './transferaccount';
import {
    TransferAccountFromJSON,
    TransferAccountFromJSONTyped,
    TransferAccountToJSON,
    TransferAccountToJSONTyped,
} from './transferaccount';

/**
 * A transfer records the movement of money between two bank accounts, or between a bank account and a nominal account. It is a child data type of [account transactions](https://docs.codat.io/lending-api#/schemas/AccountTransaction).
 * @export
 * @interface AccountingTransfer
 */
export interface AccountingTransfer {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingTransfer
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingTransfer
     */
    sourceModifiedDate?: string;
    /**
     * Unique identifier for the transfer.
     * @type {string}
     * @memberof AccountingTransfer
     */
    id?: string;
    /**
     * Description of the transfer.
     * @type {string}
     * @memberof AccountingTransfer
     */
    description?: string | null;
    /**
     * The customer or supplier for the transfer, if available.
     * @type {ContactRef}
     * @memberof AccountingTransfer
     */
    contactRef?: ContactRef;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof AccountingTransfer
     */
    date?: string;
    /**
     * The details of the accounts the transfer is moving from.
     * @type {TransferAccount}
     * @memberof AccountingTransfer
     */
    from?: TransferAccount;
    /**
     * The details of the accounts the transfer is moving to.
     * @type {TransferAccount}
     * @memberof AccountingTransfer
     */
    to?: TransferAccount;
    /**
     * The status of the transfer in the account
     * @type {string}
     * @memberof AccountingTransfer
     */
    status?: AccountingTransferStatus | null;
    /**
     * Reference to the tracking categories this transfer is being tracked against.
     * @type {Array<TrackingCategoryRef>}
     * @memberof AccountingTransfer
     */
    trackingCategoryRefs?: Array<TrackingCategoryRef> | null;
    /**
     * List of selected transactions to associate with the transfer. Use this field to include transactions which are posted to the _undeposited funds_ (or other holding) account within the transfer.
     * @type {Array<AccountingRecordRef>}
     * @memberof AccountingTransfer
     */
    depositedRecordRefs?: Array<AccountingRecordRef> | null;
    /**
     * 
     * @type {Metadata}
     * @memberof AccountingTransfer
     */
    metadata?: Metadata;
    /**
     * 
     * @type {SupplementalData}
     * @memberof AccountingTransfer
     */
    supplementalData?: SupplementalData;
}


/**
 * @export
 */
export const AccountingTransferStatusEnum = {
    Unknown: 'Unknown',
    Unreconciled: 'Unreconciled',
    Reconciled: 'Reconciled',
    Void: 'Void'
} as const;
export type AccountingTransferStatusEnum = typeof AccountingTransferStatusEnum[keyof typeof AccountingTransferStatusEnum];


/**
 * Check if a given object implements the AccountingTransfer interface.
 */
export function instanceOfAccountingTransfer(value: object): value is AccountingTransfer {
    return true;
}

export function AccountingTransferFromJSON(json: any): AccountingTransfer {
    return AccountingTransferFromJSONTyped(json, false);
}

export function AccountingTransferFromJSONTyped(json: any, ignoreDiscriminator: boolean): AccountingTransfer {
    if (json == null) {
        return json;
    }
    return {
        
        ...(json['modifiedDate'] !== undefined ? { 'modifiedDate': json['modifiedDate'] == null ? undefined : json['modifiedDate'] } : {}),
        ...(json['sourceModifiedDate'] !== undefined ? { 'sourceModifiedDate': json['sourceModifiedDate'] == null ? undefined : json['sourceModifiedDate'] } : {}),
        ...(json['id'] !== undefined ? { 'id': json['id'] == null ? undefined : json['id'] } : {}),
        ...(json['description'] !== undefined ? { 'description': json['description'] } : {}),
        ...(json['contactRef'] !== undefined ? { 'contactRef': json['contactRef'] == null ? undefined : ContactRefFromJSON(json['contactRef']) } : {}),
        ...(json['date'] !== undefined ? { 'date': json['date'] == null ? undefined : json['date'] } : {}),
        ...(json['from'] !== undefined ? { 'from': json['from'] == null ? undefined : TransferAccountFromJSON(json['from']) } : {}),
        ...(json['to'] !== undefined ? { 'to': json['to'] == null ? undefined : TransferAccountFromJSON(json['to']) } : {}),
        ...(json['status'] !== undefined ? { 'status': json['status'] } : {}),
        ...(json['trackingCategoryRefs'] !== undefined ? { 'trackingCategoryRefs': json['trackingCategoryRefs'] == null ? json['trackingCategoryRefs'] : ((json['trackingCategoryRefs'] as Array<any>).map(TrackingCategoryRefFromJSON)) } : {}),
        ...(json['depositedRecordRefs'] !== undefined ? { 'depositedRecordRefs': json['depositedRecordRefs'] == null ? json['depositedRecordRefs'] : ((json['depositedRecordRefs'] as Array<any>).map(AccountingRecordRefFromJSON)) } : {}),
        ...(json['metadata'] !== undefined ? { 'metadata': json['metadata'] == null ? undefined : MetadataFromJSON(json['metadata']) } : {}),
        ...(json['supplementalData'] !== undefined ? { 'supplementalData': json['supplementalData'] == null ? undefined : SupplementalDataFromJSON(json['supplementalData']) } : {}),
    };
}

export function AccountingTransferToJSON(json: any): AccountingTransfer {
    return AccountingTransferToJSONTyped(json, false);
}

export function AccountingTransferToJSONTyped(value?: AccountingTransfer | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'modifiedDate': value['modifiedDate'],
        'sourceModifiedDate': value['sourceModifiedDate'],
        'id': value['id'],
        'description': value['description'],
        'contactRef': ContactRefToJSON(value['contactRef']),
        'date': value['date'],
        'from': TransferAccountToJSON(value['from']),
        'to': TransferAccountToJSON(value['to']),
        'status': value['status'],
        'trackingCategoryRefs': value['trackingCategoryRefs'] == null ? value['trackingCategoryRefs'] : ((value['trackingCategoryRefs'] as Array<any>).map(TrackingCategoryRefToJSON)),
        'depositedRecordRefs': value['depositedRecordRefs'] == null ? value['depositedRecordRefs'] : ((value['depositedRecordRefs'] as Array<any>).map(AccountingRecordRefToJSON)),
        'metadata': MetadataToJSON(value['metadata']),
        'supplementalData': SupplementalDataToJSON(value['supplementalData']),
    };
}


export interface AccountingCreateTransferResponseAccountingTransfer {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingTransfer
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingTransfer
     */
    sourceModifiedDate?: string;
    /**
     * Unique identifier for the transfer.
     * @type {string}
     * @memberof AccountingTransfer
     */
    id?: string;
    /**
     * Description of the transfer.
     * @type {string}
     * @memberof AccountingTransfer
     */
    description?: string | null;
    /**
     * The customer or supplier for the transfer, if available.
     * @type {ContactRef}
     * @memberof AccountingTransfer
     */
    contactRef?: ContactRef;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof AccountingTransfer
     */
    date?: string;
    /**
     * The details of the accounts the transfer is moving from.
     * @type {TransferAccount}
     * @memberof AccountingTransfer
     */
    from?: TransferAccount;
    /**
     * The details of the accounts the transfer is moving to.
     * @type {TransferAccount}
     * @memberof AccountingTransfer
     */
    to?: TransferAccount;
    /**
     * The status of the transfer in the account
     * @type {string}
     * @memberof AccountingTransfer
     */
    status?: AccountingCreateTransferResponseStatus | null;
    /**
     * Reference to the tracking categories this transfer is being tracked against.
     * @type {Array<TrackingCategoryRef>}
     * @memberof AccountingTransfer
     */
    trackingCategoryRefs?: Array<TrackingCategoryRef> | null;
    /**
     * List of selected transactions to associate with the transfer. Use this field to include transactions which are posted to the _undeposited funds_ (or other holding) account within the transfer.
     * @type {Array<AccountingRecordRef>}
     * @memberof AccountingTransfer
     */
    depositedRecordRefs?: Array<AccountingRecordRef> | null;
    /**
     * 
     * @type {Metadata}
     * @memberof AccountingTransfer
     */
    metadata?: Metadata;
    /**
     * 
     * @type {SupplementalData}
     * @memberof AccountingTransfer
     */
    supplementalData?: SupplementalData;
}

export interface AccountingTransferGetResponseAccountingTransfer {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingTransfer
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingTransfer
     */
    sourceModifiedDate?: string;
    /**
     * Unique identifier for the transfer.
     * @type {string}
     * @memberof AccountingTransfer
     */
    id?: string;
    /**
     * Description of the transfer.
     * @type {string}
     * @memberof AccountingTransfer
     */
    description?: string | null;
    /**
     * The customer or supplier for the transfer, if available.
     * @type {ContactRef}
     * @memberof AccountingTransfer
     */
    contactRef?: ContactRef;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof AccountingTransfer
     */
    date?: string;
    /**
     * The details of the accounts the transfer is moving from.
     * @type {TransferAccount}
     * @memberof AccountingTransfer
     */
    from?: TransferAccount;
    /**
     * The details of the accounts the transfer is moving to.
     * @type {TransferAccount}
     * @memberof AccountingTransfer
     */
    to?: TransferAccount;
    /**
     * The status of the transfer in the account
     * @type {string}
     * @memberof AccountingTransfer
     */
    status?: AccountingTransferStatus | null;
    /**
     * Reference to the tracking categories this transfer is being tracked against.
     * @type {Array<TrackingCategoryRef>}
     * @memberof AccountingTransfer
     */
    trackingCategoryRefs?: Array<TrackingCategoryRef> | null;
    /**
     * List of selected transactions to associate with the transfer. Use this field to include transactions which are posted to the _undeposited funds_ (or other holding) account within the transfer.
     * @type {Array<AccountingRecordRef>}
     * @memberof AccountingTransfer
     */
    depositedRecordRefs?: Array<AccountingRecordRef> | null;
    /**
     * 
     * @type {Metadata}
     * @memberof AccountingTransfer
     */
    metadata?: Metadata;
    /**
     * 
     * @type {SupplementalData}
     * @memberof AccountingTransfer
     */
    supplementalData?: SupplementalData;
}

export interface AccountingTransfersAccountingTransfer {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingTransfer
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingTransfer
     */
    sourceModifiedDate?: string;
    /**
     * Unique identifier for the transfer.
     * @type {string}
     * @memberof AccountingTransfer
     */
    id?: string;
    /**
     * Description of the transfer.
     * @type {string}
     * @memberof AccountingTransfer
     */
    description?: string | null;
    /**
     * The customer or supplier for the transfer, if available.
     * @type {ContactRef}
     * @memberof AccountingTransfer
     */
    contactRef?: ContactRef;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof AccountingTransfer
     */
    date?: string;
    /**
     * The details of the accounts the transfer is moving from.
     * @type {TransferAccount}
     * @memberof AccountingTransfer
     */
    from?: TransferAccount;
    /**
     * The details of the accounts the transfer is moving to.
     * @type {TransferAccount}
     * @memberof AccountingTransfer
     */
    to?: TransferAccount;
    /**
     * The status of the transfer in the account
     * @type {string}
     * @memberof AccountingTransfer
     */
    status?: AccountingTransferStatus | null;
    /**
     * Reference to the tracking categories this transfer is being tracked against.
     * @type {Array<TrackingCategoryRef>}
     * @memberof AccountingTransfer
     */
    trackingCategoryRefs?: Array<TrackingCategoryRef> | null;
    /**
     * List of selected transactions to associate with the transfer. Use this field to include transactions which are posted to the _undeposited funds_ (or other holding) account within the transfer.
     * @type {Array<AccountingRecordRef>}
     * @memberof AccountingTransfer
     */
    depositedRecordRefs?: Array<AccountingRecordRef> | null;
    /**
     * 
     * @type {Metadata}
     * @memberof AccountingTransfer
     */
    metadata?: Metadata;
    /**
     * 
     * @type {SupplementalData}
     * @memberof AccountingTransfer
     */
    supplementalData?: SupplementalData;
}

export interface AccountingTransfersListResponseAccountingTransfer {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingTransfer
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingTransfer
     */
    sourceModifiedDate?: string;
    /**
     * Unique identifier for the transfer.
     * @type {string}
     * @memberof AccountingTransfer
     */
    id?: string;
    /**
     * Description of the transfer.
     * @type {string}
     * @memberof AccountingTransfer
     */
    description?: string | null;
    /**
     * The customer or supplier for the transfer, if available.
     * @type {ContactRef}
     * @memberof AccountingTransfer
     */
    contactRef?: ContactRef;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof AccountingTransfer
     */
    date?: string;
    /**
     * The details of the accounts the transfer is moving from.
     * @type {TransferAccount}
     * @memberof AccountingTransfer
     */
    from?: TransferAccount;
    /**
     * The details of the accounts the transfer is moving to.
     * @type {TransferAccount}
     * @memberof AccountingTransfer
     */
    to?: TransferAccount;
    /**
     * The status of the transfer in the account
     * @type {string}
     * @memberof AccountingTransfer
     */
    status?: AccountingTransferStatus | null;
    /**
     * Reference to the tracking categories this transfer is being tracked against.
     * @type {Array<TrackingCategoryRef>}
     * @memberof AccountingTransfer
     */
    trackingCategoryRefs?: Array<TrackingCategoryRef> | null;
    /**
     * List of selected transactions to associate with the transfer. Use this field to include transactions which are posted to the _undeposited funds_ (or other holding) account within the transfer.
     * @type {Array<AccountingRecordRef>}
     * @memberof AccountingTransfer
     */
    depositedRecordRefs?: Array<AccountingRecordRef> | null;
    /**
     * 
     * @type {Metadata}
     * @memberof AccountingTransfer
     */
    metadata?: Metadata;
    /**
     * 
     * @type {SupplementalData}
     * @memberof AccountingTransfer
     */
    supplementalData?: SupplementalData;
}

export interface CreateTransferAccountingTransfer {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingTransfer
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingTransfer
     */
    sourceModifiedDate?: string;
    /**
     * Unique identifier for the transfer.
     * @type {string}
     * @memberof AccountingTransfer
     */
    id?: string;
    /**
     * Description of the transfer.
     * @type {string}
     * @memberof AccountingTransfer
     */
    description?: string | null;
    /**
     * The customer or supplier for the transfer, if available.
     * @type {ContactRef}
     * @memberof AccountingTransfer
     */
    contactRef?: ContactRef;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof AccountingTransfer
     */
    date?: string;
    /**
     * The details of the accounts the transfer is moving from.
     * @type {TransferAccount}
     * @memberof AccountingTransfer
     */
    from?: TransferAccount;
    /**
     * The details of the accounts the transfer is moving to.
     * @type {TransferAccount}
     * @memberof AccountingTransfer
     */
    to?: TransferAccount;
    /**
     * The status of the transfer in the account
     * @type {string}
     * @memberof AccountingTransfer
     */
    status?: AccountingTransferStatus | null;
    /**
     * Reference to the tracking categories this transfer is being tracked against.
     * @type {Array<TrackingCategoryRef>}
     * @memberof AccountingTransfer
     */
    trackingCategoryRefs?: Array<TrackingCategoryRef> | null;
    /**
     * List of selected transactions to associate with the transfer. Use this field to include transactions which are posted to the _undeposited funds_ (or other holding) account within the transfer.
     * @type {Array<AccountingRecordRef>}
     * @memberof AccountingTransfer
     */
    depositedRecordRefs?: Array<AccountingRecordRef> | null;
    /**
     * 
     * @type {Metadata}
     * @memberof AccountingTransfer
     */
    metadata?: Metadata;
    /**
     * 
     * @type {SupplementalData}
     * @memberof AccountingTransfer
     */
    supplementalData?: SupplementalData;
}

export interface CreateTransferRequestBody {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingTransfer
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingTransfer
     */
    sourceModifiedDate?: string;
    /**
     * Unique identifier for the transfer.
     * @type {string}
     * @memberof AccountingTransfer
     */
    id?: string;
    /**
     * Description of the transfer.
     * @type {string}
     * @memberof AccountingTransfer
     */
    description?: string | null;
    /**
     * The customer or supplier for the transfer, if available.
     * @type {ContactRef}
     * @memberof AccountingTransfer
     */
    contactRef?: ContactRef;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof AccountingTransfer
     */
    date?: string;
    /**
     * The details of the accounts the transfer is moving from.
     * @type {TransferAccount}
     * @memberof AccountingTransfer
     */
    from?: TransferAccount;
    /**
     * The details of the accounts the transfer is moving to.
     * @type {TransferAccount}
     * @memberof AccountingTransfer
     */
    to?: TransferAccount;
    /**
     * The status of the transfer in the account
     * @type {string}
     * @memberof AccountingTransfer
     */
    status?: AccountingTransferStatus | null;
    /**
     * Reference to the tracking categories this transfer is being tracked against.
     * @type {Array<TrackingCategoryRef>}
     * @memberof AccountingTransfer
     */
    trackingCategoryRefs?: Array<TrackingCategoryRef> | null;
    /**
     * List of selected transactions to associate with the transfer. Use this field to include transactions which are posted to the _undeposited funds_ (or other holding) account within the transfer.
     * @type {Array<AccountingRecordRef>}
     * @memberof AccountingTransfer
     */
    depositedRecordRefs?: Array<AccountingRecordRef> | null;
    /**
     * 
     * @type {Metadata}
     * @memberof AccountingTransfer
     */
    metadata?: Metadata;
    /**
     * 
     * @type {SupplementalData}
     * @memberof AccountingTransfer
     */
    supplementalData?: SupplementalData;
}

export interface CreateTransferResponseAccountingTransfer {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingTransfer
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingTransfer
     */
    sourceModifiedDate?: string;
    /**
     * Unique identifier for the transfer.
     * @type {string}
     * @memberof AccountingTransfer
     */
    id?: string;
    /**
     * Description of the transfer.
     * @type {string}
     * @memberof AccountingTransfer
     */
    description?: string | null;
    /**
     * The customer or supplier for the transfer, if available.
     * @type {ContactRef}
     * @memberof AccountingTransfer
     */
    contactRef?: ContactRef;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof AccountingTransfer
     */
    date?: string;
    /**
     * The details of the accounts the transfer is moving from.
     * @type {TransferAccount}
     * @memberof AccountingTransfer
     */
    from?: TransferAccount;
    /**
     * The details of the accounts the transfer is moving to.
     * @type {TransferAccount}
     * @memberof AccountingTransfer
     */
    to?: TransferAccount;
    /**
     * The status of the transfer in the account
     * @type {string}
     * @memberof AccountingTransfer
     */
    status?: AccountingTransferStatus | null;
    /**
     * Reference to the tracking categories this transfer is being tracked against.
     * @type {Array<TrackingCategoryRef>}
     * @memberof AccountingTransfer
     */
    trackingCategoryRefs?: Array<TrackingCategoryRef> | null;
    /**
     * List of selected transactions to associate with the transfer. Use this field to include transactions which are posted to the _undeposited funds_ (or other holding) account within the transfer.
     * @type {Array<AccountingRecordRef>}
     * @memberof AccountingTransfer
     */
    depositedRecordRefs?: Array<AccountingRecordRef> | null;
    /**
     * 
     * @type {Metadata}
     * @memberof AccountingTransfer
     */
    metadata?: Metadata;
    /**
     * 
     * @type {SupplementalData}
     * @memberof AccountingTransfer
     */
    supplementalData?: SupplementalData;
}

export interface GetAccountingTransferResponseAccountingTransfer {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingTransfer
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingTransfer
     */
    sourceModifiedDate?: string;
    /**
     * Unique identifier for the transfer.
     * @type {string}
     * @memberof AccountingTransfer
     */
    id?: string;
    /**
     * Description of the transfer.
     * @type {string}
     * @memberof AccountingTransfer
     */
    description?: string | null;
    /**
     * The customer or supplier for the transfer, if available.
     * @type {ContactRef}
     * @memberof AccountingTransfer
     */
    contactRef?: ContactRef;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof AccountingTransfer
     */
    date?: string;
    /**
     * The details of the accounts the transfer is moving from.
     * @type {TransferAccount}
     * @memberof AccountingTransfer
     */
    from?: TransferAccount;
    /**
     * The details of the accounts the transfer is moving to.
     * @type {TransferAccount}
     * @memberof AccountingTransfer
     */
    to?: TransferAccount;
    /**
     * The status of the transfer in the account
     * @type {string}
     * @memberof AccountingTransfer
     */
    status?: AccountingTransferStatus | null;
    /**
     * Reference to the tracking categories this transfer is being tracked against.
     * @type {Array<TrackingCategoryRef>}
     * @memberof AccountingTransfer
     */
    trackingCategoryRefs?: Array<TrackingCategoryRef> | null;
    /**
     * List of selected transactions to associate with the transfer. Use this field to include transactions which are posted to the _undeposited funds_ (or other holding) account within the transfer.
     * @type {Array<AccountingRecordRef>}
     * @memberof AccountingTransfer
     */
    depositedRecordRefs?: Array<AccountingRecordRef> | null;
    /**
     * 
     * @type {Metadata}
     * @memberof AccountingTransfer
     */
    metadata?: Metadata;
    /**
     * 
     * @type {SupplementalData}
     * @memberof AccountingTransfer
     */
    supplementalData?: SupplementalData;
}

export interface ListAccountingTransfersResponseAccountingTransfer {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingTransfer
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingTransfer
     */
    sourceModifiedDate?: string;
    /**
     * Unique identifier for the transfer.
     * @type {string}
     * @memberof AccountingTransfer
     */
    id?: string;
    /**
     * Description of the transfer.
     * @type {string}
     * @memberof AccountingTransfer
     */
    description?: string | null;
    /**
     * The customer or supplier for the transfer, if available.
     * @type {ContactRef}
     * @memberof AccountingTransfer
     */
    contactRef?: ContactRef;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof AccountingTransfer
     */
    date?: string;
    /**
     * The details of the accounts the transfer is moving from.
     * @type {TransferAccount}
     * @memberof AccountingTransfer
     */
    from?: TransferAccount;
    /**
     * The details of the accounts the transfer is moving to.
     * @type {TransferAccount}
     * @memberof AccountingTransfer
     */
    to?: TransferAccount;
    /**
     * The status of the transfer in the account
     * @type {string}
     * @memberof AccountingTransfer
     */
    status?: AccountingTransferStatus | null;
    /**
     * Reference to the tracking categories this transfer is being tracked against.
     * @type {Array<TrackingCategoryRef>}
     * @memberof AccountingTransfer
     */
    trackingCategoryRefs?: Array<TrackingCategoryRef> | null;
    /**
     * List of selected transactions to associate with the transfer. Use this field to include transactions which are posted to the _undeposited funds_ (or other holding) account within the transfer.
     * @type {Array<AccountingRecordRef>}
     * @memberof AccountingTransfer
     */
    depositedRecordRefs?: Array<AccountingRecordRef> | null;
    /**
     * 
     * @type {Metadata}
     * @memberof AccountingTransfer
     */
    metadata?: Metadata;
    /**
     * 
     * @type {SupplementalData}
     * @memberof AccountingTransfer
     */
    supplementalData?: SupplementalData;
}

export interface TransferCreateRequestBody {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingTransfer
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingTransfer
     */
    sourceModifiedDate?: string;
    /**
     * Unique identifier for the transfer.
     * @type {string}
     * @memberof AccountingTransfer
     */
    id?: string;
    /**
     * Description of the transfer.
     * @type {string}
     * @memberof AccountingTransfer
     */
    description?: string | null;
    /**
     * The customer or supplier for the transfer, if available.
     * @type {ContactRef}
     * @memberof AccountingTransfer
     */
    contactRef?: ContactRef;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof AccountingTransfer
     */
    date?: string;
    /**
     * The details of the accounts the transfer is moving from.
     * @type {TransferAccount}
     * @memberof AccountingTransfer
     */
    from?: TransferAccount;
    /**
     * The details of the accounts the transfer is moving to.
     * @type {TransferAccount}
     * @memberof AccountingTransfer
     */
    to?: TransferAccount;
    /**
     * The status of the transfer in the account
     * @type {string}
     * @memberof AccountingTransfer
     */
    status?: AccountingTransferStatus | null;
    /**
     * Reference to the tracking categories this transfer is being tracked against.
     * @type {Array<TrackingCategoryRef>}
     * @memberof AccountingTransfer
     */
    trackingCategoryRefs?: Array<TrackingCategoryRef> | null;
    /**
     * List of selected transactions to associate with the transfer. Use this field to include transactions which are posted to the _undeposited funds_ (or other holding) account within the transfer.
     * @type {Array<AccountingRecordRef>}
     * @memberof AccountingTransfer
     */
    depositedRecordRefs?: Array<AccountingRecordRef> | null;
    /**
     * 
     * @type {Metadata}
     * @memberof AccountingTransfer
     */
    metadata?: Metadata;
    /**
     * 
     * @type {SupplementalData}
     * @memberof AccountingTransfer
     */
    supplementalData?: SupplementalData;
}

export interface TransferCreateResponseAccountingTransfer {
    /**
     * The date when the record was last fetched from the accounting software, commerce software, or open banking provider and updated in Codat’s data cache.
     * 
     * Use it to identify and retrieve records that have changed since your last fetch. For example, filtering `modifiedDate` to today will provide new records updated in Codat today.
     * 
     * This date is populated for all data types except for attachments, balance sheets, company information, and profit & loss reports ([read more](https://docs.codat.io/using-the-api/modified-dates#modified-date)).
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingTransfer
     */
    modifiedDate?: string;
    /**
     * The date when a record was last modified in the accounting software, usually by the business or a business process. For example, when payments are made against an invoice. 
     * 
     * It is not populated ([read more](https://docs.codat.io/using-the-api/modified-dates#source-modified-date)) when:
     *   - Pulling attachments
     *   - The accounting software does not provide modification dates for a data type
     *   - A record has been deleted from the source platform and Codat doesn't have a record of when the deletion occurred
     *   - A record has been voided. For certain platforms that soft delete records, `isDeleted` metadata is used to identify void records
     * 
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>.
     * @type {string}
     * @memberof AccountingTransfer
     */
    sourceModifiedDate?: string;
    /**
     * Unique identifier for the transfer.
     * @type {string}
     * @memberof AccountingTransfer
     */
    id?: string;
    /**
     * Description of the transfer.
     * @type {string}
     * @memberof AccountingTransfer
     */
    description?: string | null;
    /**
     * The customer or supplier for the transfer, if available.
     * @type {ContactRef}
     * @memberof AccountingTransfer
     */
    contactRef?: ContactRef;
    /**
     * In Codat's data model, dates and times are represented using the <a class="external" href="https://en.wikipedia.org/wiki/ISO_8601" target="_blank">ISO 8601 standard</a>. Date and time fields are formatted as strings; for example:
     * 
     * ```
     * 2020-10-08T22:40:50Z
     * 2021-01-01T00:00:00
     * ```
     * 
     * 
     * 
     * When syncing data that contains `DateTime` fields from Codat, make sure you support the following cases when reading time information:
     * 
     * - Coordinated Universal Time (UTC): `2021-11-15T06:00:00Z`
     * - Unqualified local time: `2021-11-15T01:00:00`
     * - UTC time offsets: `2021-11-15T01:00:00-05:00`
     * 
     * > Time zones
     * > 
     * > Not all dates from Codat will contain information about time zones.  
     * > Where it is not available from the underlying platform, Codat will return these as times local to the business whose data has been synced.
     * @type {string}
     * @memberof AccountingTransfer
     */
    date?: string;
    /**
     * The details of the accounts the transfer is moving from.
     * @type {TransferAccount}
     * @memberof AccountingTransfer
     */
    from?: TransferAccount;
    /**
     * The details of the accounts the transfer is moving to.
     * @type {TransferAccount}
     * @memberof AccountingTransfer
     */
    to?: TransferAccount;
    /**
     * The status of the transfer in the account
     * @type {string}
     * @memberof AccountingTransfer
     */
    status?: AccountingTransferStatus | null;
    /**
     * Reference to the tracking categories this transfer is being tracked against.
     * @type {Array<TrackingCategoryRef>}
     * @memberof AccountingTransfer
     */
    trackingCategoryRefs?: Array<TrackingCategoryRef> | null;
    /**
     * List of selected transactions to associate with the transfer. Use this field to include transactions which are posted to the _undeposited funds_ (or other holding) account within the transfer.
     * @type {Array<AccountingRecordRef>}
     * @memberof AccountingTransfer
     */
    depositedRecordRefs?: Array<AccountingRecordRef> | null;
    /**
     * 
     * @type {Metadata}
     * @memberof AccountingTransfer
     */
    metadata?: Metadata;
    /**
     * 
     * @type {SupplementalData}
     * @memberof AccountingTransfer
     */
    supplementalData?: SupplementalData;
}
